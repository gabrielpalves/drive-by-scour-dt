# Drive-by DT — framework rationale & decision log

A living record of **what** we decided, **why**, and the **references** that back it,
so the methodology section of the paper writes itself and every step is defensible
to reviewers. Newest decisions appended per section. Status ∈ {decided, built,
open}. (`built` = implemented + tested in the repo.)

> Maintained continuously: whenever we make or change a modelling choice, add an
> entry here with its justification and citation.

---

## 1. Classifier / sensors

**1.1 Champion = 2 sensors: RearBogie_Vert + CarBody_Pitch (DOF 2,5), PAA + N-HiTS, 61-class.** — *built*
Why: in the 540-run ablation this is the best 2-sensor pair (median MSE 0.414 ≈ full-8-DOF 0.386) and, unlike the alternative CarBody_Pitch + FrontBogie_Pitch, **both** of its single-sensor fall-backs are usable (CarBody_Pitch is the best single channel; FrontBogie_Pitch *alone* collapses 62% of the time). Sensor economy ("2 ≈ 8") is the headline of Paper 1.
Ref: our ablation (`ablation_analysis/summary_per_config.csv`). Sensor-economy framing aligns with drive-by SHM literature (Fernandes et al. 2024–2026).

**1.2 Observations sampled from a held-out MATLAB library, not live re-simulation.** — *built*
Why: one live TTBI passage is slow, and (historically) live Python re-sim mispredicted. Sampling a held-out set generated by the *training* pipeline removes train/serve skew and makes a fine monthly clock cheap (cf. Torzoni's pre-computed `recordings_*.npy`).
Status note (skew investigation, 2026-06-15): live Python re-sim **still** mispredicts — healthy is fine (0%→1%) but every damaged state is read as ~healthy (20/40/55% → 0–1%). Ruled out: the 13-vs-61-class metadata bug (fixed); the damage interface (Python's legacy `DOF_ChangeRate_value` maps to `scour_rates[n//2]` = central pier, same as MATLAB `A00`); and the **rail profile** (Python `TTBI_2D/x.mat`/`h.mat` are byte-identical to the MATLAB training profile `scour_MATLAB/Calc.ProfileData15_05.mat` — both pipelines use ONE fixed profile via `Profile.Type=2`). Remaining cause is in the **physics/geometry port**; prime suspect = the geometry constants forced in `b19_generate_profile.py`'s Type-2 debug block (`L=325.80`, approach `30/30`, `L_bridge=39.90`) vs `a04_options.py`'s `minL_Approach=16/minL_After=10`, which can misalign the (correct) profile against the simulation's actual track positions. Decisive next test (not yet run): compare a Python vs MATLAB signal at the same damage. **Practical decision:** generate the probe (and any DT observation) via MATLAB — it inherently uses the training pipeline. Repairing live Python is an optional, separable follow-up.

**Side-finding (record for the paper):** training AND the held-out set use a SINGLE FIXED rail profile (`Calc.ProfileData15_05.mat`, MATLAB `Profile.Type=2`). So rail-profile irregularity is NOT a source of operational variability in our data (only speed, temperature, vehicle properties, and signal noise are). Consistent (champion trained and served on the same profile), but a documented scope limit; rail-profile *as a damage type* (`profile_intensity`) is a separate, deliberate toggle.

---

## 2. Deterioration (scour)

**2.1 Scour = Kamariotis gradual power-law + Compound-Poisson flood shocks.** — *built*
Why: scour is flood-driven; a gradual creep plus discrete flood jumps is the accepted phenomenology.
Ref: Kamariotis et al. (2024), *Quantifying the value of SHM*.

**2.2 Multi-foundation correlation via a Gaussian copula** (river common factor + proximity term). — *built, optional*
Ref: Adnan (2026) for the foundation-proximity correlation term.

---

## 3. Cost / decision

**3.1 Monetary cost model with verified € figures** (c_failure 5e7, c_inspect 2e4, c_repair 6e5, downtime 1.5e5/day, 2% discount). — *built*
Ref: Kamariotis et al. (2024).
Flood-branch actions added 2026-06-24 (placeholders, set from project data): `restrict_operations` ≈ **3e3 €** (a couple of slow/light probe passages = a small reduced-capacity penalty, far below an inspection so the cheap probe has a real niche) and `interrupt` ≈ **3.2e5 €** (= inspection + ~2-day precautionary closure). Ordering do_nothing(0) < restrict(3e3) < inspect(2e4) < interrupt(3.2e5) < repair(5.1e6), so the flood planner escalates only as the risk warrants.

**3.2 Failure = a LOGNORMAL fragility P(fail | scour) = Φ((ln s − ln θ)/β), reported as a SWEPT assumption.** — *built 2026-06-15* (`costs.py::failure_prob` now lognormal; `calibrate.py::fragility_sensitivity` sweeps median θ × dispersion β; validated: P(0)=0, P(θ)=0.5, brittle β=0.05 near-step vs ductile β=0.4 gradual; cost-optimal repair threshold moves sensibly — brittle repairs later, ductile earlier)
Why: the scour literature uses a **lognormal** fragility CDF (not logistic) — justified by non-negativity of scour and multiplicative uncertainty. IM = scour intensity (our % stiffness loss, or relative scour depth Y = scour/foundation depth); θ = median capacity (50% failure point); β = lognormal dispersion. The planner ranking is not robust to the curve, so we **sweep** θ and β rather than commit to one. TTBI never models collapse — failure is a separate layer.
Literature parameters to bracket the sweep: masonry-arch (shallow, brittle) θ≈**1.52 m**, β≈**0.05** (near-step); deep piers β≈**0.3–0.4** (median geotech-dependent); counter-intuitively, piles in stiff clay can fail at *shallower* scour than in soft sand (loss of lateral confinement).
Refs: Maroni/Tubaldi/Douglas et al.; Lamb et al. (UK railway masonry); "A methodology to derive scour fragility functions for masonry arch bridges" (NSF/par.nsf.gov 10579270); "Flood-Induced Scour Fragility Analysis of Bridges in Different Soil Conditions" (E3S 2025); MCEER-03-0002 (lognormal fragility statistics). `digital_twin/calibrate.py::fragility_sensitivity`. **OPEN: implement the lognormal swap in `costs.py`; pick the swept θ/β range; decide whether IM = % stiffness loss or relative scour depth.**

**3.3 Planners: do_nothing / cost-VI (MDP) / heuristic (Kamariotis) / POMDP (myopic VoI) / hybrid.** — *built*
Why: a ladder from naive to information-aware; the POMDP is the only one that inspects rationally (inspection has purely informational value). Active inference is a planned upgrade (§5.3). A separate **`FloodResponsePlanner`** (§6.4) handles the major-flood branch for every routine planner, so "flood protocol ON vs OFF" isolates the value of flood-aware monitoring.

---

## 4. Risk perception

**4.1 Risk-sensitive objective via a coherent risk measure (CVaR / entropic), one aversion knob; default neutral = expectation.** — *built*
Why: a realistic asset owner is risk-averse to the catastrophic-failure tail; CVaR/entropic make that explicit and sweepable.
Ref: Chadha et al. (2023); Ames et al. (2025); Todd group (UCSD).
Finding (mock): with a cheap, sharp inspect available, risk-aversion buys **more monitoring** rather than fewer failures (the flood tail is largely irreducible by policy); without inspect, it repairs earlier. Robust across observation noise. **Figure 2026-06-24** (`plotting/risk_perception_figure.py` → `results/risk_perception/risk_perception.png`): sweeping CVaR_α from neutral to worst-5%, inspections per trajectory climb ~0.9 → 53 while **repairs stay flat (~0.1)** and **mean final damage is unchanged (~10.8%)**; the **CVaR_10 € tail rises** (10.8M → 12.9M) rather than shrinking. Mechanism: a cheap sharp inspection re-collapses the belief to a spike before the repair decision (CVaR≈mean on a spike), so risk routes into inspection; the catastrophic tail is dominated by sudden flood-shock jumps that outrun the inspect/repair loop. Cutting that tail needs faster monitoring or a shock-anticipating policy (→ the flood branch §6.4), not a more risk-averse objective.

---

## 5. Sensor health

**5.1 Three failure modes — transient burst / degraded / dead — with fault & noise scaling carbody < bogie < wheel.** — *built (placeholder rates)*
Why + ref: EN 61373 install categories give the physical basis and the vibration ratio. Functional RMS acceleration: **Cat 1 (car body) 1.01, Cat 2 (bogie) 5.40, Cat 3 (axle box) 38.0 m/s²** → wheel ≫ bogie ≫ car body, scaled by a vibration-fatigue acceleration law (Basquin/Steinberg, exponent ~6.4 for the S–N relation). Refs: EN 61373, EN 50155; Sandia *MEMS reliability in a vibration environment*; MDPI *Reliability of MEMS inertial devices…* (2024). See `papers/Confiabilidade MEMS Ferroviário.docx`.

**5.2 Degraded mode = persistent bias offset + scale-factor error + elevated noise** (not just noise). — *built*
Why: the dominant long-term MEMS failure is *parametric* (slow bias/scale-factor drift, VRE), not sudden death — the FMEDA "dangerous undetected" (λDU). VRE ≈ 0.5 °/h/g² (a 10 g bogie level → ~50 °/h bias). Time-to-parametric-failure (25 °/h bias) ≈ 1588 d at 25 °C → 558 d at 60 °C, i.e. car body ≫ bogie/axle lifespan. Arrhenius Ea ≈ 0.8–1.0 eV (general CMOS) / 0.16–0.22 eV (bias drift); infant mortality 0.3–5%; intermittent connector ≈ 200–500 FIT (Cat 3).
Caveat recorded in code: per-channel standardisation in our preprocessor largely removes a constant bias/scale, so for our PAA pipeline the *noise* term dominates the classifier impact; bias/scale are kept for raw-signal re-users and physical fidelity.
Refs: as 5.1 + MDPI *Reliability Assessment of MEMS Gyroscopes via Dual-Mechanism Synergistic Degradation (Physics-Informed Wiener Processes)* (2024); Tronics AXO305 / GYPRO4300 & ADI ADIS16488 datasheets.

**5.3 Coping: dead → fall back to the surviving single-DOF champion (and its wider confusion matrix, so the belief distrusts it); cheap inspect/replace-sensor actions.** — *built*
CONFIRMED (Step 1): **(b) dead-detected / degraded-undetected** — a dead sensor is obvious (flat/zero) → fall back; a degraded one is the λDU case (looks plausible, silently corrupts the estimate). The 2-sensor cross-check that catches some degraded cases is a later step toward full diagnosis (c).

---

## 6. Monitoring cadence & floods

**6.1 ~30 instrumented passages per month (default).** — *decided (build pending)*
Why: drive-by SHM counts only the *instrumented* train's crossings. A daily in-service passenger train ⇒ ~30/month; 60 is also realistic but heavier to simulate and makes no qualitative difference. We define the problem, so 30 is the chosen default.

**6.2 Routine vs flood regimes differ.** — *decided (build pending)*
Routine month: ~30 passages → (a) fuse for a near-certain monthly state, (b) sensor self-diagnosis. Flood month: few, risky, possibly-corrupted reads. ⇒ value of risk-awareness + sensor-health concentrates at floods.

**6.3 Major vs minor floods — JUSTIFIED.** — *decided*
Major flood = a hydrologically significant event, **observed externally via a river-stage gauge** → triggers the decision branch. Minor flood = small jump, **not signalled**; revealed only through subsequent routine drive-by data. We act only on observed major floods.
Literature basis (defensible for a structural-engineering paper): FHWA **HEC-18** and **AREMA Ch.8** define the **100-year design flood** (1% AEP) and **500-year check flood** (0.2% AEP); "erosion-critical" bridges carry a legally-required **Plan of Action** with **gauge-linked triggers** for inspection/closure — exactly our decision branch. Observation: river **stage** is measured precisely (±0.2%) by radar/pressure/acoustic sensors; **discharge** is inferred via a stage-discharge rating curve. Multi-source fusion (structural drive-by + hazard gauge) updated by a Bayesian Network is the state of the art (Maroni et al.; Kamariotis VoI). An advanced option is Maroni's **adaptive threshold**: solve the inverse-reliability problem for the stage that exceeds a target P_f given the SHM-updated capacity (vs a static 100-yr stage). Refs: FHWA HEC-18 (hif12003); AREMA Ch.8; Maroni et al. (Bayesian DSS, adaptive thresholds, Strathclyde/Heriot-Watt); Lamb et al. (Britain's railway scour economic risk).

**6.3b Flood-observation uncertainty → probabilistic classification.** — *built 2026-06-24* (`digital_twin/flood.py::FloodTrigger`)
Critically, the trigger is uncertain: discharge error is 2–6% in ideal flow but **50–200% when the rating curve extrapolates during the extreme floods that matter** (plus hysteresis/loop-rating). So "is this a major flood?" is itself noisy. **Implemented** as `P(classify major | severity) = Φ((ln s − ln s_major)/β_gauge)` — a lognormal CDF on a dimensionless severity `s` = (flood scour jump)/(mean jump). Construction (the clean story for the talk): the operator declares major when the gauge-inferred discharge crosses the design-flood threshold `s_major`; because the discharge inference is uncertain the gauge reads `gauge = s · ε` with ε lognormal of dispersion `β_gauge` (median-unbiased — the rating curve is the median relation), and marginalising the **hard threshold over the gauge noise gives the smooth CDF**. `β_gauge → 0` recovers the deterministic design-flood threshold; `β_gauge ≈ 0.6` is the default (50–200% extreme-flood rating error). Verified: `classify(observe_gauge(s))` reproduces `p_major(s)` in expectation. Figure: `plotting/flood_decision_map.py` → `flood_trigger_cdf.png`. To be refined with the Todd group; `s_major` and `β_gauge` are swept knobs.

**6.4 Major-flood decision branch.** — *built 2026-06-24* (`digital_twin/planner.py::FloodResponsePlanner`, integrated in `simulation.py::DTSimulator._major_flood_step`)
On an observed major flood the belief is **widened** by the uncertain scour shock (`flood.py::widen_belief`: a severity-scaled upward-diffusion kernel — the median label jump ≈ severity × mean jump, with dispersion folding in the gauge + jump uncertainty), then the owner chooses among **do_nothing / restrict_operations(probe) / inspect / interrupt** by a **risk-adjusted one-step Value-of-Information** rule (the same logic as the POMDP, generalised to several information actions with different cost, observation model, and protection):
`J(a) = action€ + expo·R(b) + E_obs[ commit(posterior) ]`, where `R(b)` is the risk-perceived failure cost carried by the (open) line, `commit(b)=min(R(b), c_repair)`, and the `expo·R(b)` exposure term (the failure risk an OPEN line carries while the survey result is pending, ≈ inspection-latency/step) **cancels among the three open actions but is removed by `interrupt`** — so closure is chosen precisely when `R(widened)` is large (near-threshold belief × severe flood). The probe carries a `reliability` factor (probability it yields usable data); a low value collapses its VoI → the planner escalates to inspect/interrupt (the **sensor-corruption escalation, at the decision level**), and the simulator also escalates a *realised* corrupted probe (`_probe_update` returns 0 usable passages → inspect). Restricted-ops fuses `n_probe` passages (multi-passage flood read). Risk aversion (RiskModel) up-weights the widened tail, lowering the bar for inspect/interrupt. The resulting four-region **decision map** (severity × pre-flood belief) and its shift under risk aversion / degraded sensors is `flood_decision_map.png`. **Finding (decision composition):** in the realistic regime (rare, already-severe major floods on a low pre-flood belief) the response is almost always **inspect**, with **interrupt** in the severe corner and the cheap **probe** occupying only a thin "barely-major + nearly-healthy" band — i.e. once a major flood is *declared*, doing nothing is rarely optimal.
**Finding (€ value, `plotting/flood_value_figure.py` → `results/flood_value/`):** because routine monthly drive-by already catches the post-flood scour at the next passage, the flood branch's effect on the *mean* life-cycle cost is small (a one-month head start + targeted closures). Its value is in the **catastrophic tail**: it trims the CVaR_10 cost by ~2% (POMDP/Hybrid, base fragility) up to **~6% under a conservative fragility** (θ=0.4, where flood-shock scour actually threatens failure). This is precisely the flood-shock tail the risk-perception result (§4.1) showed risk-aversion *cannot* reach — so the flood-ANTICIPATING protocol is the policy that addresses it (a small mean premium for tail insurance). The value grows further when routine monitoring is sparse/unreliable (untested lever). Fragility is swept throughout.

**6.5 Restrict-ops probe = a WIDER observation model; measured later, assumed now.** — *built 2026-06-24 (assumed); MATLAB measurement deferred*
The probe (low mass, locked low speed) is a *different operating condition* than the held-out library, so its confusion matrix should be **measured** from a matching MATLAB batch. Until that batch exists the probe uses an **assumed** wider banded matrix (`probe_sigma ≈ 1.0`, vs drive-by `≈ 0.7`); the harness already loads a **measured** matrix from `probe_dir` when supplied, so the real probe path is scaffolded (`run_comparison(..., probe_dir=...)`). `probe_corruption_prob` injects realised per-passage probe loss for the mock escalation demo. (Live Python re-sim now matches MATLAB, so the probe could alternatively be generated live — see the skew-fix note in §7.)

---

## 7. Open items (to resolve / research)

- **Fragility lognormal swap** — DONE (§3.2). Remaining: pick the reported swept θ/β grid for the paper, and decide IM = % stiffness loss (current) vs relative scour depth Y = scour/foundation depth.
- **Flood-classification model** — *BUILT (§6.3b, §6.4).* Chosen: **probabilistic** `P(major | severity)` lognormal CDF (hard threshold = the β→0 limit). Decision input #3 resolved: the planner consumes the **noisy observed severity** (gauge) — it both classifies (major/minor) and scales the belief widening + the response choice. Remaining: refine `s_major`/`β_gauge` and the probe confusion with the Todd group; optionally Maroni's adaptive (reliability-based) threshold.
- **Sensor diagnosis** — Step 1 uses (b) dead-detected / degraded-undetected (λDU) [CONFIRMED]; (c) 2-sensor cross-check is a later step (§5.3).
- **Active inference planner** — *decided: AFTER the multi-damage setup.* Port `Torzoni2026/` (pymdp) as a 4th planner to compare cost vs the POMDP; first check pymdp runs on Python 3.14 (else a 3.12 env).
- **Skew fix (live Python TTBI)** — *FIXED + VALIDATED.* Root cause: the Python port read the wrong rows for pitch — `d01_data_processing.py` had `pitch = Sol.Veh[0].V[0:3]` but MATLAB `D01` uses `V(4:6)` (V holds vertical velocities in rows 1-3, pitch RATES in rows 4-6). So DOFs 5,6,7 were vertical velocities mislabelled as pitch → ~10× scale mismatch on CarBody_Pitch (vs DOF2 accel which matched at 0.96), breaking the MATLAB-fit scaler → champion read everything as healthy. **Fixed to `V[3:6]`; validated: live Python champion now predicts 0→0, 20→20, 40→40, 55→55 exactly.** Localised via: damage propagates (14–21% change → not a damage bug); profile byte-identical; std ratio PY/MAT 0.96 (DOF2) vs 10.5 (DOF5). **Implication: live Python TTBI == MATLAB — the restrict-ops probe can be generated in Python (no separate MATLAB batch). Only the live/Python path was ever affected; MATLAB training/held-out data and the champion were always correct.**
- **MATLAB↔Python parity audit + Stage-2 geometry (2026-07-09) — three findings, all resolved/documented.**
  (1) **Mesh snapping / effective spans.** The 0.3 m element grid (sleeper 0.6 m / 2) cannot hit the intended support positions for the nominal lengths: B43 re-meshes L to `0.3·round(L/0.3)` and B02 argmin-snaps `linspace` supports to the nearest node. Effective geometry actually simulated: **L=40 → spans 19.8/20.1** (all single-scour data + champion + DT), **L=60 → 20.1/19.8/20.1** (Stage 0/1), nominal L=100 → 99.9 m with spans 24.9/24.9/25.2/24.9 *and a floating-point knife-edge tie* at the middle support (49.95 exactly between two nodes). Harmless when consistent (Stage 0 passed with unequal spans; Python replicates the identical cumsum/linspace/argmin op sequence so both sides snap alike), but papers should quote the effective spans. **Decision: Stage 2 uses L=99.6 = 4 × 24.9 m spans = 83 elements each — all 5 supports EXACTLY on nodes, no tie** (verified numerically; L must be a multiple of 4×1.2 m for 4 equal on-node spans).
  (2) **Crop window: the 'fixed' profile mode silently reverts the generalised crop to the 40 m window.** B19 Type-2 replaces the whole `Calc.Profile` struct with the 2023 saved struct, which *contains* `L_bridge=39.9` — so D01's generalised crop (`round(L_bridge)·100`) reads 39.9 on ANY bridge. **Verified on Stage-0 data: signals are 5831 samples (the 40 m window) on the 60 m bridge** — first-wheel travel 10→68.31 m vs bridge at 30→90 m, i.e. the instrumented vehicle NEVER REACHES pier 3 (69.9 m) inside the window, yet pier-3 scour was still quantified at MSE 0.79 (information travels through deck continuity — arguably *strengthens* the Stage-0 claim; state this in the paper). Python's b19 Type-2 hardcode mirrors the same behaviour, so MATLAB==Python (no parity break). Stage 1 (also 'fixed') will share the 5831 window → Stage-0↔Stage-1 comparison stays apples-to-apples; do NOT regenerate. **Stage 2 ('psd_fra' = Type 1, no struct replacement) automatically gets the true full-bridge window (~11831 samples).** TODO if a multi-damage live DT is built: generalise the Python Type-2 branch (and port `psd_fra` A04 block to Python a04) so the live path matches whichever window the stage's training data used.
  (3) **Noise skew fixed: MATLAB D01 adds measurement noise to the WHEEL channels only; Python d01 was adding it to all three groups** (body/bogie accel + pitch too) → live-DT observations were noisier than training on the champion's channels. `TTBI_2D/d01_data_processing.py` fixed to wheel-only (training data is the reference; sensor faults live in `digital_twin/sensor_health.py`). Validated by re-running the live champion sweep post-fix.
- **Stage 3 "all-damage" EOV arm — DECIDED 2026-07-09 (user; spec in `docs/stage3_alldamage_spec.md`).** Kitchen-sink robustness stage for the ABLATION paper only (DT paper keeps the lean set): L60/3-span, scour+bearing heads unchanged, nuisances = crack + psd_fra + ballast patches + hanging sleepers + rail pads + wheel OOR (all per-passage, logged, data-anchored via the verified NotebookLM deep-research spec `docs/track_eov_sampling_spec.md`). Rationale: the deep research showed ballast/void settlement mimics scour IN-BAND (0.5–15 Hz, profile-mediated — NOT covered by stationary psd_fra), so track EOVs add real coverage; wheel-flat fundamental (~7.7 Hz @80 km/h) is the one vehicle-side in-band threat; pads = designed NEGATIVE CONTROL (content >30 Hz sprung cutoff and >PAA-512 Nyquist ~85–110 Hz → predicted null). PAA band analysis says PAA-512 keeps every band that matters; pre-registered contingency if scour MSE inflates = n_segments 1024/2048 re-run before RAW. Scope boundary (paper limitation sentences): pier rotation/tilt NOT representable (piers are boundary springs, not members — a deck-node rot-spring would be deck fixity, not tilt; needs frame/3D), lateral/torsional/SSI/prescribed settlement out; suspension drift already covered by vehicle variability; completeness claim is PATHWAY-BASED (cover confounders sharing scour's vertical low-frequency support/track-stiffness pathway), not exhaustive.
- **Other deterioration mechanisms (track/vehicle layers)** — *superseded 2026-07-09 by the Stage-3 decision above for the ablation paper; still out of scope for the DT paper.* Modelling time-evolution/degradation of the rail irregularity profile AND of the other bridge/track layers (ballast, sub-ballast, sleepers, rail pads, wheels, etc.) would be too far from this study's scope. Scope is kept on **scour**: finish the single-scour DT → then multiple-scour and possibly bearing and/or crack. A literature search on per-layer degradation models is needed if ever pursued.
- **Multi-damage — ABLATION first, then DT.** *Sequence (decided):* single-scour DT → **multi-damage ablation** → later DT extensions. **Stage 0 STARTED 2026-06-24** (engine built; data generates on the PC): formulation decided = **multi-output REGRESSION** (one continuous head per target support; `core/task.py` `task='regression'` switch, classification stays the default so the single-scour ablation is byte-identical). Independent LHS scour sampling for TRAINING (broad joint coverage); the copula correlation is a DEPLOYMENT assumption only. Torzoni's classify-then-regress = a later comparison arm; add a per-pier "damaged?" detection head only if regression false-positives on healthy piers. **Staged:** Stage 0 (2 piers, no bearing → can drive-by *localise*?) → Stage 1 (+bearing → scour-vs-bearing disentanglement) → Stage 2 (4-span scale-up). Reduced grid: 1-sensor sweep → best pair (~9 configs), single seed, no 30-seed MC. New metrics: per-pier MSE, aggregate MSE, **localisation accuracy**. Built: `scour_MATLAB/A00_Run.m` multi_scour mode (per-pier `scour_vector` label), `core/dataset.load_ttbi_dataset(target_supports=...)`, the `core/task.py` abstraction threaded through models/trainer/robustness/pipeline, and `comprehensive_ablation_multidamage.py`. See [[multi-damage-problem-setup]], [[dt-flood-branch]].
- **Sensor-health numbers** — replace `SensorHealthParams` placeholders with the values in `papers/Confiabilidade MEMS Ferroviário.docx` (EN 61373 ratio 1.01:5.40:38.0; bias-life 1588→558 d; VRE 0.5 °/h/g²; Arrhenius Ea; infant mortality 0.3–5%; 200–500 FIT).
- **Confounder damages = training-set EOVs.** — *decided 2026-07-06 (build pending)*
  Crack + rail-profile degradation enter the multi-damage study as **randomized nuisances in the TRAINING set** (domain-randomization / EOV-augmentation), NOT as regression heads and NOT as evolving DT states — nuisances need no evolution law; only decision-relevant states do. **Bearing is the exception** (confounds scour via the same support-stiffness pathway) → keeps its own regression head (Stage 1). Methodological rule (open-set SHM literature): a damage type present only at TEST time corrupts a closed-set model — confounders must be in the training distribution for a robustness claim. **Staging: Stage 0 stays clean** (pure multi-scour localisation gate); nuisance EOVs enter at Stage 1. Precedent: Fernandes et al. 2025 (crack+bearing+scour in one training set); a 2026 S&IE drive-by study trains across distinct track-irregularity conditions; "domain randomization" is the sim-to-real term of art.
  - **Crack** = Sinha-style local EI reduction — **BUILT in BOTH pipelines 2026-07-06** (Python `TTBI_2D/damage_config.py::apply_cracks`; MATLAB `B00_Calculations.m::local_apply_cracks`, exact mirror: elements within [loc±lc] get I×(1−intensity), lc≤0 = single element; Fernandes-compatible ratios 0.05/0.1 ≈ 14/22% EI loss). Smoke-tested end-to-end (`scour_MATLAB/smoke_damage_toggles.m`): exactly 1 element at 0.78·I, full dynamic solve OK.
  - **Rail-profile degradation** — **BUILT in MATLAB 2026-07-06**: `A00_Run.m` `profile_mode` = `'fixed'` (legacy, byte-identical) | `'fixed_scaled'` (measured profile × per-passage amplitude factor; multiplier mirrored in `B19`/Python `b19`) | `'psd_fra'` (profile REGENERATED per passage from the FRA PSD with new random phases — no profile memorisation; severity = FRA class 1–6 drawn per passage, Berawi A_v ladder in `A04_Options`). Rationale: amplitude-scaling one fixed profile is only a first-order knob (deep-research verdict: degradation changes PSD shape; single profile → spatial overfitting); PSD-class regeneration is the defensible representation, anchored to **EN 13848** D1 SD classes / FRA classes; evolution (if ever needed): SD grows ~linear/exponential with tonnage (Sato; Shenton). Per-passage draws are logged in each file (`crack_log`, `profile_log`) for traceability — they are NOT labels. NOTE: adding profile EOV changes the training distribution → champion must be re-confirmed in the reduced ablation (planned anyway). Python live path (probe/DT) keeps the fixed profile; add `psd_fra` there only if DT observations should carry profile EOV.
  - **Bearing 'target' mode** — **BUILT 2026-07-06** (`A00_Run.m` `bearing_mode` = off | fixed(legacy) | target): Stage 1 bearing state [left,right] joins the LABELS — ONE joint LHS over scour+bearing dims (broad joint coverage) + single-bearing anchors; saved per file as `data.bearing_vector` and in `damage_states.mat::BearingStates`.
  - **Bearing treatment — SOURCE-VERIFIED 2026-07-10 (for Paper 1 §methodology).** Mechanism = rotational spring at the abutment deck nodes, healthy k_r=0 (free rotation) — identical to Fernandes 2025 and Khan 2022. Khan 2022 (verified from the PDF) treats k_r as CONTINUOUS, sweeping 0 → 10^12 Nm/rad ("supports start behaving almost like fully fixed — 93% — at 10^12"), validated FULL-SCALE on an Irish railway bridge (damaged vs rehabilitated states); Fernandes 2025 binarises it to {0, 10^9} with 10^9 = the minimum drive-by-detectable seized bearing (Feng 2023 / O'Brien line). OUR design: keep Fernandes's 10^9 anchor as bearing_max but restore Khan's continuity — LHS over [0, 10^9], regressed as seized-% (k/10^9 × 100), bearing = HEAD not nuisance (decision-relevant + shares the support-stiffness pathway with scour → the Stage-1 disentanglement question). RANGE VERIFIED ON OUR OWN MODEL (2026-07-10, `results/bearing_sensitivity/bearing_sensitivity_sweep.png`; 8-point live-TTBI sweep k_r = 10^8→10^12 on the entry abutment, L60/3-span): the response change tracks the analytic fixity ratio k/(k+4EI/L_span) almost exactly; at bearing_max=10^9 (fixity 0.31) the relative RMS signal change is already 10–16% on the sprung channels (CarBody_P 15.9% — the MOST bearing-sensitive channel — CarBody_V 14.1%, FrontBog_V 11.8%, RearBog_V 10.5%), i.e. comfortably learnable in-simulation (Khan's "1e9 = minimum detectable" was a REAL-WORLD claim; our clean channels see 10^8 at ~2%). Saturation at ~10^11 (fixity 0.98; 10^11→10^12 adds <2% more). So the HONEST limitation of [0, 10^9] is RANGE COVERAGE, not a noise floor: it spans fixity 0–31% ≈ the lower third of the available dynamic range (~35% of the saturation response), and the linear seized-% label maps near-linearly to response over it (good for regression). DECISION: KEEP bearing_max=10^9 for the whole staged campaign (comparability + Fernandes anchor + learnable range, pending confirmation from the Stage-1 bearing parity); extension to ~10^11 with LOG-spaced sampling (fixity-ratio label) = quantified future work — a linear label over [0,10^12] would concentrate all response in the first ~1% of label range. Side-note: bearing sensitivity ranks CarBody_Pitch ≫ carbody/bogie verticals ≫ wheels/bogie-pitch — consistent with the Stage-1 auto-pair flipping to FrontBogie_Vert+CarBody_Pitch once bearing heads entered the task.
  - **Bearing-head LOADER — BUILT + smoke-tested 2026-07-08** (Python side of Stage 1). `core/dataset.py` reads `bearing_vector` when `config['bearing_targets']` (e.g. `['left','right']`) is set, laid out AFTER the scour heads: label = [scour_1..S, bearing_1..B]. **Bearing is normalised to a "seized %" 0-100** (bearing / bearing_max x100, bearing_max read from `case_info.mat::bearing_max_Nm_rad` or `damage_states.mat`, else observed max) so the MSE loss balances heads instead of being swamped by the 1e9-scale stiffness. Threaded: `core/task.py` (`n_outputs`=scour+bearing; `n_scour_outputs`/`n_bearing_outputs`; `evaluate` scores localisation over SCOUR heads only and adds `scour_mse`/`bearing_mse`), `_cache_stem` (`_b<targets>` -> distinct cache), pipeline DT metadata (`bearing_targets`), and `comprehensive_ablation_multidamage.py` (`BEARING_TARGETS` toggle; None = Stage 0 byte-identical). Stage 1 is now turnkey: set `BEARING_TARGETS=['left','right']` + point DATASET at a `stage1_bearing` folder. Linear stiffness normalisation is a documented choice (matches the linear LHS sampling; log-scale is a possible refinement). NOTE: bearing head-vs-nuisance is still the open methodological question Stage 1 answers empirically (`scour_mse` with vs without the bearing head).
  - **STAGE presets + turnkey Stage-0 ablation — BUILT 2026-07-08.** `A00_Run.m` now has a `STAGE` selector (one line = one paper case): `stage0_multiscour` | `stage1_bearing` | `stage1_eov` | `stage1_full` | `stage2_4span`; each sets geometry + which damages are targets vs EOV nuisances. Verified end-to-end at reduced counts (stage1_full: `bearing_vector`, `crack_log`, `profile_log`, `BearingStates` all saved correctly). `comprehensive_ablation_multidamage.py` rewritten TURNKEY: Phase-1 single-DOF sweep → AUTO-selects best 2 sensors (lowest PAA+N-HiTS agg MSE from the Optuna DBs) → Phase-2 pair → results summary (`results/MD0_summary/MD0_leaderboard.csv` with per-support MSE + **localisation accuracy** + `MD0_parity_best.png`) — the summary fills the gap that per-head/localisation metrics were computed but never persisted with run_robustness=False. Smoke-tested on the real Stage-0 data (loader→PAA→both archs→train→metrics) in .venv (torch yes, optuna no → full run is PC-only). Bundle `multidamage_ablation_bundle.zip` (script + core/training/plotting + scour_MATLAB, NO data) for copy-extract-run on the data PC.
  - **Stage-1 ablation code READY 2026-07-09.** `comprehensive_ablation_multidamage.py` now has a `STAGE` selector (stage0_multiscour | stage1_bearing | stage2_4span) mirroring A00 — sets DATASET + TARGET_SUPPORTS + BEARING_TARGETS in one line; outputs go to `results/<STAGE>_summary/`. Added `disentanglement_report()` (the approved Stage-1 metric): from the single-damage anchor subsets it computes **`scour_leak_on_bearing_%`** (false scour a seized bearing induces where scour is truly absent — the dangerous case, ideal ~0) and `bearing_leak_on_scour_%`, written to `disentanglement.csv`; leaderboard adds `scour_mse`/`bearing_mse`; parity plot labels scour(%) vs bearing(seized %). Leakage math unit-tested; 4-head loader/model/evaluate synthetic-smoke-tested (py3.13). Delivered as `multidamage_stage1_bundle.zip` (ablation STAGE=stage1_bearing + MATLAB STAGE=stage1_bearing + code, no data). Predicted Stage-1 folder: `L60_3span_multi_scour_scourS2-3_bearTGT_dano0-60pct_states267_Npass50_varNVST` (17 anchors + 250 LHS; CONFIRM states count).
  - **Batch-crash fix (2026-06 lab-PC MATLAB crashes, probable cause):** `A04_Options` had live figure flags ON and `C03_TTB_2D_Plots` runs after EVERY passage and never closes figures → thousands of accumulated figures in a dataset run → OOM. All plot flags now commented with a batch-safety note; per-state `rng(damage_seed·1e5+DC)` also added in A00 so all EOV draws are reproducible.
  - Refs: EN 13848-5; FRA track classes; ORE/ERRI B176 + German spectra; Sato / Shenton track-degradation models; Sinha et al. (2002); Fernandes et al. (2025); open-set SHM (e.g. "Structural damage classification under varying environmental conditions and unknown classes via open set domain adaptation", MSSP 2024); Gemini deep-research report 2026-07-06 (to be saved in `papers/`).
- **Stage-1 (bearing head) RESULTS — ANALYSED 2026-07-12** (Lab-PC upload `results/Stage1/`; ran the 2-arm bundle PAA_NHiTS + PAA_S2V_NHiTS; Optuna healthy: every study 75/75 trials). **Bearing-as-HEAD CONFIRMED**: auto-picked pair (FLIPPED to FrontBogie_Vert+CarBody_Pitch) bearing MSE 7.0 (left 4.8 / right 9.2 → RMSE ≈ 2.2 / 3.0 seized-%), tight parity on all 4 heads — real skill, validating the sensitivity-sweep prediction (10–16% rel-RMS at 1e9) and the continuous-[0,1e9] design (range caveat unchanged). Scour cost of adding the bearing dimension: champion-pair scour MSE 0.757 → 1.798 (RMSE 0.87 → 1.34 pp; per-pier 1.78/1.81 balanced; localisation 0.9898 → 0.9883) — disentanglement largely succeeds; residual leak quantified: false-scour-from-bearing 2.37 pp, false-bearing-from-scour 3.43 seized-% (S2V pair: 1.85/2.85). The pair flip is bearing-driven and coherent: (a) the k_r sweep ranks CarBody_Pitch most bearing-sensitive; (b) FrontBogie_Vert is the leading sensor over the ENTRY abutment; (c) right-bearing MSE > left-bearing MSE on EVERY channel — the 'fixed'-profile 40 m crop (§ parity-audit finding 2) ends before the exit abutment, so right-bearing information arrives only through deck continuity (same mechanism as Stage-0 pier 3; quote in paper). S2V robustness note: the champion wins 7/8 singles, but S2V wins the PAIR by ~10% (agg 3.95 vs 4.40; scour 1.62 vs 1.80) — opposite sign to Stage 0 where S2V hurt the pair (+69%). Policy unchanged (architecture fixed at Stage 0); report as a robustness observation, not a selection change. Stage 1 is FINAL — never regenerate; feeds Paper-1 Results §5.
- **Stage-2 (4-span scale-up) RESULTS — FORENSICS 2026-07-12: data used the PRE-FIX L=100 geometry, and all sprung channels collapsed. Regeneration at L=99.6 recommended; EOV design review FIRST.**
  (1) *Provenance:* the Lab PC generated with the v3 bundle (before the parity-audit L=99.6 fix); the shipped ablation driver in `results/Stage2/` was hand-edited to `DATASET="L100_4span_..."`. So the simulated bridge is the audit-rejected effective 99.9 m with spans 24.9/24.9/25.2/24.9 and the FP knife-edge mid-support tie. The geometry is CONSTANT across states/passages (deterministic snap) ⇒ it cannot cause the collapse and the data is internally consistent — but it contradicts the documented design and the committed L=99.6 decision, so Stage 2 must be regenerated at L=99.6 regardless of the EOV question (get-it-right rule).
  (2) *The collapse* (`results/Stage2/results/stage2_4span_summary/leaderboard.csv`): all six sprung channels sit at scour MSE ≈ 327–340 ≈ label variance (U(0,60) → 300) and bearing MSE ≈ 923–937 ≈ variance (U(0,100) → 833), localisation ≈ chance — the predict-the-mean fixed point; each study's Optuna best plateaus at that exact value across 19–27 COMPLETE trials (75 total each) ⇒ hyperparameter-robust unlearnability, not undertraining. Only the wheel (unsprung) channels retain skill: singles 222–240; auto-pair Wheel1+Wheel2 scour MSE 126 (RMSE ≈ 11 pp), bearing 431, loc 0.744, with a diffuse-but-correlated parity cloud. Forensics ELIMINATED: crop bug (D01 generalised window verified; study metadata shows correct 5 heads / supports [2,3,4] / PAA-512) and code drift (shipped `core/`, `training/`, `plotting/` byte-identical to repo; only the driver's STAGE/DATASET lines differ). Note the wheels are the ONLY channels carrying added measurement noise (D01 wheel-only) — and still won.
  (3) *Attribution is CONFOUNDED by design:* Stage 1 → Stage 2 changed BOTH the geometry (3-span/60 m → 4-span/~100 m, 5-D label LHS with only 250 points) AND the EOV nuisances (crack_p = 1.0 — a fresh random crack EVERY passage, EI-loss U(0.05,0.30) anywhere in [0.1,0.9]·L — plus `psd_fra` profile re-drawn per passage with class from {4,5,6}). The physics hypothesis for the inversion (wheels beat sprung, opposite of Stage 0): per-passage roughness dominates the suspension-band (0.5–15 Hz) response of body/bogie channels, masking the quasi-static scour deflection signature, while the unsprung wheel rides the profile + local deflection directly. Potentially a PUBLISHABLE finding — IF the EOV design survives literature scrutiny.
  (4) *Plan:* (a) NotebookLM deep research on profile/crack EOV realism — prompt at `docs/deep_research_prompt_profile_crack_eov.md` (key questions: per-passage vs per-state profile redraw; FRA class set for 70–90 km/h; crack as persistent per-state nuisance vs per-passage transient; axle-box-vs-carbody precedent; scour drive-by with roughness). (b) Then run the `stage1_full` isolation preset (already in A00: L60/3-span + bearing target + crack + psd_fra) to attribute EOV-vs-scale-up on known-good geometry — it also gates Stage 3 (same EOV superset). (c) Then regenerate Stage 2 at L=99.6 with the settled EOV design. (d) `smoke_stage3.m` before any Stage-3 generation. Optional zero-cost pilot on the idle Lab PC meanwhile: a designed MIXED pair (best wheel + best sprung, e.g. Wheel1_Vert+FrontBogie_Vert) on the existing L100 data to test the profile-reference + deflection-sensor fusion hypothesis (question 7 of the research prompt); its qualitative answer survives regeneration.
- **EOV DESIGN REVIEW — RESOLVED + IMPLEMENTED 2026-07-12** (deep-research report received: `papers/Drive-By Scour ML Literature Design.{md,docx}`; NotebookLM/Gemini — treat per-number claims with the usual quote-check discipline, flags below). REPORT VERDICTS, all adopted: (1) **Profile: per-STATE draw, NOT per-passage** — published drive-by ML uses fixed/per-scenario profiles (Locke 2020 one fixed realization, robustness holds "provided the roughness profile remains constant"; NuBe-DBBM fixed P00/PA1/PA2 profiles — Sarwar & Cantero; Fernandes fixed FRA class 4 per scenario); physically, track geometry evolves over MGT (Sato/Shenton) and EN 13848-2 D1 repeatability is ≤0.5 mm @95% pass-to-pass — a fresh class+phase redraw per train "simulates an impossible geotechnical environment". (2) **Class FIXED at FRA 4** (roughest permissible at 70–90 km/h ≈ Class-4 80 mph passenger limit; classes 5/6 = premium track, unrealistically smooth for a scour-prone regional line) + **0.5 mm additive per-passage jitter** (EN 13848-2 repeatability). (3) **Crack: per-STATE, prevalence ~0.25** (persistent condition; p=1.0 destroys the healthy-deck baseline — "ontological failure"; location U(0.1,0.9)·L, EI-loss U(0.05,0.30) Sinha-type retained). (4) **Track-layer damage (Stage 3): per-STATE too** (same persistence argument); **wheel OOR stays per-passage** (a different fleet train each passage — consistent with vehicle-property randomization). (5) The sprung-collapse physics is VINDICATED (suspension filtering + carbody 1–3 Hz resonances vs quasi-static deflection; ABA literature: unsprung = direct kinematic trace of profile+deflection) and **continuous scour REGRESSION from sprung channels under per-pass roughness is "virtually undocumented"** — the L100 result is publishable as a negative finding with the design caveat. (6) **Fusion hypothesis endorsed**: wheel (profile reference) + sprung (inertial response) ≫ wheel+wheel (TSD/two-axle residual logic, OBrien/Keenahan). VERIFY-BEFORE-PAPER flags: crack prevalence 20–30% (weakest sourcing in the report), EN 13848-2 0.5 mm number, the Locke/NuBe-DBBM methodology quotes, Fernandes "fixed class 4 + 100 passages" (user knows the thesis — quick check). IMPLEMENTED (MATLAB): A00 `crack_draw/profile_draw/track_draw='per_state'` (draw at j_pass==1, held across the state's passages; logs keep per-passage shape), `crack_p=0.25`, `profile_fra_classes=4`, `profile_jitter_sd_mm=0.5`; B19 seeds the PSD phase draw per state (global stream saved/restored) and forces the speed-independent `min_dx` grid under phase-lock (default dx tracks v·dt — same seed would NOT give the same track at different speeds otherwise) + adds the per-passage jitter; A04 passes phase_seed/jitter through; new folder tags `crackST`/`prof-psd_fraST`/`trackEOVST` keep legacy folders distinguishable; new A00+driver preset `stage1_crack` (bridge-damage stage: scour+bearing+crack, profile fixed — Fernandes-comparable, and disambiguates crack-vs-profile if stage1_full degrades). PYTHON live-path mirror TODO unchanged (b19 psd_fra not ported; live DT keeps fixed profile).
- **ABLATION GRID UPGRADE 2026-07-12 (user decision):** `N_TRIALS` 75→100 (TPE `n_startup_trials` auto-scales to n_trials//4 = 25 — the "larger initial sampling plan"), `SEEDS=[42,1337,2026]` (3 independent Optuna runs per config; extendable to 5). Leaderboard: one row per seed + `leaderboard_median.csv` (median + IQR = the paper table, consistent with the median-not-mean rule); auto-pair ranks by MEDIAN champion MSE across seeds; train/val split stays fixed (random_state=42, trainer.py) so seed spread isolates init/HPO variance. NOTE: more trials cannot rescue an unlearnable task (Stage-2 sprung studies plateaued at label variance across 75 trials, hyperparameter-robust) — the upgrade buys RANKING confidence, not skill. `EXTRA_PAIRS` config added (designed pairs beyond auto top-2). **Mixed-pair pilot shipped** (`pilot_stage2_L100_mixed_pairs.py`, in bundle v7): Wheel1+FrontBogie_Vert and Wheel1+CarBody_Vert on the existing L100 data, 75 trials/seed42 for budget-matched comparability, summary to `results/stage2_4span_L100pilot_summary/`. **Measurement-noise question (user asked, decision documented):** current model = legacy wheel-channels-only, MULTIPLICATIVE (D01: std = 0.05·|signal|; sprung channels noise-free). Physically all channels carry sensor noise, and the multiplicative form keeps SNR constant (unrealistic; an additive floor punishes small signals). DECISION: keep the noise model FIXED through the ablation chain (comparability; and it biases AGAINST the wheels-win finding — wheels won while being the only noisy channels — so the finding is conservative, state this in the paper), and test all-channel noise as a LOADER-LEVEL augmentation robustness arm later (additive, SNR anchored to the rail-qualified IMU datasheets already in the reference shortlist — no regeneration needed since D01 noise is baked at processing time but extra noise can be injected at load time). Campaign bundle **v7** rebuilt (89 files: revised A00/A04/B19, new driver, pilot, README_STAGE2.md v7 with the new run order: pilot ∥ smoke_stage3 → stage1_crack → stage1_full → Stage-2 regen L=99.6 → Stage 3).
- **NOISE POLICY REVISED 2026-07-12 (user decision — supersedes the "keep legacy noise through the chain" line in the previous entry): generation is NOISE-FREE; measurement noise moves to LOAD time.** Why: (1) the noise model stays configurable per experiment AND per channel without ever regenerating data — sensor grade genuinely differs by mounting position (EN 61373 vibration severity carbody ≈1 : bogie ≈5.4 : axle ≈38; `papers/Confiabilidade (Sensores) MEMS Ferroviário(s).docx`), and the legacy D01 model (multiplicative 5%, wheel-only) was both position-blind and SNR-constant (unrealistic); (2) bulletproof-methodology rule — noise belongs to the OBSERVATION model, not the physics data. IMPLEMENTED: A00 `use_signal_noise=false` (D01 then adds nothing; new folders tagged `varVST` — no `N`); `core/dataset.py` gains `sensor_noise` config + `_inject_sensor_noise()` (applied to RAW signals pre-PAA, exactly where D01 acted; deterministic seed 42; cache stem noise-tagged so clean/noisy caches never collide; first mode `legacy_wheel` reproduces the old D01 distribution on noise-free data; per-channel additive floor modes = future robustness arm anchored to the rail-qualified IMU datasheets); driver knob `SENSOR_NOISE=None` (noise-free chain default). Chain-comparability note (for the paper): legacy noise was WHEEL-ONLY, so the sprung champion channels were always noise-free — sprung-pair comparisons across old (varNVST) and new (varVST) stages are unaffected; only wheel-channel leaderboard rows change meaning (new stages: noise-free wheels). Python live path needs no code change (d01 noise fires only when `Damage.desvio>0`; set 0 for models trained noise-free); `smoke_stage3.m` already runs desvio=0. FACT for the record: existing Stage-0/1 datasets have the legacy wheel noise BAKED IN (applied at D01 processing; not removable) — they stay valid as published, and `legacy_wheel` injection makes new data distribution-comparable if ever needed.
- **RE-RUN POLICY 2026-07-12 (user): never EXTEND existing Optuna studies — re-runs start FROM SCRATCH.** Driver `RUN_TAG` implemented: when set (e.g. "v8"), every study/output name gets the suffix → fresh full-budget studies; old DB rows/weights preserved (provenance). New datasets need no tag (dataset-keyed DB paths ⇒ no collision). Exception by design: the L100 mixed-pair PILOT deliberately RESUMES the old 75-trial/seed-42 context (budget-matched comparability with the existing wheel-pair studies) — do not tag it. Bundle **v8** rebuilt with the noise policy + RUN_TAG + varVST folder names. FOOTGUN FIX 2026-07-13: the noise config is now part of the study NAME too (`_cfg_suffix()`: `_nz-<mode>` + `_<RUN_TAG>`) — without it, re-running a stage with a different `SENSOR_NOISE` would silently resume studies whose earlier trials trained on differently-noised data.
- **L100 MIXED-PAIR PILOT — RESULTS 2026-07-13 (`results/Stage2/results/stage2_4span_L100pilot_summary/`; 75 trials/seed42, budget-matched to the deprecated L100 leaderboard). FUSION CONFIRMED, with a suspension-chain gradient.** FrontBogie_Vert+Wheel1_Vert: scour MSE **105.1** vs Wheel1+Wheel2 126.1 (**−17%**; RMSE 10.3 vs 11.2 pp), localisation **0.797** vs 0.744, pier-2 MSE 74.3 vs 124.3 (−40%), false-scour-from-bearing leak 19.2 vs 22.7 pp (bearing head slightly worse, 448 vs 431, driven by bearing_right). The remarkable part: FrontBogie_Vert ALONE is collapse-level on this data (scour 327, loc 0.42) yet adds 17% as a fusion partner — the TSD/two-axle residual mechanism (wheel supplies the roughness reference; the bogie supplies the suspension-filtered inertial response), exactly what the deep-research report predicted (OBrien/Keenahan logic). Gradient: CarBody_Vert+Wheel1 (155.5) is WORSE than wheel+wheel — one more suspension stage between partner and track destroys the fusion value; the partner must sit LOW in the chain. Methodological consequence: the greedy auto-pair (top-2 singles) is demonstrably suboptimal under roughness EOV (best pair = #1 + #6 single) → `EXTRA_PAIRS=[[1,3]]` (FBvert+W1) is now the DEFAULT in every stage for chain-wide comparability. Caveats: deprecated L100 geometry + per-passage EOV data, single seed — treat as a pilot direction, to be re-established on the regenerated chain.
- **EN 61373 CORRECTION 2026-07-13 (user caught an over-claim).** The carbody:bogie:axle ≈ 1:5.4:38 severities are random-vibration TEST levels for equipment QUALIFICATION by mounting position — they describe the vibration ENVIRONMENT a device must survive, NOT measurement noise added to an acquired signal. Correct uses: sensor RANGE selection per position, reliability/aging scaling (what the MEMS report uses them for → `digital_twin/sensor_health.py` territory), and second-order vibration-rectification bias (VRE ∝ g²; the docx cites gyro VRE 0.5°/h/g²). The load-time noise model must instead use per-channel ADDITIVE floors from sensor DATASHEETS (noise density [µg/√Hz] × √bandwidth; rail-qualified IMUs in the shortlist: ADIS16488, Tronics AXO305/GYPRO4300) — the indirect position dependence enters only because higher-vibration positions need larger full-scale ranges, which typically carry higher noise densities (a datasheet fact, not an EN 61373 number). Comments corrected in core/dataset.py, A00, README.
- **🔴 CRITICAL — DECK MASS WAS 1000× TOO LIGHT. FIXED 2026-07-15 (rho 9.6 → 9600 kg/m). Every dataset generated before this is INVALID.** The user supplied Fernandes's published parameters for this exact bridge — I = 0.33 m⁴, 2 × 20 m spans, 0.3 m elements, E = 3.5e10 N/m², **rho = 9600 kg/m**, 3% damping — which finally resolved the deck-mass flag raised 2026-07-13. `A03_Bridge.m` carried `Beam.Prop.rho = 9.6`; since `B43` sets `Prop.A = 1 m²` and `B01/B03` build the mass matrix as `rho_n · A_n`, that value IS the deck mass per unit length ⇒ **1000× too light** (9.6 kg/m ≈ a 0.004 m² rod, not a deck). VERIFIED with the model's own numbers (`plotting/beam_freq.py`: Euler–Bernoulli FE, consistent mass, the real `k_v0 = 344e6 N/m` support springs): **rho=9.6 → f₁ = 130.6 Hz** (L40/2-span) vs **rho=9600 → f₁ = 4.13 Hz** (textbook for a 20 m railway span); ratio = √1000 = 31.6× exactly. Also L60/3-span 132.2 → 4.18 Hz; L99.6/4-span 86.9 → 2.75 Hz. **Why this is severe, not cosmetic:** a ~131 Hz deck is effectively RIGID to a vehicle whose car body sits at 1–3 Hz and bogies at ~10–30 Hz, so the vehicle–bridge *interaction* — the entire physical basis of drive-by monitoring — carried almost no deck dynamic signature. The models could still read scour (support-stiffness loss still changes the quasi-static deflection), which is precisely why this hid for so long: **the ML worked, so nothing looked broken.** CONSEQUENCES: (1) all previously published numbers (Stage-0 0.757 / loc 0.99, Stage-1 bearing, the L100 pilot) are **invalid**, not merely superseded — they describe an unphysical bridge; the paper drafts must stop quoting them even as "direction". (2) **The roughness finding may change**: with a correct ~4 Hz deck sitting right in the car-body/bogie band, the sprung channels have a real deck signature to find, which they never had before — `s14_prof` may now answer differently. (3) Timing was pure luck: the whole campaign was already being regenerated. LESSON for the paper's honesty section: a physically wrong model that still trains well is the most dangerous kind of bug — this is the argument for the frequency sanity-check now on the verify list.
- **BEARING RANGE EXTENDED 2026-07-15 (user asked: are we considering higher values than Fernandes's? — answer: no, and we should).** Old design: `k_r ~ U(0, 1e9)`, label = seized-% = k_r/1e9. **The flaw:** Fernandes's 1e9 is the *minimum drive-by-DETECTABLE* seized bearing (a detectability FLOOR from the Feng/O'Brien line), so using it as our MAXIMUM put the entire label range between "healthy" and "barely detectable" — backwards. Khan (2022) sweeps k_r continuously to 1e12 with near-full fixity, so real seized bearings live far above 1e9. Response is governed by the analytic **fixity ratio** φ = k/(k + 4EI/L_end) (confirmed by our own sweep, `results/bearing_sensitivity/`); for our deck 4EI/L_end = 2.31e9 (L60) / 1.86e9 (L99.6), so k_r = 1e9 is only **φ ≈ 0.30 / 0.35** — the lower third of the available response. But a *linear* draw over a large k_r range is equally wrong (it piles all the response into the first ~1% of the label). **FIX:** draw **φ ~ U(0, 0.95)** and invert k_r = φ/(1−φ)·4EI/L_end (φ=0.95 → k_r = 4.39e10 at L60). φ is bounded [0,1], **near-linear in response** (a good regression target), and **geometry-normalised** — so the bearing label means the same thing on the L60 and L99.6 bridges, which raw k_r never did (a hidden cross-rung comparability bug in the old design). Fernandes's 1e9 survives as a **reportable landmark** (φ≈0.30), not a cap; the documented RANGE limitation is now **removed** from the paper. IMPLEMENTED: A00 samples fixity in the joint LHS + anchors, stores BOTH `bearing_vector` (k_r, the physics) and `bearing_fixity` (the label); manifest records `bearing_fixity_max`, `bearing_k_ref_Nm_rad`, `bearing_label='fixity_ratio'`; `core/dataset.py` prefers `bearing_fixity` (→ fixity %) and falls back to legacy `k/bearing_max` for old files.
- **TRACK COUNTS ANCHORED + CRACK LOCATION CORRECTED 2026-07-15 (Gemini deep research — too large for NotebookLM; `papers/Track Defect Prevalence Data Search.{md,docx}`).** The last un-cited numbers in the damage model are now anchored; full before/after table + caveats at the top of `docs/track_eov_sampling_spec.md`. **The prevalence paradox is RESOLVED**: the cited "~50% of concrete sleepers show some voiding" and our ~9% mechanical model are both right because most voids are SUB-THRESHOLD (<1.0 mm = accommodation; 1.0–2.5 mm = dynamic-impact threshold, where a 1–2 mm void raises adjacent contact force up to 70% / wheel loads ~85%; only 10–20% of visibly-settled sleepers exceed 1.5–2.0 mm ⇒ **impactfully unsupported = 5–10%**). CHANGES (all MC-verified, `scratchpad/check_track_stats.py`): hanging groups DU{0..3}→**Poisson λ=3.0/100 m**; ballast patches DU{0..2}→**Poisson λ=1.2/100 m** (GPR: FI>30 on 10–20% of route ⇒ 15% ÷ 12.5 m mean patch); **counts now scale with the modelled window** (120/159.6 m — the old fixed-count draw was itself an error); pads **0.005→0.02** (the cited 0.5% is an annual INCIDENCE rate, not a snapshot; secondary lines renew on a 3–5 yr cadence ⇒ standing prevalence 1.5–3.0%, ~3–4 positions/100 m — we were ~4× sub-representative); **fouling↔voiding COUPLED** (×3 hanging density inside a fouled patch — bidirectional mud-pumping feedback; independence was a documented vulnerability); **ballast ×3 within 20 m of an abutment** (deck runoff into the joint + impact; corrective work 4–8× more frequent there). **λ=3.0 resolves a self-inconsistency in the report** (it recommends λ=2–3 but its table demands 5–10% of sleepers; λ≥2.78 for ≥5% and ≤3.0 for the range ⇒ λ=3.0 uniquely satisfies both, giving 5.4%); its table's P(no hanging)≈0.25 also contradicts its own λ — we followed the derivation. ⚠ VERIFY: the pivotal "FI>30 on 10–20% of length" cites a **Slideshare deck** — re-check against the peer-reviewed GPR papers in the same report before submission; every entry in its spec table is flagged INFERENCE (derived from cited anchors) — state that in the paper. **CRACK: prevalence 0.25 CONFIRMED** (macroscopic EI loss in the Sinha 5–30% band ≈ 20–30% of an aged inventory); **location CORRECTED — and this overturns yesterday's moment-envelope conclusion.** Our envelope favours mid-span ~2:1, but real crack prevalence is **hogging-dominated >4:1–5:1**: over an internal support the TOP fibre is in tension (concrete's weakest mode) *and* takes deck runoff/chlorides, whereas mid-span soffit cracks close under compression and rarely give an EI loss. **Eurocode 4 mandates** a cracked section over **15% of the span each side of internal supports**. The moment envelope was simply the wrong lens for *prevalence* (it answers "where is bending large?", not "where does concrete fail?") — our own logged caveat ("hogging may crack more than its moment share") was right and is now quantified. Implemented: hogging:sagging **4:1**, ±17.5% of a span about the chosen section.
- **DAMAGE-LOCATION PRIORS — AUDITED 2026-07-14 (user asked: don't damages have preferred locations? do we need deep research?). VERDICT: 3 of 4 families already have one; the crack's uniform draw is DEFENSIBLE (computed, not assumed); NO deep research needed.** Status per family: **scour** = only at the chosen `scour_supports` (piers in the flow) — location prior by construction; **bearing** = abutment rotational springs only (Fernandes/Khan mechanism) — by construction (scope note: real bridges also bear on piers; ours are abutment-only); **hanging sleepers** = CITED density spike within ±15 m of the bridge transitions (`hang_p_transition=0.6`, `hang_trans_margin=15`) — a real, sourced prior; **ballast patches / rail pads** = uniform over the modelled track (could be transition-weighted like hanging groups, but that would be another extrapolation — left uniform); **wheel OOR/flats** = no bridge location (travels with the train; random phase). **THE CRACK was the only open case, and the computation OVERTURNED the intuition.** The natural argument — "cracks belong at high-moment sections, so U(0.1,0.9)·L wastes draws on inflection points" — is *wrong for a MOVING load*: the |M| ENVELOPE over all load positions is broad, because nearly every section is near a peak for SOME train position. Computed for our exact geometries (continuous Euler–Bernoulli beam, moving unit load, rigid supports; `scratchpad/moment_envelope.py`): **only ~4% (L60: eff. spans 20.1/19.8/20.1) and ~2% (L99.6: 4×24.9) of the [0.10, 0.90]·L range ever sees |M| < 35% of max** — two narrow inflection bands at x/L ≈ 0.29 and ≈ 0.70 (L60). Envelope peaks: **mid-spans |M|/max = 1.00 / 0.84, over-piers 0.51 / 0.42** ⇒ mid-span sagging dominates over-pier hogging ≈ 2:1. CONCLUSION: the uniform draw is ~96% structurally plausible AND is the broader, more conservative domain randomization for a NUISANCE the network must be invariant to — **keep it; document the justification** (this is a reviewer-facing answer, and the envelope makes a good methods figure). Optional 2nd-order refinement (NOT implemented): weight mid-spans ~2× the over-pier sections. **Deep research NOT warranted**: where moments peak is computable from our own model (no literature needed); the only thing literature could add is crack-prevalence-vs-location surveys, whose payoff is low given the envelope is broad and the crack is a nuisance rather than a head. **Two honest caveats for the paper:** (1) real hogging regions over piers may crack MORE than their moment share (top-fibre tension + water/chloride ingress) — the moment envelope alone cannot capture durability; (2) **torsion is out of scope** — the 2-D vertical model has no torsional moment, so "high torsional moment" crack sites are not representable (add to the limitations list next to pier rotation/tilt).
- **OPTION B ADOPTED + IMPLEMENTED 2026-07-14 (user, with a 3rd Lab PC): D01 saves the RAW time-domain signal; Python interpolates + crops at LOAD time.** Rationale: the noise-domain finding (below) proved before≠after the interpolation, so baking ANY choice into the data forecloses the other. Now the measurement model lives entirely at load time and can change forever without regeneration. IMPLEMENTED: `D01_DataProcessing.m` stores `Sol.Veh(1).A(1:3,:)` / `V(4:6,:)` / `acc_under(1:4,:)` un-interpolated + noise-free, plus per-passage `DimAcel/DimSpace/crop_start/crop_end/bridge_samp/L_bridge_eff`; A00 ships them in `data2save` (presence of `DimSpace` = RAW format flag); `core/dataset._raw_to_space_crop` mirrors the legacy transform exactly (`xx=linspace(1,DimSpace,DimAcel)`, `xi=1:DimSpace`, `np.interp` == `interp1` linear, 1-based inclusive crop) with a ragged-length guard; legacy (pre-interpolated) files still load via the `DimSpace`-absent branch. Legacy baked noise made CONDITIONAL (`if Damage.desvio>0`) so no RNG is consumed when off — and when ON it now lands in the TIME domain of the saved raw signal = the physically correct place. **STORAGE IS ~NEUTRAL (the user's worry was unfounded): `DimSpace ≈ 2.2·DimAcel` (space grid = 100 samples/m; ~5856 time samples vs the old ~5831-sample crop) ⇒ the raw series is about the size of the old cropped window while covering the WHOLE passage (approach+bridge+exit).** VERIFIED: Python self-test (`scratchpad/test_raw_crop.py`) — ramp reproduces to 0.0, 1-based indexing exact, legacy 5821-sample window reproduced, clamped-crop case OK. PENDING (user runs once per campaign): `scour_MATLAB/smoke_raw_parity.m` + `check_raw_parity.py` on real data → must print PARITY PASS (<1e-12) before the long generation.
- **ABLATION LADDER RESTRUCTURED 2026-07-14 (user's damage-matrix proposal + 3 agreed refinements).** Framing (the user's, and it is the right one): *the bridge maintainer cares about scour, bearing and crack — but must also be able to say whether RAIL and TRAIN damage are interfering with the detection.* Ladder, ONE factor per rung: **L60/3-span** `s0_scour` → `s11_bear` → `s12_crack` → `s13_bearcrack` (all BRIDGE damages) → `s14_prof` (ROUGHNESS rung) → `s15_track` → `s16_all`; **L99.6/4-span** `s21_scour4` → `s22_bearcrack4` → `s23_all4`. DECISIONS (user-confirmed): (1) **crack stays a NUISANCE**, not a head — the framework rule (head only if it shares scour's support-stiffness pathway; bearing does, crack doesn't) plus the stronger argument that *cracks are visually inspectable while submerged scour is not* — drive-by's value is scour. (2) **Profile gets its OWN rung** (`s14_prof`): fixed baseline profile through s0–s13 so those isolate bridge-damage interactions, then profile enters alone — isolating the roughness effect that collapsed the sprung channels in the deprecated L100 pilot. The profile is a *condition*, not a damage. (3) **"All damages" SPLIT into track (`s15`) then train (`s16`)** so a degradation is attributable to the rail vs the train — the user's own question demands it. Heads throughout = scour (per pier) + bearing (per abutment) ONLY.
- **WINDOWS MAX_PATH FIX 2026-07-14: dataset folder names shortened.** The self-describing name reached ~110 chars and, nested under the repo + results tree, blew past Windows' 260-char MAX_PATH. Folder is now `<stage>_L<len>_st<N>` (~20 chars, e.g. `s14_prof_L60_st267`); the FULL descriptor is preserved as `case_info.case_desc` (+ `case_info.txt`, + the new `case_info.stage`), so the dataset stays self-describing with no path pain. (Registry `LongPathsEnabled=1` + `git config core.longpaths true` are the OS-side complements, but many tools — incl. parts of MATLAB — still break on >260 even with it, so shortening is the real fix and costs nothing since everything is being regenerated.)
- **TRACK-EOV FIXES 2026-07-14 (both caught by the user; both real).** (1) **No healthy-track state existed**: `ballast_n_patches=[1 2]` / `hang_n_groups=[1 3]` fed `randi()`, which returns ≥1 — so EVERY state carried ballast AND hanging-sleeper damage. Real lines are mostly sound track, and the network needs the absent case or it learns the damage as a constant. Fixed to `[0 2]` / `[0 3]`. Other damages were already absence-capable (scour/bearing LHS include ~0 + healthy anchors; crack p=0.25; flats q=0.171; polygonisation p=0.30; pad failures Binomial) — the user's read of the code was right except for these two. (2) **Track damage was confined to the bridge**: non-transition hanging groups sampled `track_L_app + rand()*L_bridge` (bridge span only) and ballast patches only bridge±15 m. But the track *continues* before/after the bridge and degrades there — and the approach is exactly where the vehicle is excited *before* reaching the deck. Both now sample the full modelled track `[0, track_L_app+L_bridge+track_L_after]`; new `track_L_after=30` knob; pad window unified to the same `track_win`. ⚠ **LITERATURE STATUS (user asked "are all our values literature-based?"): NO — two remain flagged EXTRAPOLATIONS** and they are exactly these counts: `docs/track_eov_sampling_spec.md` marks "1–2 ballast patches /100 m" and "1–3 sleeper groups /100 m" as un-cited (as is the 0.83 failed-pads/100 m figure); the zero-inclusive lower bound is equally un-cited. Everything else on the track side is cited (void depths, group size 1–5, patch length U(5,20) m, GRF θ≈10 m, pad χ ranges, P_fail=0.005/yr, transition spike ±15 m) and the wheel side is fully verified. Crack prevalence 0.25 is also weakly sourced (report's 20–30%). PENDING: the notebook follow-up anchoring prevalence per 100 m of track.
- **NOISE-DOMAIN FINDING 2026-07-13 (user asked: is adding noise before vs after the D01 time→space interpolation the same?). ANSWER: NO — verified numerically (`scratchpad/noise_domain_check.py`, MC over 400 realizations, representative 2.22× upsampling).** D01 adds multiplicative noise in the TIME domain then `interp1`-resamples to space; the load-time injector adds it in the SPACE domain (post-interpolation). Because `interp1` is linear, baked-noise = interp(white) = a LOW-PASS-SMOOTHED, band-limited, SPEED-DEPENDENT process, not white: measured lag-1 autocorrelation +0.77 (vs 0.00 for load-time white); variance 0.67× (interpolation averages neighbours); and — counterintuitively — 1.46× MORE noise energy SURVIVES PAA-512 (the coloring concentrates energy at low spatial frequencies, exactly what PAA's averaging preserves; white noise's high-frequency energy is averaged away). So same nominal 5% ⇒ materially different perturbation. IMPLICATIONS: (1) `legacy_wheel` load-time mode APPROXIMATES but does NOT bit-reproduce the Stage-0/1 baked noise (docstring corrected); fine as a robustness knob, and the noise-A/B GATE conclusion (small-vs-large wheel-row delta) is unaffected since both are representative 5% perturbations. (2) The physically-correct sensor model is an ADDITIVE, signal-INDEPENDENT floor from datasheets (noise-density×√BW), and once additive the multiplicative-coupling domain difference vanishes (only white-in-time-vs-space + interpolation-variance remain, both 2nd-order and PAA-attenuated). (3) Acquisition noise is physically white-in-TIME (sensor+ADC sample in time); load-time injection is white-in-space — a standard, minor simplification to state explicitly in the paper. Recommendation ADOPTED: keep load-time noise; document the domain caveat; prefer additive-datasheet floor for the robustness arm.
- **DECK-MASS / FUNDAMENTAL-FREQUENCY FLAG 2026-07-13 (surfaced while grounding the paper methodology) — MUST VERIFY before Paper 1 submission / the Todd talk.** `A03_Bridge.m` sets `Beam.Prop.rho=9.6` (comment mislabels it "[kg/m]"; B01/B03 use it as DENSITY [kg/m³]) and `B43_ModelGeometry.m` sets `Beam.Prop.A=1` m² ⇒ deck mass/length = ρ·A = 9.6 kg/m. In ISOLATION that gives a ~20 m-span fundamental ≈ 100+ Hz (hand calc: ω1=(π/Lspan)²√(EI/m), EI=35e9·0.33=1.155e10, m=9.6 → ~136 Hz), i.e. ~20–40× a real bridge (~3–5 Hz). The Zhai track layer adds mass (rail ρ=121.3 kg/m + ballast-on-bridge), so the AS-BUILT coupled system may be physically reasonable — but this is unverified and it underpins the scour→frequency credibility the whole method rests on. ACTION: read the model's own computed fundamental from `B09_BeamFrq`/`B56_ModelFrq` output (or a quick modal run) and confirm it is a few Hz; report it + the effective spans in the paper. Do NOT assert any bridge frequency until checked. If it really is ~100 Hz, that's a latent unit bug affecting every dataset (would need a decision before more generation) — flagged, not assumed.
- **STAGE-0/1 REGENERATION QUESTION 2026-07-13 (user asked "re-run even Stage 0?") — DECISION: gate on a zero-cost noise A/B, do not regenerate yet.** Facts: Stage-0/1 DATA carries the legacy wheel-only multiplicative noise BAKED IN at D01 processing (not removable without full MATLAB regeneration); re-running only the ablation changes nothing. Why regeneration is NOT urgent: (a) Stage-0's two binding decisions — architecture selection (all arms saw identical data) and the localisation gate/champion pair (sprung channels, never noisy) — are insensitive to wheel noise; (b) every stage re-runs the full 8-sensor sweep, so a noise-free L60 sensor ranking arrives automatically with stage1_crack; (c) the only noise-touched claim is "wheels 20–50× worse" (needs a one-line caveat at worst). THE GATE (zero generation cost): when stage1_crack data lands, ablate its wheel singles TWICE — `SENSOR_NOISE=None` vs `{'mode':'legacy_wheel','desvio':0.05}` (same data, only noise differs; study names now separate automatically via `_nz-` suffix) — the wheel-row delta is the PURE noise effect. Small delta ⇒ Stage-0/1 stand as published with the caveat sentence; large delta ⇒ queue noise-free Stage-0/1 regeneration at the END (after Stage 3) for full chain uniformity.

---

## Reference shortlist
- Kamariotis et al. (2024) — value of SHM; gradual + shock scour; € costs; CNN drive-by scour detection (5–10% stiffness loss).
- Torzoni et al. (2024, 2026) — DT decision frameworks (DBN; active inference).
- Chadha et al. (2023); Ames et al. (2025) — risk-aware / risk-sensitive SHM decisions.
- Fernandes et al. (2024–2026) — drive-by railway-bridge damage / scour.
- Adnan (2026) — scour correlation between nearby foundations.
- FHWA HEC-18 (Evaluating Scour at Bridges) & AREMA Ch.8 — design (100-yr) / check (500-yr) floods; Plan of Action gauge triggers.
- Maroni, Tubaldi, Douglas et al. — Bayesian-network DSS for bridge-scour risk; monitoring-based adaptive water-level thresholds; VoI.
- Lamb et al. (2019) — probabilistic economic risk to Britain's railway from bridge scour during floods.
- Prendergast & Gavin — bridge natural frequency as a scour indicator (≈40% drop at 10 m scour).
- Briaud et al. — SRICOS-EFA scour-rate method (cohesive vs non-cohesive soils, critical shear stress).
- Scour fragility: "A methodology to derive scour fragility functions for masonry arch bridges" (NSF par 10579270); "Flood-Induced Scour Fragility Analysis of Bridges in Different Soil Conditions" (E3S 2025); MCEER-03-0002 (lognormal fragility statistics).
- EN 61373, EN 50155 — rolling-stock vibration/shock categories & electronics.
- Sandia NL — MEMS reliability in a vibration environment.
- MDPI Sensors (2024) — MEMS inertial reliability review; dual-mechanism gyroscope degradation (Wiener).
- Tronics AXO305 / GYPRO4300; ADI ADIS16488 — rail-qualified IMU datasheets.
- (full source list in the two `papers/Confiabilidade … MEMS …` reports.)
