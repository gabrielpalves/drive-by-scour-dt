# Drive-by DT — framework rationale & decision log

A living record of **what** we decided, **why**, and the **references** that back it,
so the methodology section of the paper writes itself and every step is defensible
to reviewers. Newest decisions appended per section. Status ∈ {decided, built,
open}. (`built` = implemented + tested in the repo.)

> Maintained continuously: whenever we make or change a modelling choice, add an
> entry here with its justification and citation.

---

## 1. Classifier / sensors

**1.1 Champion = 2 sensors: RearBogie_Vert + CarBody_Pitch (DOF 2,5), PAA + N-HiTS, 61-class.** — *built*
Why: in the 540-run ablation this is the best 2-sensor pair (median MSE 0.414 ≈ full-8-DOF 0.386) and, unlike the alternative CarBody_Pitch + FrontBogie_Pitch, **both** of its single-sensor fall-backs are usable (CarBody_Pitch is the best single channel; FrontBogie_Pitch *alone* collapses 62% of the time). Sensor economy ("2 ≈ 8") is the headline of Paper 1.
Ref: our ablation (`ablation_analysis/summary_per_config.csv`). Sensor-economy framing aligns with drive-by SHM literature (Fernandes et al. 2024–2026).

**1.2 Observations sampled from a held-out MATLAB library, not live re-simulation.** — *built*
Why: one live TTBI passage is slow, and (historically) live Python re-sim mispredicted. Sampling a held-out set generated by the *training* pipeline removes train/serve skew and makes a fine monthly clock cheap (cf. Torzoni's pre-computed `recordings_*.npy`).
Status note (skew investigation, 2026-06-15): live Python re-sim **still** mispredicts — healthy is fine (0%→1%) but every damaged state is read as ~healthy (20/40/55% → 0–1%). Ruled out: the 13-vs-61-class metadata bug (fixed); the damage interface (Python's legacy `DOF_ChangeRate_value` maps to `scour_rates[n//2]` = central pier, same as MATLAB `A00`); and the **rail profile** (Python `TTBI_2D/x.mat`/`h.mat` are byte-identical to the MATLAB training profile `scour_MATLAB/Calc.ProfileData15_05.mat` — both pipelines use ONE fixed profile via `Profile.Type=2`). Remaining cause is in the **physics/geometry port**; prime suspect = the geometry constants forced in `b19_generate_profile.py`'s Type-2 debug block (`L=325.80`, approach `30/30`, `L_bridge=39.90`) vs `a04_options.py`'s `minL_Approach=16/minL_After=10`, which can misalign the (correct) profile against the simulation's actual track positions. Decisive next test (not yet run): compare a Python vs MATLAB signal at the same damage. **Practical decision:** generate the probe (and any DT observation) via MATLAB — it inherently uses the training pipeline. Repairing live Python is an optional, separable follow-up.

**Side-finding (record for the paper):** training AND the held-out set use a SINGLE FIXED rail profile (`Calc.ProfileData15_05.mat`, MATLAB `Profile.Type=2`). So rail-profile irregularity is NOT a source of operational variability in our data (only speed, temperature, vehicle properties, and signal noise are). Consistent (champion trained and served on the same profile), but a documented scope limit; rail-profile *as a damage type* (`profile_intensity`) is a separate, deliberate toggle.

---

## 2. Deterioration (scour)

**2.1 Scour = Kamariotis gradual power-law + Compound-Poisson flood shocks.** — *built*
Why: scour is flood-driven; a gradual creep plus discrete flood jumps is the accepted phenomenology.
Ref: Kamariotis et al. (2024), *Quantifying the value of SHM*.

**2.2 Multi-foundation correlation via a Gaussian copula** (river common factor + proximity term). — *built, optional*
Ref: Adnan (2026) for the foundation-proximity correlation term.

---

## 3. Cost / decision

**3.1 Monetary cost model with verified € figures** (c_failure 5e7, c_inspect 2e4, c_repair 6e5, downtime 1.5e5/day, 2% discount). — *built*
Ref: Kamariotis et al. (2024).
Flood-branch actions added 2026-06-24 (placeholders, set from project data): `restrict_operations` ≈ **3e3 €** (a couple of slow/light probe passages = a small reduced-capacity penalty, far below an inspection so the cheap probe has a real niche) and `interrupt` ≈ **3.2e5 €** (= inspection + ~2-day precautionary closure). Ordering do_nothing(0) < restrict(3e3) < inspect(2e4) < interrupt(3.2e5) < repair(5.1e6), so the flood planner escalates only as the risk warrants.

**3.2 Failure = a LOGNORMAL fragility P(fail | scour) = Φ((ln s − ln θ)/β), reported as a SWEPT assumption.** — *built 2026-06-15* (`costs.py::failure_prob` now lognormal; `calibrate.py::fragility_sensitivity` sweeps median θ × dispersion β; validated: P(0)=0, P(θ)=0.5, brittle β=0.05 near-step vs ductile β=0.4 gradual; cost-optimal repair threshold moves sensibly — brittle repairs later, ductile earlier)
Why: the scour literature uses a **lognormal** fragility CDF (not logistic) — justified by non-negativity of scour and multiplicative uncertainty. IM = scour intensity (our % stiffness loss, or relative scour depth Y = scour/foundation depth); θ = median capacity (50% failure point); β = lognormal dispersion. The planner ranking is not robust to the curve, so we **sweep** θ and β rather than commit to one. TTBI never models collapse — failure is a separate layer.
Literature parameters to bracket the sweep: masonry-arch (shallow, brittle) θ≈**1.52 m**, β≈**0.05** (near-step); deep piers β≈**0.3–0.4** (median geotech-dependent); counter-intuitively, piles in stiff clay can fail at *shallower* scour than in soft sand (loss of lateral confinement).
Refs: Maroni/Tubaldi/Douglas et al.; Lamb et al. (UK railway masonry); "A methodology to derive scour fragility functions for masonry arch bridges" (NSF/par.nsf.gov 10579270); "Flood-Induced Scour Fragility Analysis of Bridges in Different Soil Conditions" (E3S 2025); MCEER-03-0002 (lognormal fragility statistics). `digital_twin/calibrate.py::fragility_sensitivity`. **OPEN: implement the lognormal swap in `costs.py`; pick the swept θ/β range; decide whether IM = % stiffness loss or relative scour depth.**

**3.3 Planners: do_nothing / cost-VI (MDP) / heuristic (Kamariotis) / POMDP (myopic VoI) / hybrid.** — *built*
Why: a ladder from naive to information-aware; the POMDP is the only one that inspects rationally (inspection has purely informational value). Active inference is a planned upgrade (§5.3). A separate **`FloodResponsePlanner`** (§6.4) handles the major-flood branch for every routine planner, so "flood protocol ON vs OFF" isolates the value of flood-aware monitoring.

---

## 4. Risk perception

**4.1 Risk-sensitive objective via a coherent risk measure (CVaR / entropic), one aversion knob; default neutral = expectation.** — *built*
Why: a realistic asset owner is risk-averse to the catastrophic-failure tail; CVaR/entropic make that explicit and sweepable.
Ref: Chadha et al. (2023); Ames et al. (2025); Todd group (UCSD).
Finding (mock): with a cheap, sharp inspect available, risk-aversion buys **more monitoring** rather than fewer failures (the flood tail is largely irreducible by policy); without inspect, it repairs earlier. Robust across observation noise. **Figure 2026-06-24** (`plotting/risk_perception_figure.py` → `results/risk_perception/risk_perception.png`): sweeping CVaR_α from neutral to worst-5%, inspections per trajectory climb ~0.9 → 53 while **repairs stay flat (~0.1)** and **mean final damage is unchanged (~10.8%)**; the **CVaR_10 € tail rises** (10.8M → 12.9M) rather than shrinking. Mechanism: a cheap sharp inspection re-collapses the belief to a spike before the repair decision (CVaR≈mean on a spike), so risk routes into inspection; the catastrophic tail is dominated by sudden flood-shock jumps that outrun the inspect/repair loop. Cutting that tail needs faster monitoring or a shock-anticipating policy (→ the flood branch §6.4), not a more risk-averse objective.

---

## 5. Sensor health

**5.1 Three failure modes — transient burst / degraded / dead — with fault & noise scaling carbody < bogie < wheel.** — *built (placeholder rates)*
Why + ref: EN 61373 install categories give the physical basis and the vibration ratio. Functional RMS acceleration: **Cat 1 (car body) 1.01, Cat 2 (bogie) 5.40, Cat 3 (axle box) 38.0 m/s²** → wheel ≫ bogie ≫ car body, scaled by a vibration-fatigue acceleration law (Basquin/Steinberg, exponent ~6.4 for the S–N relation). Refs: EN 61373, EN 50155; Sandia *MEMS reliability in a vibration environment*; MDPI *Reliability of MEMS inertial devices…* (2024). See `papers/Confiabilidade MEMS Ferroviário.docx`.

**5.2 Degraded mode = persistent bias offset + scale-factor error + elevated noise** (not just noise). — *built*
Why: the dominant long-term MEMS failure is *parametric* (slow bias/scale-factor drift, VRE), not sudden death — the FMEDA "dangerous undetected" (λDU). VRE ≈ 0.5 °/h/g² (a 10 g bogie level → ~50 °/h bias). Time-to-parametric-failure (25 °/h bias) ≈ 1588 d at 25 °C → 558 d at 60 °C, i.e. car body ≫ bogie/axle lifespan. Arrhenius Ea ≈ 0.8–1.0 eV (general CMOS) / 0.16–0.22 eV (bias drift); infant mortality 0.3–5%; intermittent connector ≈ 200–500 FIT (Cat 3).
Caveat recorded in code: per-channel standardisation in our preprocessor largely removes a constant bias/scale, so for our PAA pipeline the *noise* term dominates the classifier impact; bias/scale are kept for raw-signal re-users and physical fidelity.
Refs: as 5.1 + MDPI *Reliability Assessment of MEMS Gyroscopes via Dual-Mechanism Synergistic Degradation (Physics-Informed Wiener Processes)* (2024); Tronics AXO305 / GYPRO4300 & ADI ADIS16488 datasheets.

**5.3 Coping: dead → fall back to the surviving single-DOF champion (and its wider confusion matrix, so the belief distrusts it); cheap inspect/replace-sensor actions.** — *built*
CONFIRMED (Step 1): **(b) dead-detected / degraded-undetected** — a dead sensor is obvious (flat/zero) → fall back; a degraded one is the λDU case (looks plausible, silently corrupts the estimate). The 2-sensor cross-check that catches some degraded cases is a later step toward full diagnosis (c).

---

## 6. Monitoring cadence & floods

**6.1 ~30 instrumented passages per month (default).** — *decided (build pending)*
Why: drive-by SHM counts only the *instrumented* train's crossings. A daily in-service passenger train ⇒ ~30/month; 60 is also realistic but heavier to simulate and makes no qualitative difference. We define the problem, so 30 is the chosen default.

**6.2 Routine vs flood regimes differ.** — *decided (build pending)*
Routine month: ~30 passages → (a) fuse for a near-certain monthly state, (b) sensor self-diagnosis. Flood month: few, risky, possibly-corrupted reads. ⇒ value of risk-awareness + sensor-health concentrates at floods.

**6.3 Major vs minor floods — JUSTIFIED.** — *decided*
Major flood = a hydrologically significant event, **observed externally via a river-stage gauge** → triggers the decision branch. Minor flood = small jump, **not signalled**; revealed only through subsequent routine drive-by data. We act only on observed major floods.
Literature basis (defensible for a structural-engineering paper): FHWA **HEC-18** and **AREMA Ch.8** define the **100-year design flood** (1% AEP) and **500-year check flood** (0.2% AEP); "erosion-critical" bridges carry a legally-required **Plan of Action** with **gauge-linked triggers** for inspection/closure — exactly our decision branch. Observation: river **stage** is measured precisely (±0.2%) by radar/pressure/acoustic sensors; **discharge** is inferred via a stage-discharge rating curve. Multi-source fusion (structural drive-by + hazard gauge) updated by a Bayesian Network is the state of the art (Maroni et al.; Kamariotis VoI). An advanced option is Maroni's **adaptive threshold**: solve the inverse-reliability problem for the stage that exceeds a target P_f given the SHM-updated capacity (vs a static 100-yr stage). Refs: FHWA HEC-18 (hif12003); AREMA Ch.8; Maroni et al. (Bayesian DSS, adaptive thresholds, Strathclyde/Heriot-Watt); Lamb et al. (Britain's railway scour economic risk).

**6.3b Flood-observation uncertainty → probabilistic classification.** — *built 2026-06-24* (`digital_twin/flood.py::FloodTrigger`)
Critically, the trigger is uncertain: discharge error is 2–6% in ideal flow but **50–200% when the rating curve extrapolates during the extreme floods that matter** (plus hysteresis/loop-rating). So "is this a major flood?" is itself noisy. **Implemented** as `P(classify major | severity) = Φ((ln s − ln s_major)/β_gauge)` — a lognormal CDF on a dimensionless severity `s` = (flood scour jump)/(mean jump). Construction (the clean story for the talk): the operator declares major when the gauge-inferred discharge crosses the design-flood threshold `s_major`; because the discharge inference is uncertain the gauge reads `gauge = s · ε` with ε lognormal of dispersion `β_gauge` (median-unbiased — the rating curve is the median relation), and marginalising the **hard threshold over the gauge noise gives the smooth CDF**. `β_gauge → 0` recovers the deterministic design-flood threshold; `β_gauge ≈ 0.6` is the default (50–200% extreme-flood rating error). Verified: `classify(observe_gauge(s))` reproduces `p_major(s)` in expectation. Figure: `plotting/flood_decision_map.py` → `flood_trigger_cdf.png`. To be refined with the Todd group; `s_major` and `β_gauge` are swept knobs.

**6.4 Major-flood decision branch.** — *built 2026-06-24* (`digital_twin/planner.py::FloodResponsePlanner`, integrated in `simulation.py::DTSimulator._major_flood_step`)
On an observed major flood the belief is **widened** by the uncertain scour shock (`flood.py::widen_belief`: a severity-scaled upward-diffusion kernel — the median label jump ≈ severity × mean jump, with dispersion folding in the gauge + jump uncertainty), then the owner chooses among **do_nothing / restrict_operations(probe) / inspect / interrupt** by a **risk-adjusted one-step Value-of-Information** rule (the same logic as the POMDP, generalised to several information actions with different cost, observation model, and protection):
`J(a) = action€ + expo·R(b) + E_obs[ commit(posterior) ]`, where `R(b)` is the risk-perceived failure cost carried by the (open) line, `commit(b)=min(R(b), c_repair)`, and the `expo·R(b)` exposure term (the failure risk an OPEN line carries while the survey result is pending, ≈ inspection-latency/step) **cancels among the three open actions but is removed by `interrupt`** — so closure is chosen precisely when `R(widened)` is large (near-threshold belief × severe flood). The probe carries a `reliability` factor (probability it yields usable data); a low value collapses its VoI → the planner escalates to inspect/interrupt (the **sensor-corruption escalation, at the decision level**), and the simulator also escalates a *realised* corrupted probe (`_probe_update` returns 0 usable passages → inspect). Restricted-ops fuses `n_probe` passages (multi-passage flood read). Risk aversion (RiskModel) up-weights the widened tail, lowering the bar for inspect/interrupt. The resulting four-region **decision map** (severity × pre-flood belief) and its shift under risk aversion / degraded sensors is `flood_decision_map.png`. **Finding (decision composition):** in the realistic regime (rare, already-severe major floods on a low pre-flood belief) the response is almost always **inspect**, with **interrupt** in the severe corner and the cheap **probe** occupying only a thin "barely-major + nearly-healthy" band — i.e. once a major flood is *declared*, doing nothing is rarely optimal.
**Finding (€ value, `plotting/flood_value_figure.py` → `results/flood_value/`):** because routine monthly drive-by already catches the post-flood scour at the next passage, the flood branch's effect on the *mean* life-cycle cost is small (a one-month head start + targeted closures). Its value is in the **catastrophic tail**: it trims the CVaR_10 cost by ~2% (POMDP/Hybrid, base fragility) up to **~6% under a conservative fragility** (θ=0.4, where flood-shock scour actually threatens failure). This is precisely the flood-shock tail the risk-perception result (§4.1) showed risk-aversion *cannot* reach — so the flood-ANTICIPATING protocol is the policy that addresses it (a small mean premium for tail insurance). The value grows further when routine monitoring is sparse/unreliable (untested lever). Fragility is swept throughout.

**6.5 Restrict-ops probe = a WIDER observation model; measured later, assumed now.** — *built 2026-06-24 (assumed); MATLAB measurement deferred*
The probe (low mass, locked low speed) is a *different operating condition* than the held-out library, so its confusion matrix should be **measured** from a matching MATLAB batch. Until that batch exists the probe uses an **assumed** wider banded matrix (`probe_sigma ≈ 1.0`, vs drive-by `≈ 0.7`); the harness already loads a **measured** matrix from `probe_dir` when supplied, so the real probe path is scaffolded (`run_comparison(..., probe_dir=...)`). `probe_corruption_prob` injects realised per-passage probe loss for the mock escalation demo. (Live Python re-sim now matches MATLAB, so the probe could alternatively be generated live — see the skew-fix note in §7.)

---

## 7. Open items (to resolve / research)

- **Fragility lognormal swap** — DONE (§3.2). Remaining: pick the reported swept θ/β grid for the paper, and decide IM = % stiffness loss (current) vs relative scour depth Y = scour/foundation depth.
- **Flood-classification model** — *BUILT (§6.3b, §6.4).* Chosen: **probabilistic** `P(major | severity)` lognormal CDF (hard threshold = the β→0 limit). Decision input #3 resolved: the planner consumes the **noisy observed severity** (gauge) — it both classifies (major/minor) and scales the belief widening + the response choice. Remaining: refine `s_major`/`β_gauge` and the probe confusion with the Todd group; optionally Maroni's adaptive (reliability-based) threshold.
- **Sensor diagnosis** — Step 1 uses (b) dead-detected / degraded-undetected (λDU) [CONFIRMED]; (c) 2-sensor cross-check is a later step (§5.3).
- **Active inference planner** — *decided: AFTER the multi-damage setup.* Port `Torzoni2026/` (pymdp) as a 4th planner to compare cost vs the POMDP; first check pymdp runs on Python 3.14 (else a 3.12 env).
- **Skew fix (live Python TTBI)** — *FIXED + VALIDATED.* Root cause: the Python port read the wrong rows for pitch — `d01_data_processing.py` had `pitch = Sol.Veh[0].V[0:3]` but MATLAB `D01` uses `V(4:6)` (V holds vertical velocities in rows 1-3, pitch RATES in rows 4-6). So DOFs 5,6,7 were vertical velocities mislabelled as pitch → ~10× scale mismatch on CarBody_Pitch (vs DOF2 accel which matched at 0.96), breaking the MATLAB-fit scaler → champion read everything as healthy. **Fixed to `V[3:6]`; validated: live Python champion now predicts 0→0, 20→20, 40→40, 55→55 exactly.** Localised via: damage propagates (14–21% change → not a damage bug); profile byte-identical; std ratio PY/MAT 0.96 (DOF2) vs 10.5 (DOF5). **Implication: live Python TTBI == MATLAB — the restrict-ops probe can be generated in Python (no separate MATLAB batch). Only the live/Python path was ever affected; MATLAB training/held-out data and the champion were always correct.**
- **MATLAB↔Python parity audit + Stage-2 geometry (2026-07-09) — three findings, all resolved/documented.**
  (1) **Mesh snapping / effective spans.** The 0.3 m element grid (sleeper 0.6 m / 2) cannot hit the intended support positions for the nominal lengths: B43 re-meshes L to `0.3·round(L/0.3)` and B02 argmin-snaps `linspace` supports to the nearest node. Effective geometry actually simulated: **L=40 → spans 19.8/20.1** (all single-scour data + champion + DT), **L=60 → 20.1/19.8/20.1** (Stage 0/1), nominal L=100 → 99.9 m with spans 24.9/24.9/25.2/24.9 *and a floating-point knife-edge tie* at the middle support (49.95 exactly between two nodes). Harmless when consistent (Stage 0 passed with unequal spans; Python replicates the identical cumsum/linspace/argmin op sequence so both sides snap alike), but papers should quote the effective spans. **Decision: Stage 2 uses L=99.6 = 4 × 24.9 m spans = 83 elements each — all 5 supports EXACTLY on nodes, no tie** (verified numerically; L must be a multiple of 4×1.2 m for 4 equal on-node spans).
  (2) **Crop window: the 'fixed' profile mode silently reverts the generalised crop to the 40 m window.** B19 Type-2 replaces the whole `Calc.Profile` struct with the 2023 saved struct, which *contains* `L_bridge=39.9` — so D01's generalised crop (`round(L_bridge)·100`) reads 39.9 on ANY bridge. **Verified on Stage-0 data: signals are 5831 samples (the 40 m window) on the 60 m bridge** — first-wheel travel 10→68.31 m vs bridge at 30→90 m, i.e. the instrumented vehicle NEVER REACHES pier 3 (69.9 m) inside the window, yet pier-3 scour was still quantified at MSE 0.79 (information travels through deck continuity — arguably *strengthens* the Stage-0 claim; state this in the paper). Python's b19 Type-2 hardcode mirrors the same behaviour, so MATLAB==Python (no parity break). Stage 1 (also 'fixed') will share the 5831 window → Stage-0↔Stage-1 comparison stays apples-to-apples; do NOT regenerate. **Stage 2 ('psd_fra' = Type 1, no struct replacement) automatically gets the true full-bridge window (~11831 samples).** TODO if a multi-damage live DT is built: generalise the Python Type-2 branch (and port `psd_fra` A04 block to Python a04) so the live path matches whichever window the stage's training data used.
  (3) **Noise skew fixed: MATLAB D01 adds measurement noise to the WHEEL channels only; Python d01 was adding it to all three groups** (body/bogie accel + pitch too) → live-DT observations were noisier than training on the champion's channels. `TTBI_2D/d01_data_processing.py` fixed to wheel-only (training data is the reference; sensor faults live in `digital_twin/sensor_health.py`). Validated by re-running the live champion sweep post-fix.
- **Stage 3 "all-damage" EOV arm — DECIDED 2026-07-09 (user; spec in `docs/stage3_alldamage_spec.md`).** Kitchen-sink robustness stage for the ABLATION paper only (DT paper keeps the lean set): L60/3-span, scour+bearing heads unchanged, nuisances = crack + psd_fra + ballast patches + hanging sleepers + rail pads + wheel OOR (all per-passage, logged, data-anchored via the verified NotebookLM deep-research spec `docs/track_eov_sampling_spec.md`). Rationale: the deep research showed ballast/void settlement mimics scour IN-BAND (0.5–15 Hz, profile-mediated — NOT covered by stationary psd_fra), so track EOVs add real coverage; wheel-flat fundamental (~7.7 Hz @80 km/h) is the one vehicle-side in-band threat; pads = designed NEGATIVE CONTROL (content >30 Hz sprung cutoff and >PAA-512 Nyquist ~85–110 Hz → predicted null). PAA band analysis says PAA-512 keeps every band that matters; pre-registered contingency if scour MSE inflates = n_segments 1024/2048 re-run before RAW. Scope boundary (paper limitation sentences): pier rotation/tilt NOT representable (piers are boundary springs, not members — a deck-node rot-spring would be deck fixity, not tilt; needs frame/3D), lateral/torsional/SSI/prescribed settlement out; suspension drift already covered by vehicle variability; completeness claim is PATHWAY-BASED (cover confounders sharing scour's vertical low-frequency support/track-stiffness pathway), not exhaustive.
- **Other deterioration mechanisms (track/vehicle layers)** — *superseded 2026-07-09 by the Stage-3 decision above for the ablation paper; still out of scope for the DT paper.* Modelling time-evolution/degradation of the rail irregularity profile AND of the other bridge/track layers (ballast, sub-ballast, sleepers, rail pads, wheels, etc.) would be too far from this study's scope. Scope is kept on **scour**: finish the single-scour DT → then multiple-scour and possibly bearing and/or crack. A literature search on per-layer degradation models is needed if ever pursued.
- **Multi-damage — ABLATION first, then DT.** *Sequence (decided):* single-scour DT → **multi-damage ablation** → later DT extensions. **Stage 0 STARTED 2026-06-24** (engine built; data generates on the PC): formulation decided = **multi-output REGRESSION** (one continuous head per target support; `core/task.py` `task='regression'` switch, classification stays the default so the single-scour ablation is byte-identical). Independent LHS scour sampling for TRAINING (broad joint coverage); the copula correlation is a DEPLOYMENT assumption only. Torzoni's classify-then-regress = a later comparison arm; add a per-pier "damaged?" detection head only if regression false-positives on healthy piers. **Staged:** Stage 0 (2 piers, no bearing → can drive-by *localise*?) → Stage 1 (+bearing → scour-vs-bearing disentanglement) → Stage 2 (4-span scale-up). Reduced grid: 1-sensor sweep → best pair (~9 configs), single seed, no 30-seed MC. New metrics: per-pier MSE, aggregate MSE, **localisation accuracy**. Built: `scour_MATLAB/A00_Run.m` multi_scour mode (per-pier `scour_vector` label), `core/dataset.load_ttbi_dataset(target_supports=...)`, the `core/task.py` abstraction threaded through models/trainer/robustness/pipeline, and `comprehensive_ablation_multidamage.py`. See [[multi-damage-problem-setup]], [[dt-flood-branch]].
- **Sensor-health numbers** — replace `SensorHealthParams` placeholders with the values in `papers/Confiabilidade MEMS Ferroviário.docx` (EN 61373 ratio 1.01:5.40:38.0; bias-life 1588→558 d; VRE 0.5 °/h/g²; Arrhenius Ea; infant mortality 0.3–5%; 200–500 FIT).
- **Confounder damages = training-set EOVs.** — *decided 2026-07-06 (build pending)*
  Crack + rail-profile degradation enter the multi-damage study as **randomized nuisances in the TRAINING set** (domain-randomization / EOV-augmentation), NOT as regression heads and NOT as evolving DT states — nuisances need no evolution law; only decision-relevant states do. **Bearing is the exception** (confounds scour via the same support-stiffness pathway) → keeps its own regression head (Stage 1). Methodological rule (open-set SHM literature): a damage type present only at TEST time corrupts a closed-set model — confounders must be in the training distribution for a robustness claim. **Staging: Stage 0 stays clean** (pure multi-scour localisation gate); nuisance EOVs enter at Stage 1. Precedent: Fernandes et al. 2025 (crack+bearing+scour in one training set); a 2026 S&IE drive-by study trains across distinct track-irregularity conditions; "domain randomization" is the sim-to-real term of art.
  - **Crack** = damaged-element (uniform-block) local EI reduction — *renamed 2026-07-17: formerly mis-labelled "Sinha-style"; Sinha 2002 is a tapered model parametrized by crack depth* — **BUILT in BOTH pipelines 2026-07-06** (Python `TTBI_2D/damage_config.py::apply_cracks`; MATLAB `B00_Calculations.m::local_apply_cracks`, exact mirror: elements within [loc±lc] get I×(1−intensity), lc≤0 = single element; Fernandes-compatible ratios 0.05/0.1 ≈ 14/22% EI loss). Smoke-tested end-to-end (`scour_MATLAB/smoke_damage_toggles.m`): exactly 1 element at 0.78·I, full dynamic solve OK.
  - **Rail-profile degradation** — **BUILT in MATLAB 2026-07-06**: `A00_Run.m` `profile_mode` = `'fixed'` (legacy, byte-identical) | `'fixed_scaled'` (measured profile × per-passage amplitude factor; multiplier mirrored in `B19`/Python `b19`) | `'psd_fra'` (profile REGENERATED per passage from the FRA PSD with new random phases — no profile memorisation; severity = FRA class 1–6 drawn per passage, Berawi A_v ladder in `A04_Options`). Rationale: amplitude-scaling one fixed profile is only a first-order knob (deep-research verdict: degradation changes PSD shape; single profile → spatial overfitting); PSD-class regeneration is the defensible representation, anchored to **EN 13848** D1 SD classes / FRA classes; evolution (if ever needed): SD grows ~linear/exponential with tonnage (Sato; Shenton). Per-passage draws are logged in each file (`crack_log`, `profile_log`) for traceability — they are NOT labels. NOTE: adding profile EOV changes the training distribution → champion must be re-confirmed in the reduced ablation (planned anyway). Python live path (probe/DT) keeps the fixed profile; add `psd_fra` there only if DT observations should carry profile EOV.
  - **Bearing 'target' mode** — **BUILT 2026-07-06** (`A00_Run.m` `bearing_mode` = off | fixed(legacy) | target): Stage 1 bearing state [left,right] joins the LABELS — ONE joint LHS over scour+bearing dims (broad joint coverage) + single-bearing anchors; saved per file as `data.bearing_vector` and in `damage_states.mat::BearingStates`.
  - **Bearing treatment — SOURCE-VERIFIED 2026-07-10 (for Paper 1 §methodology).** Mechanism = rotational spring at the abutment deck nodes, healthy k_r=0 (free rotation) — identical to Fernandes 2025 and Khan 2022. Khan 2022 (verified from the PDF) treats k_r as CONTINUOUS, sweeping 0 → 10^12 Nm/rad ("supports start behaving almost like fully fixed — 93% — at 10^12"), validated FULL-SCALE on an Irish railway bridge (damaged vs rehabilitated states); Fernandes 2025 binarises it to {0, 10^9} with 10^9 = the minimum drive-by-detectable seized bearing (Feng 2023 / O'Brien line). OUR design: keep Fernandes's 10^9 anchor as bearing_max but restore Khan's continuity — LHS over [0, 10^9], regressed as seized-% (k/10^9 × 100), bearing = HEAD not nuisance (decision-relevant + shares the support-stiffness pathway with scour → the Stage-1 disentanglement question). RANGE VERIFIED ON OUR OWN MODEL (2026-07-10, `results/bearing_sensitivity/bearing_sensitivity_sweep.png`; 8-point live-TTBI sweep k_r = 10^8→10^12 on the entry abutment, L60/3-span): the response change tracks the analytic fixity ratio k/(k+4EI/L_span) almost exactly; at bearing_max=10^9 (fixity 0.31) the relative RMS signal change is already 10–16% on the sprung channels (CarBody_P 15.9% — the MOST bearing-sensitive channel — CarBody_V 14.1%, FrontBog_V 11.8%, RearBog_V 10.5%), i.e. comfortably learnable in-simulation (Khan's "1e9 = minimum detectable" was a REAL-WORLD claim; our clean channels see 10^8 at ~2%). Saturation at ~10^11 (fixity 0.98; 10^11→10^12 adds <2% more). So the HONEST limitation of [0, 10^9] is RANGE COVERAGE, not a noise floor: it spans fixity 0–31% ≈ the lower third of the available dynamic range (~35% of the saturation response), and the linear seized-% label maps near-linearly to response over it (good for regression). DECISION: KEEP bearing_max=10^9 for the whole staged campaign (comparability + Fernandes anchor + learnable range, pending confirmation from the Stage-1 bearing parity); extension to ~10^11 with LOG-spaced sampling (fixity-ratio label) = quantified future work — a linear label over [0,10^12] would concentrate all response in the first ~1% of label range. Side-note: bearing sensitivity ranks CarBody_Pitch ≫ carbody/bogie verticals ≫ wheels/bogie-pitch — consistent with the Stage-1 auto-pair flipping to FrontBogie_Vert+CarBody_Pitch once bearing heads entered the task.
  - **Bearing-head LOADER — BUILT + smoke-tested 2026-07-08** (Python side of Stage 1). `core/dataset.py` reads `bearing_vector` when `config['bearing_targets']` (e.g. `['left','right']`) is set, laid out AFTER the scour heads: label = [scour_1..S, bearing_1..B]. **Bearing is normalised to a "seized %" 0-100** (bearing / bearing_max x100, bearing_max read from `case_info.mat::bearing_max_Nm_rad` or `damage_states.mat`, else observed max) so the MSE loss balances heads instead of being swamped by the 1e9-scale stiffness. Threaded: `core/task.py` (`n_outputs`=scour+bearing; `n_scour_outputs`/`n_bearing_outputs`; `evaluate` scores localisation over SCOUR heads only and adds `scour_mse`/`bearing_mse`), `_cache_stem` (`_b<targets>` -> distinct cache), pipeline DT metadata (`bearing_targets`), and `comprehensive_ablation_multidamage.py` (`BEARING_TARGETS` toggle; None = Stage 0 byte-identical). Stage 1 is now turnkey: set `BEARING_TARGETS=['left','right']` + point DATASET at a `stage1_bearing` folder. Linear stiffness normalisation is a documented choice (matches the linear LHS sampling; log-scale is a possible refinement). NOTE: bearing head-vs-nuisance is still the open methodological question Stage 1 answers empirically (`scour_mse` with vs without the bearing head).
  - **STAGE presets + turnkey Stage-0 ablation — BUILT 2026-07-08.** `A00_Run.m` now has a `STAGE` selector (one line = one paper case): `stage0_multiscour` | `stage1_bearing` | `stage1_eov` | `stage1_full` | `stage2_4span`; each sets geometry + which damages are targets vs EOV nuisances. Verified end-to-end at reduced counts (stage1_full: `bearing_vector`, `crack_log`, `profile_log`, `BearingStates` all saved correctly). `comprehensive_ablation_multidamage.py` rewritten TURNKEY: Phase-1 single-DOF sweep → AUTO-selects best 2 sensors (lowest PAA+N-HiTS agg MSE from the Optuna DBs) → Phase-2 pair → results summary (`results/MD0_summary/MD0_leaderboard.csv` with per-support MSE + **localisation accuracy** + `MD0_parity_best.png`) — the summary fills the gap that per-head/localisation metrics were computed but never persisted with run_robustness=False. Smoke-tested on the real Stage-0 data (loader→PAA→both archs→train→metrics) in .venv (torch yes, optuna no → full run is PC-only). Bundle `multidamage_ablation_bundle.zip` (script + core/training/plotting + scour_MATLAB, NO data) for copy-extract-run on the data PC.
  - **Stage-1 ablation code READY 2026-07-09.** `comprehensive_ablation_multidamage.py` now has a `STAGE` selector (stage0_multiscour | stage1_bearing | stage2_4span) mirroring A00 — sets DATASET + TARGET_SUPPORTS + BEARING_TARGETS in one line; outputs go to `results/<STAGE>_summary/`. Added `disentanglement_report()` (the approved Stage-1 metric): from the single-damage anchor subsets it computes **`scour_leak_on_bearing_%`** (false scour a seized bearing induces where scour is truly absent — the dangerous case, ideal ~0) and `bearing_leak_on_scour_%`, written to `disentanglement.csv`; leaderboard adds `scour_mse`/`bearing_mse`; parity plot labels scour(%) vs bearing(seized %). Leakage math unit-tested; 4-head loader/model/evaluate synthetic-smoke-tested (py3.13). Delivered as `multidamage_stage1_bundle.zip` (ablation STAGE=stage1_bearing + MATLAB STAGE=stage1_bearing + code, no data). Predicted Stage-1 folder: `L60_3span_multi_scour_scourS2-3_bearTGT_dano0-60pct_states267_Npass50_varNVST` (17 anchors + 250 LHS; CONFIRM states count).
  - **Batch-crash fix (2026-06 lab-PC MATLAB crashes, probable cause):** `A04_Options` had live figure flags ON and `C03_TTB_2D_Plots` runs after EVERY passage and never closes figures → thousands of accumulated figures in a dataset run → OOM. All plot flags now commented with a batch-safety note; per-state `rng(damage_seed·1e5+DC)` also added in A00 so all EOV draws are reproducible.
  - Refs: EN 13848-5; FRA track classes; ORE/ERRI B176 + German spectra; Sato / Shenton track-degradation models; Sinha et al. (2002); Fernandes et al. (2025); open-set SHM (e.g. "Structural damage classification under varying environmental conditions and unknown classes via open set domain adaptation", MSSP 2024); Gemini deep-research report 2026-07-06 (to be saved in `papers/`).
- **Stage-1 (bearing head) RESULTS — ANALYSED 2026-07-12** (Lab-PC upload `results/Stage1/`; ran the 2-arm bundle PAA_NHiTS + PAA_S2V_NHiTS; Optuna healthy: every study 75/75 trials). **Bearing-as-HEAD CONFIRMED**: auto-picked pair (FLIPPED to FrontBogie_Vert+CarBody_Pitch) bearing MSE 7.0 (left 4.8 / right 9.2 → RMSE ≈ 2.2 / 3.0 seized-%), tight parity on all 4 heads — real skill, validating the sensitivity-sweep prediction (10–16% rel-RMS at 1e9) and the continuous-[0,1e9] design (range caveat unchanged). Scour cost of adding the bearing dimension: champion-pair scour MSE 0.757 → 1.798 (RMSE 0.87 → 1.34 pp; per-pier 1.78/1.81 balanced; localisation 0.9898 → 0.9883) — disentanglement largely succeeds; residual leak quantified: false-scour-from-bearing 2.37 pp, false-bearing-from-scour 3.43 seized-% (S2V pair: 1.85/2.85). The pair flip is bearing-driven and coherent: (a) the k_r sweep ranks CarBody_Pitch most bearing-sensitive; (b) FrontBogie_Vert is the leading sensor over the ENTRY abutment; (c) right-bearing MSE > left-bearing MSE on EVERY channel — the 'fixed'-profile 40 m crop (§ parity-audit finding 2) ends before the exit abutment, so right-bearing information arrives only through deck continuity (same mechanism as Stage-0 pier 3; quote in paper). S2V robustness note: the champion wins 7/8 singles, but S2V wins the PAIR by ~10% (agg 3.95 vs 4.40; scour 1.62 vs 1.80) — opposite sign to Stage 0 where S2V hurt the pair (+69%). Policy unchanged (architecture fixed at Stage 0); report as a robustness observation, not a selection change. Stage 1 is FINAL — never regenerate; feeds Paper-1 Results §5.
- **Stage-2 (4-span scale-up) RESULTS — FORENSICS 2026-07-12: data used the PRE-FIX L=100 geometry, and all sprung channels collapsed. Regeneration at L=99.6 recommended; EOV design review FIRST.**
  (1) *Provenance:* the Lab PC generated with the v3 bundle (before the parity-audit L=99.6 fix); the shipped ablation driver in `results/Stage2/` was hand-edited to `DATASET="L100_4span_..."`. So the simulated bridge is the audit-rejected effective 99.9 m with spans 24.9/24.9/25.2/24.9 and the FP knife-edge mid-support tie. The geometry is CONSTANT across states/passages (deterministic snap) ⇒ it cannot cause the collapse and the data is internally consistent — but it contradicts the documented design and the committed L=99.6 decision, so Stage 2 must be regenerated at L=99.6 regardless of the EOV question (get-it-right rule).
  (2) *The collapse* (`results/Stage2/results/stage2_4span_summary/leaderboard.csv`): all six sprung channels sit at scour MSE ≈ 327–340 ≈ label variance (U(0,60) → 300) and bearing MSE ≈ 923–937 ≈ variance (U(0,100) → 833), localisation ≈ chance — the predict-the-mean fixed point; each study's Optuna best plateaus at that exact value across 19–27 COMPLETE trials (75 total each) ⇒ hyperparameter-robust unlearnability, not undertraining. Only the wheel (unsprung) channels retain skill: singles 222–240; auto-pair Wheel1+Wheel2 scour MSE 126 (RMSE ≈ 11 pp), bearing 431, loc 0.744, with a diffuse-but-correlated parity cloud. Forensics ELIMINATED: crop bug (D01 generalised window verified; study metadata shows correct 5 heads / supports [2,3,4] / PAA-512) and code drift (shipped `core/`, `training/`, `plotting/` byte-identical to repo; only the driver's STAGE/DATASET lines differ). Note the wheels are the ONLY channels carrying added measurement noise (D01 wheel-only) — and still won.
  (3) *Attribution is CONFOUNDED by design:* Stage 1 → Stage 2 changed BOTH the geometry (3-span/60 m → 4-span/~100 m, 5-D label LHS with only 250 points) AND the EOV nuisances (crack_p = 1.0 — a fresh random crack EVERY passage, EI-loss U(0.05,0.30) anywhere in [0.1,0.9]·L — plus `psd_fra` profile re-drawn per passage with class from {4,5,6}). The physics hypothesis for the inversion (wheels beat sprung, opposite of Stage 0): per-passage roughness dominates the suspension-band (0.5–15 Hz) response of body/bogie channels, masking the quasi-static scour deflection signature, while the unsprung wheel rides the profile + local deflection directly. Potentially a PUBLISHABLE finding — IF the EOV design survives literature scrutiny.
  (4) *Plan:* (a) NotebookLM deep research on profile/crack EOV realism — prompt at `docs/deep_research_prompt_profile_crack_eov.md` (key questions: per-passage vs per-state profile redraw; FRA class set for 70–90 km/h; crack as persistent per-state nuisance vs per-passage transient; axle-box-vs-carbody precedent; scour drive-by with roughness). (b) Then run the `stage1_full` isolation preset (already in A00: L60/3-span + bearing target + crack + psd_fra) to attribute EOV-vs-scale-up on known-good geometry — it also gates Stage 3 (same EOV superset). (c) Then regenerate Stage 2 at L=99.6 with the settled EOV design. (d) `smoke_stage3.m` before any Stage-3 generation. Optional zero-cost pilot on the idle Lab PC meanwhile: a designed MIXED pair (best wheel + best sprung, e.g. Wheel1_Vert+FrontBogie_Vert) on the existing L100 data to test the profile-reference + deflection-sensor fusion hypothesis (question 7 of the research prompt); its qualitative answer survives regeneration.
- **EOV DESIGN REVIEW — RESOLVED + IMPLEMENTED 2026-07-12** (deep-research report received: `papers/Drive-By Scour ML Literature Design.{md,docx}`; NotebookLM/Gemini — treat per-number claims with the usual quote-check discipline, flags below). REPORT VERDICTS, all adopted: (1) **Profile: per-STATE draw, NOT per-passage** — published drive-by ML uses fixed/per-scenario profiles (Locke 2020 one fixed realization, robustness holds "provided the roughness profile remains constant"; NuBe-DBBM fixed P00/PA1/PA2 profiles — Sarwar & Cantero; Fernandes fixed FRA class 4 per scenario); physically, track geometry evolves over MGT (Sato/Shenton) and EN 13848-2 D1 repeatability is ≤0.5 mm @95% pass-to-pass — a fresh class+phase redraw per train "simulates an impossible geotechnical environment". (2) **Class FIXED at FRA 4** (roughest permissible at 70–90 km/h ≈ Class-4 80 mph passenger limit; classes 5/6 = premium track, unrealistically smooth for a scour-prone regional line) + **0.5 mm additive per-passage jitter** (EN 13848-2 repeatability). (3) **Crack: per-STATE, prevalence ~0.25** (persistent condition; p=1.0 destroys the healthy-deck baseline — "ontological failure"; location U(0.1,0.9)·L, EI-loss U(0.05,0.30) Sinha-type retained). (4) **Track-layer damage (Stage 3): per-STATE too** (same persistence argument); **wheel OOR stays per-passage** (a different fleet train each passage — consistent with vehicle-property randomization). (5) The sprung-collapse physics is VINDICATED (suspension filtering + carbody 1–3 Hz resonances vs quasi-static deflection; ABA literature: unsprung = direct kinematic trace of profile+deflection) and **continuous scour REGRESSION from sprung channels under per-pass roughness is "virtually undocumented"** — the L100 result is publishable as a negative finding with the design caveat. (6) **Fusion hypothesis endorsed**: wheel (profile reference) + sprung (inertial response) ≫ wheel+wheel (TSD/two-axle residual logic, OBrien/Keenahan). VERIFY-BEFORE-PAPER flags: crack prevalence 20–30% (weakest sourcing in the report), EN 13848-2 0.5 mm number, the Locke/NuBe-DBBM methodology quotes, Fernandes "fixed class 4 + 100 passages" (user knows the thesis — quick check). IMPLEMENTED (MATLAB): A00 `crack_draw/profile_draw/track_draw='per_state'` (draw at j_pass==1, held across the state's passages; logs keep per-passage shape), `crack_p=0.25`, `profile_fra_classes=4`, `profile_jitter_sd_mm=0.5`; B19 seeds the PSD phase draw per state (global stream saved/restored) and forces the speed-independent `min_dx` grid under phase-lock (default dx tracks v·dt — same seed would NOT give the same track at different speeds otherwise) + adds the per-passage jitter; A04 passes phase_seed/jitter through; new folder tags `crackST`/`prof-psd_fraST`/`trackEOVST` keep legacy folders distinguishable; new A00+driver preset `stage1_crack` (bridge-damage stage: scour+bearing+crack, profile fixed — Fernandes-comparable, and disambiguates crack-vs-profile if stage1_full degrades). PYTHON live-path mirror TODO unchanged (b19 psd_fra not ported; live DT keeps fixed profile).
- **ABLATION GRID UPGRADE 2026-07-12 (user decision):** `N_TRIALS` 75→100 (TPE `n_startup_trials` auto-scales to n_trials//4 = 25 — the "larger initial sampling plan"), `SEEDS=[42,1337,2026]` (3 independent Optuna runs per config; extendable to 5). Leaderboard: one row per seed + `leaderboard_median.csv` (median + IQR = the paper table, consistent with the median-not-mean rule); auto-pair ranks by MEDIAN champion MSE across seeds; train/val split stays fixed (random_state=42, trainer.py) so seed spread isolates init/HPO variance. NOTE: more trials cannot rescue an unlearnable task (Stage-2 sprung studies plateaued at label variance across 75 trials, hyperparameter-robust) — the upgrade buys RANKING confidence, not skill. `EXTRA_PAIRS` config added (designed pairs beyond auto top-2). **Mixed-pair pilot shipped** (`pilot_stage2_L100_mixed_pairs.py`, in bundle v7): Wheel1+FrontBogie_Vert and Wheel1+CarBody_Vert on the existing L100 data, 75 trials/seed42 for budget-matched comparability, summary to `results/stage2_4span_L100pilot_summary/`. **Measurement-noise question (user asked, decision documented):** current model = legacy wheel-channels-only, MULTIPLICATIVE (D01: std = 0.05·|signal|; sprung channels noise-free). Physically all channels carry sensor noise, and the multiplicative form keeps SNR constant (unrealistic; an additive floor punishes small signals). DECISION: keep the noise model FIXED through the ablation chain (comparability; and it biases AGAINST the wheels-win finding — wheels won while being the only noisy channels — so the finding is conservative, state this in the paper), and test all-channel noise as a LOADER-LEVEL augmentation robustness arm later (additive, SNR anchored to the rail-qualified IMU datasheets already in the reference shortlist — no regeneration needed since D01 noise is baked at processing time but extra noise can be injected at load time). Campaign bundle **v7** rebuilt (89 files: revised A00/A04/B19, new driver, pilot, README_STAGE2.md v7 with the new run order: pilot ∥ smoke_stage3 → stage1_crack → stage1_full → Stage-2 regen L=99.6 → Stage 3).
- **NOISE POLICY REVISED 2026-07-12 (user decision — supersedes the "keep legacy noise through the chain" line in the previous entry): generation is NOISE-FREE; measurement noise moves to LOAD time.** Why: (1) the noise model stays configurable per experiment AND per channel without ever regenerating data — sensor grade genuinely differs by mounting position (EN 61373 vibration severity carbody ≈1 : bogie ≈5.4 : axle ≈38; `papers/Confiabilidade (Sensores) MEMS Ferroviário(s).docx`), and the legacy D01 model (multiplicative 5%, wheel-only) was both position-blind and SNR-constant (unrealistic); (2) bulletproof-methodology rule — noise belongs to the OBSERVATION model, not the physics data. IMPLEMENTED: A00 `use_signal_noise=false` (D01 then adds nothing; new folders tagged `varVST` — no `N`); `core/dataset.py` gains `sensor_noise` config + `_inject_sensor_noise()` (applied to RAW signals pre-PAA, exactly where D01 acted; deterministic seed 42; cache stem noise-tagged so clean/noisy caches never collide; first mode `legacy_wheel` reproduces the old D01 distribution on noise-free data; per-channel additive floor modes = future robustness arm anchored to the rail-qualified IMU datasheets); driver knob `SENSOR_NOISE=None` (noise-free chain default). Chain-comparability note (for the paper): legacy noise was WHEEL-ONLY, so the sprung champion channels were always noise-free — sprung-pair comparisons across old (varNVST) and new (varVST) stages are unaffected; only wheel-channel leaderboard rows change meaning (new stages: noise-free wheels). Python live path needs no code change (d01 noise fires only when `Damage.desvio>0`; set 0 for models trained noise-free); `smoke_stage3.m` already runs desvio=0. FACT for the record: existing Stage-0/1 datasets have the legacy wheel noise BAKED IN (applied at D01 processing; not removable) — they stay valid as published, and `legacy_wheel` injection makes new data distribution-comparable if ever needed.
- **RE-RUN POLICY 2026-07-12 (user): never EXTEND existing Optuna studies — re-runs start FROM SCRATCH.** Driver `RUN_TAG` implemented: when set (e.g. "v8"), every study/output name gets the suffix → fresh full-budget studies; old DB rows/weights preserved (provenance). New datasets need no tag (dataset-keyed DB paths ⇒ no collision). Exception by design: the L100 mixed-pair PILOT deliberately RESUMES the old 75-trial/seed-42 context (budget-matched comparability with the existing wheel-pair studies) — do not tag it. Bundle **v8** rebuilt with the noise policy + RUN_TAG + varVST folder names. FOOTGUN FIX 2026-07-13: the noise config is now part of the study NAME too (`_cfg_suffix()`: `_nz-<mode>` + `_<RUN_TAG>`) — without it, re-running a stage with a different `SENSOR_NOISE` would silently resume studies whose earlier trials trained on differently-noised data.
- **L100 MIXED-PAIR PILOT — RESULTS 2026-07-13 (`results/Stage2/results/stage2_4span_L100pilot_summary/`; 75 trials/seed42, budget-matched to the deprecated L100 leaderboard). FUSION CONFIRMED, with a suspension-chain gradient.** FrontBogie_Vert+Wheel1_Vert: scour MSE **105.1** vs Wheel1+Wheel2 126.1 (**−17%**; RMSE 10.3 vs 11.2 pp), localisation **0.797** vs 0.744, pier-2 MSE 74.3 vs 124.3 (−40%), false-scour-from-bearing leak 19.2 vs 22.7 pp (bearing head slightly worse, 448 vs 431, driven by bearing_right). The remarkable part: FrontBogie_Vert ALONE is collapse-level on this data (scour 327, loc 0.42) yet adds 17% as a fusion partner — the TSD/two-axle residual mechanism (wheel supplies the roughness reference; the bogie supplies the suspension-filtered inertial response), exactly what the deep-research report predicted (OBrien/Keenahan logic). Gradient: CarBody_Vert+Wheel1 (155.5) is WORSE than wheel+wheel — one more suspension stage between partner and track destroys the fusion value; the partner must sit LOW in the chain. Methodological consequence: the greedy auto-pair (top-2 singles) is demonstrably suboptimal under roughness EOV (best pair = #1 + #6 single) → `EXTRA_PAIRS=[[1,3]]` (FBvert+W1) is now the DEFAULT in every stage for chain-wide comparability. Caveats: deprecated L100 geometry + per-passage EOV data, single seed — treat as a pilot direction, to be re-established on the regenerated chain.
- **EN 61373 CORRECTION 2026-07-13 (user caught an over-claim).** The carbody:bogie:axle ≈ 1:5.4:38 severities are random-vibration TEST levels for equipment QUALIFICATION by mounting position — they describe the vibration ENVIRONMENT a device must survive, NOT measurement noise added to an acquired signal. Correct uses: sensor RANGE selection per position, reliability/aging scaling (what the MEMS report uses them for → `digital_twin/sensor_health.py` territory), and second-order vibration-rectification bias (VRE ∝ g²; the docx cites gyro VRE 0.5°/h/g²). The load-time noise model must instead use per-channel ADDITIVE floors from sensor DATASHEETS (noise density [µg/√Hz] × √bandwidth; rail-qualified IMUs in the shortlist: ADIS16488, Tronics AXO305/GYPRO4300) — the indirect position dependence enters only because higher-vibration positions need larger full-scale ranges, which typically carry higher noise densities (a datasheet fact, not an EN 61373 number). Comments corrected in core/dataset.py, A00, README.
- **🔴 CRITICAL — DECK MASS WAS 1000× TOO LIGHT. FIXED 2026-07-15 (rho 9.6 → 9600 kg/m). Every dataset generated before this is INVALID.** The user supplied Fernandes's published parameters for this exact bridge — I = 0.33 m⁴, 2 × 20 m spans, 0.3 m elements, E = 3.5e10 N/m², **rho = 9600 kg/m**, 3% damping — which finally resolved the deck-mass flag raised 2026-07-13. `A03_Bridge.m` carried `Beam.Prop.rho = 9.6`; since `B43` sets `Prop.A = 1 m²` and `B01/B03` build the mass matrix as `rho_n · A_n`, that value IS the deck mass per unit length ⇒ **1000× too light** (9.6 kg/m ≈ a 0.004 m² rod, not a deck). VERIFIED with the model's own numbers (`plotting/beam_freq.py`: Euler–Bernoulli FE, consistent mass, the real `k_v0 = 344e6 N/m` support springs): **rho=9.6 → f₁ = 130.6 Hz** (L40/2-span) vs **rho=9600 → f₁ = 4.13 Hz** (textbook for a 20 m railway span); ratio = √1000 = 31.6× exactly. Also L60/3-span 132.2 → 4.18 Hz; L99.6/4-span 86.9 → 2.75 Hz. **Why this is severe, not cosmetic:** a ~131 Hz deck is effectively RIGID to a vehicle whose car body sits at 1–3 Hz and bogies at ~10–30 Hz, so the vehicle–bridge *interaction* — the entire physical basis of drive-by monitoring — carried almost no deck dynamic signature. The models could still read scour (support-stiffness loss still changes the quasi-static deflection), which is precisely why this hid for so long: **the ML worked, so nothing looked broken.** CONSEQUENCES: (1) all previously published numbers (Stage-0 0.757 / loc 0.99, Stage-1 bearing, the L100 pilot) are **invalid**, not merely superseded — they describe an unphysical bridge; the paper drafts must stop quoting them even as "direction". (2) **The roughness finding may change**: with a correct ~4 Hz deck sitting right in the car-body/bogie band, the sprung channels have a real deck signature to find, which they never had before — `s14_prof` may now answer differently. (3) Timing was pure luck: the whole campaign was already being regenerated. LESSON for the paper's honesty section: a physically wrong model that still trains well is the most dangerous kind of bug — this is the argument for the frequency sanity-check now on the verify list.
- **BEARING RANGE EXTENDED 2026-07-15 (user asked: are we considering higher values than Fernandes's? — answer: no, and we should).** Old design: `k_r ~ U(0, 1e9)`, label = seized-% = k_r/1e9. **The flaw:** Fernandes's 1e9 is the *minimum drive-by-DETECTABLE* seized bearing (a detectability FLOOR from the Feng/O'Brien line), so using it as our MAXIMUM put the entire label range between "healthy" and "barely detectable" — backwards. Khan (2022) sweeps k_r continuously to 1e12 with near-full fixity, so real seized bearings live far above 1e9. Response is governed by the analytic **fixity ratio** φ = k/(k + 4EI/L_end) (confirmed by our own sweep, `results/bearing_sensitivity/`); for our deck 4EI/L_end = 2.31e9 (L60) / 1.86e9 (L99.6), so k_r = 1e9 is only **φ ≈ 0.30 / 0.35** — the lower third of the available response. But a *linear* draw over a large k_r range is equally wrong (it piles all the response into the first ~1% of the label). **FIX:** draw **φ ~ U(0, 0.95)** and invert k_r = φ/(1−φ)·4EI/L_end (φ=0.95 → k_r = 4.39e10 at L60). φ is bounded [0,1], **near-linear in response** (a good regression target), and **geometry-normalised** — so the bearing label means the same thing on the L60 and L99.6 bridges, which raw k_r never did (a hidden cross-rung comparability bug in the old design). Fernandes's 1e9 survives as a **reportable landmark** (φ≈0.30), not a cap; the documented RANGE limitation is now **removed** from the paper. IMPLEMENTED: A00 samples fixity in the joint LHS + anchors, stores BOTH `bearing_vector` (k_r, the physics) and `bearing_fixity` (the label); manifest records `bearing_fixity_max`, `bearing_k_ref_Nm_rad`, `bearing_label='fixity_ratio'`; `core/dataset.py` prefers `bearing_fixity` (→ fixity %) and falls back to legacy `k/bearing_max` for old files.
- **TRACK COUNTS ANCHORED + CRACK LOCATION CORRECTED 2026-07-15 (Gemini deep research — too large for NotebookLM; `papers/Track Defect Prevalence Data Search.{md,docx}`).** The last un-cited numbers in the damage model are now anchored; full before/after table + caveats at the top of `docs/track_eov_sampling_spec.md`. **The prevalence paradox is RESOLVED**: the cited "~50% of concrete sleepers show some voiding" and our ~9% mechanical model are both right because most voids are SUB-THRESHOLD (<1.0 mm = accommodation; 1.0–2.5 mm = dynamic-impact threshold, where a 1–2 mm void raises adjacent contact force up to 70% / wheel loads ~85%; only 10–20% of visibly-settled sleepers exceed 1.5–2.0 mm ⇒ **impactfully unsupported = 5–10%**). CHANGES (all MC-verified, `scratchpad/check_track_stats.py`): hanging groups DU{0..3}→**Poisson λ=3.0/100 m**; ballast patches DU{0..2}→**Poisson λ=1.2/100 m** (GPR: FI>30 on 10–20% of route ⇒ 15% ÷ 12.5 m mean patch); **counts now scale with the modelled window** (120/159.6 m — the old fixed-count draw was itself an error); pads **0.005→0.02** (the cited 0.5% is an annual INCIDENCE rate, not a snapshot; secondary lines renew on a 3–5 yr cadence ⇒ standing prevalence 1.5–3.0%, ~3–4 positions/100 m — we were ~4× sub-representative); **fouling↔voiding COUPLED** (×3 hanging density inside a fouled patch — bidirectional mud-pumping feedback; independence was a documented vulnerability); **ballast ×3 within 20 m of an abutment** (deck runoff into the joint + impact; corrective work 4–8× more frequent there). **λ=3.0 resolves a self-inconsistency in the report** (it recommends λ=2–3 but its table demands 5–10% of sleepers; λ≥2.78 for ≥5% and ≤3.0 for the range ⇒ λ=3.0 uniquely satisfies both, giving 5.4%); its table's P(no hanging)≈0.25 also contradicts its own λ — we followed the derivation. ⚠ VERIFY: the pivotal "FI>30 on 10–20% of length" cites a **Slideshare deck** — re-check against the peer-reviewed GPR papers in the same report before submission; every entry in its spec table is flagged INFERENCE (derived from cited anchors) — state that in the paper. **CRACK: prevalence 0.25 CONFIRMED** (macroscopic EI loss in the Sinha 5–30% band ≈ 20–30% of an aged inventory); **location CORRECTED — and this overturns yesterday's moment-envelope conclusion.** Our envelope favours mid-span ~2:1, but real crack prevalence is **hogging-dominated >4:1–5:1**: over an internal support the TOP fibre is in tension (concrete's weakest mode) *and* takes deck runoff/chlorides, whereas mid-span soffit cracks close under compression and rarely give an EI loss. **Eurocode 4 mandates** a cracked section over **15% of the span each side of internal supports**. The moment envelope was simply the wrong lens for *prevalence* (it answers "where is bending large?", not "where does concrete fail?") — our own logged caveat ("hogging may crack more than its moment share") was right and is now quantified. Implemented: hogging:sagging **4:1**, ±17.5% of a span about the chosen section.
- **DAMAGE-LOCATION PRIORS — AUDITED 2026-07-14 (user asked: don't damages have preferred locations? do we need deep research?). VERDICT: 3 of 4 families already have one; the crack's uniform draw is DEFENSIBLE (computed, not assumed); NO deep research needed.** Status per family: **scour** = only at the chosen `scour_supports` (piers in the flow) — location prior by construction; **bearing** = abutment rotational springs only (Fernandes/Khan mechanism) — by construction (scope note: real bridges also bear on piers; ours are abutment-only); **hanging sleepers** = CITED density spike within ±15 m of the bridge transitions (`hang_p_transition=0.6`, `hang_trans_margin=15`) — a real, sourced prior; **ballast patches / rail pads** = uniform over the modelled track (could be transition-weighted like hanging groups, but that would be another extrapolation — left uniform); **wheel OOR/flats** = no bridge location (travels with the train; random phase). **THE CRACK was the only open case, and the computation OVERTURNED the intuition.** The natural argument — "cracks belong at high-moment sections, so U(0.1,0.9)·L wastes draws on inflection points" — is *wrong for a MOVING load*: the |M| ENVELOPE over all load positions is broad, because nearly every section is near a peak for SOME train position. Computed for our exact geometries (continuous Euler–Bernoulli beam, moving unit load, rigid supports; `scratchpad/moment_envelope.py`): **only ~4% (L60: eff. spans 20.1/19.8/20.1) and ~2% (L99.6: 4×24.9) of the [0.10, 0.90]·L range ever sees |M| < 35% of max** — two narrow inflection bands at x/L ≈ 0.29 and ≈ 0.70 (L60). Envelope peaks: **mid-spans |M|/max = 1.00 / 0.84, over-piers 0.51 / 0.42** ⇒ mid-span sagging dominates over-pier hogging ≈ 2:1. CONCLUSION: the uniform draw is ~96% structurally plausible AND is the broader, more conservative domain randomization for a NUISANCE the network must be invariant to — **keep it; document the justification** (this is a reviewer-facing answer, and the envelope makes a good methods figure). Optional 2nd-order refinement (NOT implemented): weight mid-spans ~2× the over-pier sections. **Deep research NOT warranted**: where moments peak is computable from our own model (no literature needed); the only thing literature could add is crack-prevalence-vs-location surveys, whose payoff is low given the envelope is broad and the crack is a nuisance rather than a head. **Two honest caveats for the paper:** (1) real hogging regions over piers may crack MORE than their moment share (top-fibre tension + water/chloride ingress) — the moment envelope alone cannot capture durability; (2) **torsion is out of scope** — the 2-D vertical model has no torsional moment, so "high torsional moment" crack sites are not representable (add to the limitations list next to pier rotation/tilt).
- **OPTION B ADOPTED + IMPLEMENTED 2026-07-14 (user, with a 3rd Lab PC): D01 saves the RAW time-domain signal; Python interpolates + crops at LOAD time.** Rationale: the noise-domain finding (below) proved before≠after the interpolation, so baking ANY choice into the data forecloses the other. Now the measurement model lives entirely at load time and can change forever without regeneration. IMPLEMENTED: `D01_DataProcessing.m` stores `Sol.Veh(1).A(1:3,:)` / `V(4:6,:)` / `acc_under(1:4,:)` un-interpolated + noise-free, plus per-passage `DimAcel/DimSpace/crop_start/crop_end/bridge_samp/L_bridge_eff`; A00 ships them in `data2save` (presence of `DimSpace` = RAW format flag); `core/dataset._raw_to_space_crop` mirrors the legacy transform exactly (`xx=linspace(1,DimSpace,DimAcel)`, `xi=1:DimSpace`, `np.interp` == `interp1` linear, 1-based inclusive crop) with a ragged-length guard; legacy (pre-interpolated) files still load via the `DimSpace`-absent branch. Legacy baked noise made CONDITIONAL (`if Damage.desvio>0`) so no RNG is consumed when off — and when ON it now lands in the TIME domain of the saved raw signal = the physically correct place. **STORAGE IS ~NEUTRAL (the user's worry was unfounded): `DimSpace ≈ 2.2·DimAcel` (space grid = 100 samples/m; ~5856 time samples vs the old ~5831-sample crop) ⇒ the raw series is about the size of the old cropped window while covering the WHOLE passage (approach+bridge+exit).** VERIFIED: Python self-test (`scratchpad/test_raw_crop.py`) — ramp reproduces to 0.0, 1-based indexing exact, legacy 5821-sample window reproduced, clamped-crop case OK. PENDING (user runs once per campaign): `scour_MATLAB/smoke_raw_parity.m` + `check_raw_parity.py` on real data → must print PARITY PASS (<1e-12) before the long generation.
- **ABLATION LADDER RESTRUCTURED 2026-07-14 (user's damage-matrix proposal + 3 agreed refinements).** Framing (the user's, and it is the right one): *the bridge maintainer cares about scour, bearing and crack — but must also be able to say whether RAIL and TRAIN damage are interfering with the detection.* Ladder, ONE factor per rung: **L60/3-span** `s0_scour` → `s11_bear` → `s12_crack` → `s13_bearcrack` (all BRIDGE damages) → `s14_prof` (ROUGHNESS rung) → `s15_track` → `s16_all`; **L99.6/4-span** `s21_scour4` → `s22_bearcrack4` → `s23_all4`. DECISIONS (user-confirmed): (1) **crack stays a NUISANCE**, not a head — the framework rule (head only if it shares scour's support-stiffness pathway; bearing does, crack doesn't) plus the stronger argument that *cracks are visually inspectable while submerged scour is not* — drive-by's value is scour. (2) **Profile gets its OWN rung** (`s14_prof`): fixed baseline profile through s0–s13 so those isolate bridge-damage interactions, then profile enters alone — isolating the roughness effect that collapsed the sprung channels in the deprecated L100 pilot. The profile is a *condition*, not a damage. (3) **"All damages" SPLIT into track (`s15`) then train (`s16`)** so a degradation is attributable to the rail vs the train — the user's own question demands it. Heads throughout = scour (per pier) + bearing (per abutment) ONLY.
- **WINDOWS MAX_PATH FIX 2026-07-14: dataset folder names shortened.** The self-describing name reached ~110 chars and, nested under the repo + results tree, blew past Windows' 260-char MAX_PATH. Folder is now `<stage>_L<len>_st<N>` (~20 chars, e.g. `s14_prof_L60_st267`); the FULL descriptor is preserved as `case_info.case_desc` (+ `case_info.txt`, + the new `case_info.stage`), so the dataset stays self-describing with no path pain. (Registry `LongPathsEnabled=1` + `git config core.longpaths true` are the OS-side complements, but many tools — incl. parts of MATLAB — still break on >260 even with it, so shortening is the real fix and costs nothing since everything is being regenerated.)
- **TRACK-EOV FIXES 2026-07-14 (both caught by the user; both real).** (1) **No healthy-track state existed**: `ballast_n_patches=[1 2]` / `hang_n_groups=[1 3]` fed `randi()`, which returns ≥1 — so EVERY state carried ballast AND hanging-sleeper damage. Real lines are mostly sound track, and the network needs the absent case or it learns the damage as a constant. Fixed to `[0 2]` / `[0 3]`. Other damages were already absence-capable (scour/bearing LHS include ~0 + healthy anchors; crack p=0.25; flats q=0.171; polygonisation p=0.30; pad failures Binomial) — the user's read of the code was right except for these two. (2) **Track damage was confined to the bridge**: non-transition hanging groups sampled `track_L_app + rand()*L_bridge` (bridge span only) and ballast patches only bridge±15 m. But the track *continues* before/after the bridge and degrades there — and the approach is exactly where the vehicle is excited *before* reaching the deck. Both now sample the full modelled track `[0, track_L_app+L_bridge+track_L_after]`; new `track_L_after=30` knob; pad window unified to the same `track_win`. ⚠ **LITERATURE STATUS (user asked "are all our values literature-based?"): NO — two remain flagged EXTRAPOLATIONS** and they are exactly these counts: `docs/track_eov_sampling_spec.md` marks "1–2 ballast patches /100 m" and "1–3 sleeper groups /100 m" as un-cited (as is the 0.83 failed-pads/100 m figure); the zero-inclusive lower bound is equally un-cited. Everything else on the track side is cited (void depths, group size 1–5, patch length U(5,20) m, GRF θ≈10 m, pad χ ranges, P_fail=0.005/yr, transition spike ±15 m) and the wheel side is fully verified. Crack prevalence 0.25 is also weakly sourced (report's 20–30%). PENDING: the notebook follow-up anchoring prevalence per 100 m of track.
- **NOISE-DOMAIN FINDING 2026-07-13 (user asked: is adding noise before vs after the D01 time→space interpolation the same?). ANSWER: NO — verified numerically (`scratchpad/noise_domain_check.py`, MC over 400 realizations, representative 2.22× upsampling).** D01 adds multiplicative noise in the TIME domain then `interp1`-resamples to space; the load-time injector adds it in the SPACE domain (post-interpolation). Because `interp1` is linear, baked-noise = interp(white) = a LOW-PASS-SMOOTHED, band-limited, SPEED-DEPENDENT process, not white: measured lag-1 autocorrelation +0.77 (vs 0.00 for load-time white); variance 0.67× (interpolation averages neighbours); and — counterintuitively — 1.46× MORE noise energy SURVIVES PAA-512 (the coloring concentrates energy at low spatial frequencies, exactly what PAA's averaging preserves; white noise's high-frequency energy is averaged away). So same nominal 5% ⇒ materially different perturbation. IMPLICATIONS: (1) `legacy_wheel` load-time mode APPROXIMATES but does NOT bit-reproduce the Stage-0/1 baked noise (docstring corrected); fine as a robustness knob, and the noise-A/B GATE conclusion (small-vs-large wheel-row delta) is unaffected since both are representative 5% perturbations. (2) The physically-correct sensor model is an ADDITIVE, signal-INDEPENDENT floor from datasheets (noise-density×√BW), and once additive the multiplicative-coupling domain difference vanishes (only white-in-time-vs-space + interpolation-variance remain, both 2nd-order and PAA-attenuated). (3) Acquisition noise is physically white-in-TIME (sensor+ADC sample in time); load-time injection is white-in-space — a standard, minor simplification to state explicitly in the paper. Recommendation ADOPTED: keep load-time noise; document the domain caveat; prefer additive-datasheet floor for the robustness arm.
- **DECK-MASS / FUNDAMENTAL-FREQUENCY FLAG 2026-07-13 (surfaced while grounding the paper methodology) — MUST VERIFY before Paper 1 submission / the Todd talk.** `A03_Bridge.m` sets `Beam.Prop.rho=9.6` (comment mislabels it "[kg/m]"; B01/B03 use it as DENSITY [kg/m³]) and `B43_ModelGeometry.m` sets `Beam.Prop.A=1` m² ⇒ deck mass/length = ρ·A = 9.6 kg/m. In ISOLATION that gives a ~20 m-span fundamental ≈ 100+ Hz (hand calc: ω1=(π/Lspan)²√(EI/m), EI=35e9·0.33=1.155e10, m=9.6 → ~136 Hz), i.e. ~20–40× a real bridge (~3–5 Hz). The Zhai track layer adds mass (rail ρ=121.3 kg/m + ballast-on-bridge), so the AS-BUILT coupled system may be physically reasonable — but this is unverified and it underpins the scour→frequency credibility the whole method rests on. ACTION: read the model's own computed fundamental from `B09_BeamFrq`/`B56_ModelFrq` output (or a quick modal run) and confirm it is a few Hz; report it + the effective spans in the paper. Do NOT assert any bridge frequency until checked. If it really is ~100 Hz, that's a latent unit bug affecting every dataset (would need a decision before more generation) — flagged, not assumed.
- **STAGE-0/1 REGENERATION QUESTION 2026-07-13 (user asked "re-run even Stage 0?") — DECISION: gate on a zero-cost noise A/B, do not regenerate yet.** Facts: Stage-0/1 DATA carries the legacy wheel-only multiplicative noise BAKED IN at D01 processing (not removable without full MATLAB regeneration); re-running only the ablation changes nothing. Why regeneration is NOT urgent: (a) Stage-0's two binding decisions — architecture selection (all arms saw identical data) and the localisation gate/champion pair (sprung channels, never noisy) — are insensitive to wheel noise; (b) every stage re-runs the full 8-sensor sweep, so a noise-free L60 sensor ranking arrives automatically with stage1_crack; (c) the only noise-touched claim is "wheels 20–50× worse" (needs a one-line caveat at worst). THE GATE (zero generation cost): when stage1_crack data lands, ablate its wheel singles TWICE — `SENSOR_NOISE=None` vs `{'mode':'legacy_wheel','desvio':0.05}` (same data, only noise differs; study names now separate automatically via `_nz-` suffix) — the wheel-row delta is the PURE noise effect. Small delta ⇒ Stage-0/1 stand as published with the caveat sentence; large delta ⇒ queue noise-free Stage-0/1 regeneration at the END (after Stage 3) for full chain uniformity.
- **🔴 EXTERNAL AUDIT VERIFIED + FIXED 2026-07-17 (ChatGPT Codex review of the MATLAB damage models; every claim independently re-verified against the code before acting; all fixes landed BEFORE any post-mass-fix data was generated).** Seven confirmed defects and their fixes, in severity order:
  (1) **Speed/temp LHS transposed** — `A00` called `lhsdesign(2, Npass)` (2 samples in Npass dims): each LHS column stratifies its 2 values into opposite halves ⇒ speed–temperature correlation **−0.75 exactly** (−0.78 under the default maximin criterion) and the (slow,cold)/(fast,hot) quadrants NEVER sampled, invisible in marginals. Affected every rung, every passage. FIX: `lhsdesign(Npass,2)'` + an in-loop corr/quadrant guard that errors on a degenerate draw.
  (2) **Track-layer damage landed off-deck with probability 1 (L60)** — descriptors were sampled in a [0,120] m window assuming the deck at [30,90], but B54 consumes GLOBAL sleeper coordinates and under `redux=0` the deck starts at `L_Aw = L_Approach + max_TL` = **123.0 m** (L60; [123,222.6] at L99.6, where the first ~37 m could still be hit). So s15/s16/s23's on-deck/transition confounders never existed; the defects acted as an approach-only EOV. FIX: descriptors are now declared BRIDGE-LOCAL (`Tk.x_bridge_local`); B54 shifts its sleeper axis by `num_app·spacing − x_bridge_local` before selection (legacy global descriptors still read bit-identically), returns placement diagnostics (`Model.TrackDmgDbg`), and `smoke_stage3.m` asserts the patch/hang/pad-failure land on/around the real deck.
  (3) **FRA PSD unit mismatch** — B19's axis is cycles/m but A04 fed the corner `Ω_c = 0.8245 rad/m` unconverted (the A_v conversion `1e-4/(2π)` was already correct — the bug was one constant): ~**16×** too much PSD at the 1.524 m cutoff, acceleration-weighted variance ~8.5× high, displacement RMS almost unchanged (+1.6% — why it looked fine in elevation plots). FIX: `inputs(3)=0.8245/(2π)` cycles/m; `smoke_audit.m` asserts implemented ≡ reference (rad/m form, consistently converted) across the band.
  (4) **0.5 mm per-passage physical jitter removed** — it misread EN 13848-2 (measuring-system repeatability, not physical track change; the repo even cited the same number for the phase-lock and as a physical delta), and being WHITE on a profile band-limited at λ≥1.524 m it contributed ~0 in-band while injecting 100% fictitious short-wavelength roughness (~125 g-equivalent hdd forcing to the solver Nyquist, loading precisely the wheel channels central to the sensor-placement finding; even self-inconsistent: implies 1.39 mm pass-to-pass @95% vs the cited 0.5). FIX: `profile_jitter_sd_mm = 0` (mechanism kept for a future BAND-LIMITED D1-band σ≈0.18 mm implementation if ever wanted); measurement noise stays load-time (existing policy).
  (5) **Wheel flats disabled; contact violations now measured and persisted** — every sampled flat exceeds the uplift threshold 12–38× (kinematic |ḧ| = π²v²/4R, length-independent under d = L²/8R, vs F_stat/m_wheel ≈ 88 m/s²) while the solver couples wheel–rail BILATERALLY (rides tension through the dip), and at dt = 1 ms a 10–60 mm flat gets 0.4–2.4 samples (aliased). FIX: `oor_flats_enabled=false` (polygonization stays — worst case ~0.9 g ≈ 10× below threshold, valid); B66 now measures tension over the WHOLE on-track path (old check was bridge-window only), per vehicle, and A00 persists `data2save.contact_log = [contactLost_bridge, contactLost_track, tension_frac_max, F_tension_max]` per passage so Python can filter; smokes assert zero tension. RE-ENABLING FLATS REQUIRES: unilateral/Hertzian contact with gap opening + re-contact, impact-resolving timestep (or event refinement), anti-aliased downsampling — a solver project, not a toggle. Do NOT "resample until contact holds" (censors the severest flats).
  (6) **Per-passage split leakage (Python)** — all five split sites did ungrouped seed-42 `train_test_split` over passages ⇒ ~40 train/~10 val passages of the SAME state (same label, same persistent crack/profile/track realization on EOV rungs) ⇒ val overestimates generalisation exactly where the ladder asks the nuisance-invariance question. (Scaler was NOT leaked — fit post-split on train, global per-channel.) FIX: loader now returns per-sample `groups` (= MAT-file/state id, cached as `cache_*_groups.npy`); ONE function `core.dataset.canonical_train_val_split` (GroupShuffleSplit, 20% of STATES, seed 42) used by scaler fit, trainer, summary, robustness (seed-varying grouped), confusion, pilot; cache stem gains a `_gs1` schema tag so every pre-audit cache is orphaned; legacy classification (one file per class) keeps the per-passage fallback with a loud warning. `check_split_grouping.py` = the invariant test (ALL PASS). CONSEQUENCE for numbers: grouped-val = UNSEEN-state interpolation — expect honest (higher) MSEs than any pre-audit run.
  (7) **Crack "Sinha" mis-citation** — the implementation is a uniform damaged-element EI block (even for lc>0), not Sinha's triangular taper (which is parametrized by crack depth, not EI-loss). Renamed in B00/damage_config + paper docs; physics unchanged (defensible Fernandes-compatible benchmark).
  ALSO FIXED IN THE SAME PASS: `comprehensive_ablation_multidamage.py` arch-policy check compared `STAGE` to the pre-ladder name `stage0_multiscour`, so the renamed `s0_scour` silently ran ONLY `CHAMPION_ARCH` — Stage 0 now runs all arms (`ARCH_SELECTION_STAGES`), and CHAMPION_ARCH is flagged VOID until the post-mass-fix s0 leaderboard re-selects it; `trainer.py` objective hardcoded `set_global_seed(42)` ignoring the config seed (the "3 independent seeds" varied only the Optuna sampler) — now seeds from the config while the canonical split stays fixed at 42; latent `NameError` in `robustness.py`'s progress bar. Track-spec doc: hanging-sleeper `g_v` void-depth severity was specified but NEVER implemented (all groups fully voided, binary) — documented as such, not silently claimed. TESTS: `smoke_audit.m` (LHS/PSD/jitter, ALL PASS in R2025b), `smoke_stage3.m` (parity + placement + contact, full solve), `check_split_grouping.py` (ALL PASS py3.13), B54 frame-mapping unit test vs the Python mirror (ALL PASS). Bundles MUST be rebuilt from this tree before any PC generates; the 2026-07-16 bundles carry all seven bugs. AUDIT CORRECTIONS TO THE AUDITOR (for the record): scaler-before-split was false; s0 architecture comparison was internally consistent (identical split, no fingerprint at s0) — its real gap was unseen-state interpolation; polygonization does NOT cause separation; s23's track window partially overlaps the L99.6 deck entry (not fully off-bridge).
- **🔴 EXTERNAL AUDIT — ROUND 2 VERIFIED + FIXED 2026-07-17 (Codex re-review of my round-1 fixes; each claim re-verified against the code, incl. loading the stored profile .mat).** Round 2 was substantially right — my round-1 "all 13 done, ready to distribute" was premature. Six more real defects, all now fixed:
  (1) **`fixed`-profile crop reverted to a 39.9 m bridge** (P0). `B19_GenerateProfile.m` Type 2 (`profile_mode='fixed'`) did `Calc.Profile = B` — replacing the ENTIRE live Profile with the stored struct `Calc.ProfileData15_05.mat`, whose fields are L_bridge=39.9, L_Approach=30, **L=325.8 m** (verified by loading it). So `D01_DataProcessing.m` cropped `round(39.9)*100` samples (5831) on a 60/99.6 m bridge (PSD rungs correctly used 7831/11831 — so `s13→s14` changed BOTH profile AND spatial window, breaking "one factor per rung"); B66's contact window used the wrong span; and on **L99.6 the stored 325.8 m realization is shorter than the 351.6 m track** (s21/s22 physics + crop invalid, pier-4 region outside the window). FIX: Type 2 now loads ONLY the elevation `h`, resampled (and wrapped/tiled if the live track is longer) onto the LIVE x-grid; all geometry stays live. D01/B66 then use the live L_bridge automatically. Because RAW is saved, existing data could be re-cropped in Python — but none exists yet, so it regenerates clean. `smoke_geometry.m` (new) asserts L_bridge, crop, and profile coverage for L60/L99.6 × fixed/psd_fra.
  (2) **MATLAB resume could silently mix code versions** (P0). A00 reuses the same `Results/<case>` folder and skips existing `NNNN.mat`; extracting a new bundle over a 2026-07-16 run would keep old states and regenerate only the rest, then overwrite the manifest to look clean. FIX: `gen_schema='audit-2026-07-17'` written to the manifest AND every `NNNN.mat`; a resume guard reads the existing manifest BEFORE overwriting and ABORTS if the schema differs. Use a fresh folder (or the guard fires) — never extract over an old run.
  (3) **Python could resume pre-audit Optuna studies** (P0). `_gs1` tags only the feature cache; the pipeline uses `load_if_exists=True` keyed by study name, and with `RUN_TAG=""` the name matched a pre-audit study exactly. FIX: `SCHEMA_TAG="gs1a20260717"` is now always in `_cfg_suffix()`, so every study/output name changes and old studies can't be resumed. (Cache stays reusable — the split it encodes is unchanged — only studies are forced fresh.)
  (4) **Champion auto-reselection did not exist — I over-claimed it in round 1.** `s0_scour` ran all arms, but `auto_select_pair` still ranked sensors by the hardcoded `RANK_ARCH=CHAMPION_ARCH=PAA_NHiTS`, and s11+ hardcoded the same — so a different winner's best pair was never sought and nothing propagated. FIX: `_pick_champion_arch()` selects the backbone from the completed single-DOF medians at s0, ranks the auto-pair with the WINNER, and writes `results/_champion_arch_<schema>.json`; every later rung READS it and hard-errors if absent ("run s0 first"). The pre-audit PAA_NHiTS default is gone.
  (5) **Stochastic robustness leaked the scaler across seeds.** `run_single_training` varied the split per seed while the cached scaler was fit on the seed-42 grouped train — so for other seeds some val groups were in the scaler fit. FIX: robustness keeps the CANONICAL seed-42 grouped split and varies only init/shuffle (also the correct estimand for init-variance; true CV-resampling would need a per-split scaler = separate analysis). (Low-severity residues noted, not blockers: the median-leaderboard parity plot uses seed `SEEDS[0]` as a representative; there is a single healthy group so healthy false-alarm isn't independently evaluated — the paper should reserve untouched test groups and replicate healthy/anchor states.)
  (6) **Contact validity wasn't closed end-to-end.** `contact_log` was saved but no loader read it; B66's bridge window used the wrong (reverted) geometry. FIX: B66 geometry is corrected by (1); `core/dataset._load_multi_output` now reads `contact_log`, DROPS any passage flagged with track tension (col 1 > 0), prints a summary, and ABORTS if the flagged fraction exceeds `_CONTACT_ABORT_FRAC=2%` (flats are disabled ⇒ expected ~0); `oor_flats_enabled` is recorded in the manifest. TESTS added/updated: `smoke_geometry.m` (4 configs), and the existing `smoke_audit`/`smoke_stage3`/`check_split_grouping` still pass. Bundles are rebuilt ONLY after `smoke_geometry` passes. HONEST STATUS: this was a genuine second round — do not treat round-1 as final; the ladder's "one factor per rung" property in particular was silently broken by (1) until now.
- **🔴 EXTERNAL AUDIT — ROUND 3 VERIFIED + FIXED 2026-07-17 (Codex re-review; it independently RAN an L99.6/fixed case and caught a bug I INTRODUCED in R2).** Round-2's "all green, bundles ready" was again premature. Six issues, all fixed and now contact-tested:
  (1) **My R2 profile tiling created real contact loss** (P0). The R2 fix used `mod()` to wrap the stored profile end (h=+0.195 mm) back to its start — a STEP discontinuity that spiked wheel accel to ~176 m/s² and tripped wheel-rail tension (F≈1.7e5 N) on L99.6 fixed; `smoke_geometry` passed it only because it didn't check contact. FIX: replace the wrap with a **continuous linear stretch** (map the live x-range onto the stored range, a ≤8% wavelength rescale, no seam/kink). `smoke_geometry` now asserts `contactLost_track==0` and tests L99.6/fixed at 70/80/90 km/h — **all show pure compression** (F_tension_max ≈ −1.18e5 N).
  (2) **Cross-stage study/DB collision** (P0). `define_save_locations` (core/utils.py) collapses every ladder dataset to the tag `all` (none contain noise/temp/vehicle/speed), and study names carried no STAGE — so s0/s11/s12… shared one SQLite DB + output dir, and `load_if_exists` let s11 silently RESUME s0's studies (even mixing 2-head weights into a 4-head bearing config). FIX: `_cfg_suffix` includes `_st-<STAGE>`, and a `_locs()` wrapper STAGE-prefixes the DB/output/cache paths — every rung is now isolated.
  (3) **Champion-manifest cross-PC catch-22** (P0). Bundles don't ship the manifest, so a fresh-folder s11+ errored ("run s0 first"), while a shared workspace hit collision (2). FIX: `CHAMPION_MANIFEST` path is env-overridable, a `CHAMPION_ARCH_OVERRIDE` escape hatch lets s11+ run on a PC without the file, and `_load_champion_arch` validates the manifest's schema (and notes dataset lineage). The manifest write prints a "copy this to the s11+ PCs" reminder.
  (4) **Provenance not enforced** (P0). A00 checked only the folder manifest's `gen_schema`, then trusted any `NNNN.mat` by filename; the `_gs1` cache fast-path returned stale features. FIX: A00 adds a **`gen_fingerprint`** (Npass, seeds, severities, ranges, toggles…) to the manifest and aborts resume on either schema OR fingerprint mismatch; the Python loader now REQUIRES `gen_schema=='audit-2026-07-17'` and `contact_log` in every file (hard error otherwise); the cache tag is bumped **`_gs1`→`_gs2`** (geometry/crop/contact changed the loaded samples) so older caches are orphaned.
  (5) **B66 bridge window 6 m off** (P1). It used `L_Approach + Position.x_0` (x_0 = max_TL+extra_L2) = 129 m, but the deck starts at `L_Aw` = L_Approach+max_TL = 123 m. Only the bridge column of contact_log was affected (the whole-track column the loader uses was correct). FIX: B66 uses `L_Aw`.
  (6) **Contact policy + reporting** (P1). The R2 global 2% drop could censor whole states silently. FIX: the gate is now **force-based** (peak tension > 1000 N, not any numerical fuzz), with a **per-state abort** (any state losing >20% of passages) plus a tightened 0.5% global abort; `_find_cfg` deterministically picks the SEEDS[0] representative so parity/disentanglement don't mislabel the seed; `D01` crop rounding fixed to `round(100*L_bridge)` (9960 at L99.6, was 10000). Both `smoke_audit` and `smoke_geometry` now `error()` on failure (nonzero exit). HONEST STATUS: three rounds in, treat any "done" as provisional until the smokes are green on the generating machine; the champion selection is still "best arch by median single-DOF MSE, then its best pair" (a documented greedy criterion, not a joint arch×pair search).
- **🔴 EXTERNAL AUDIT — ROUND 4 VERIFIED + FIXED 2026-07-17 (Codex re-review of R3; caught a scientific confound I introduced in R3 plus provenance/robustness gaps).** Five items; the profile confound and the provenance holes were the important ones:
  (1) **R3's linear STRETCH introduced a scale-up confound** (P0, scientific). Rescaling the whole 325.8 m stored profile to each track length shifted wavelengths differently per geometry (L60 −4.2%, L99.6 +7.9%, ~12.7% spectral gap), so the "fixed" baseline was no longer the SAME physical irregularity on L60 vs L99.6 — contaminating exactly the one-factor scale-up comparison. FIX: **triangle-wave REFLECT fold** — identity on [0, 325.8 m], mirror beyond. Verified that both decks (L60 ends 183 m, L99.6 ends 222.6 m) and crops sit within 325.8 m, so the stored realization is used EXACTLY (no rescale) for every geometry where it matters; only L99.6's tail (325.8→351.6 m, ~103 m past the deck exit, outside the crop) is mirrored, and `smoke_geometry` confirms it stays contact-clean at 70/80/90 km/h.
  (2) **Provenance couldn't distinguish R2/R3 from R4, and resume/saves were unguarded.** FIX: `gen_schema` bumped to **`audit-2026-07-17-r4`** (loader `_EXPECTED_GEN_SCHEMA` mirrors it); `gen_fingerprint` now saved in EVERY `NNNN.mat` and extended to cover polygonization + profile ranges + crack/OOR params; `save_progress.m` writes to a temp file then does an **atomic rename** (a crash can't leave a partial file that resume treats as done).
  (3) **The loader accepted incomplete/invalid datasets.** FIX: strict, manifest-driven validation in `_load_multi_output` — requires **contiguous** `0001..n_states` (a gap aborts), per-file `gen_schema` + `gen_fingerprint` matching the manifest, `contact_log` shape exactly (Npass, 4) with finite `F_tension`, no blanket `except` swallow, and a post-load completeness/length check. The cache fast-path validates `len(X)==len(y)==len(groups)` and a **provenance sidecar** (`_prov.json`) so a cache built from an older regeneration of the same-named dataset is rebuilt. `check_loader_provenance.py` (new) proves ALL of these reject their bad variant (stale schema, missing/mismatched fingerprint, gap, short/missing/NaN contact_log, real tension).
  (4) **Auto-pair ≠ best pair.** The greedy top-2-singles pair can be beaten by an interacting pair. FIX: `EXHAUSTIVE_PAIRS` flag runs all C(8,2)=28 pairs on the champion arch (default off — it's a compute decision; documented that greedy is only a heuristic and a joint arch×pair optimum needs >1 finalist arch). The user decides whether the "best placement" claim needs the exhaustive run.
  (5) **Reporting/provenance polish.** `check_split_grouping.py` now globs caches **recursively** (they moved into STAGE-prefixed sub-dirs, so the old top-level glob printed ALL PASS while skipping the real ones); the parity plot labels the seed of the model it ACTUALLY plots (SEEDS[0]); the champion manifest records `seeds`/`n_trials`/`exhaustive_pairs` and warns on protocol drift; B66's unused VBI=0 branch uses `L_Aw` too. TESTS (all green): `smoke_geometry` (reflection, contact-clean 70/80/90), `smoke_stage3`, `smoke_audit`, `check_split_grouping`, `check_loader_provenance` (new adversarial), A00 `checkcode` clean. HONEST STATUS: four rounds in — this resolves the confound and closes the silent-mixing risk, but I would still run one more external pass before the multi-day distributed generation; and the exhaustive-pair rigor is a deliberate user choice, not yet enabled.
- **🔴 EXTERNAL AUDIT — ROUND 5 VERIFIED + FIXED 2026-07-17 (Codex re-review of R4; confirmed the reflect profile is correct, flagged provenance/selection gaps + two scope decisions).** The user chose BOTH recommended options (exhaustive pairs + outer test set). Seven areas, all addressed:
  (1) **Provenance made end-to-end.** The manifest is now MANDATORY for multi-output loading, with an EXACT file inventory (no missing AND no extra `NNNN.mat`), a REQUIRED `_GENERATION_COMPLETE` marker (A00 writes it only when all n_states files exist), and per-file `gen_fingerprint` matched to the manifest. The cache fast-path validates the SOURCE dataset's schema+fingerprint+state-count+completion via a `_prov.json` sidecar, so a cache from an older/truncated/vanished source is rebuilt, not reused.
  (2) **MATLAB resume validates files, not just filenames.** A00 now checks each existing `NNNN.mat`'s schema+fingerprint (read cheaply from small TOP-LEVEL vars added by `save_progress`, no big-array load) and ABORTS on a foreign/old/mixed file instead of skipping it as "done."
  (3) **Canonical hash fingerprint.** The hand-maintained `sprintf` (which silently omitted params) is replaced by a **SHA-256 of a struct holding every run-defining knob + the actual DamageStates/BearingStates/BearingFixity matrices** (`jsonencode` → digest). Any config change changes the hash.
  (4) **Contact loss is HARD-FAIL.** With flats off and the reflect profile contact-safe, the loader now aborts on the FIRST passage exceeding the tension floor (1000 N) rather than silently censoring passages (which would bias the retained set / MNAR).
  (5) **Coordinated R5 schema bump.** MATLAB `gen_schema`→`audit-2026-07-17-r5`, cache tag `_gs2`→`_gs3`, Optuna study/champion-manifest `SCHEMA_TAG`→`gs3a20260717r5` — so no R3/R4 study, manifest, or cache can pair with R5 data.
  (6) **EXHAUSTIVE_PAIRS = True (user decision).** All C(8,2)=28 pairs run on the champion arch; the paper's "best placement" is the best pair by median TEST MSE. Documented caveat: this is the best pair *conditioned on* the champion architecture (a joint arch×pair optimum would need >1 finalist arch).
  (7) **Selection/reporting fixes + outer test set (user decision).** The canonical split is now **3-way grouped**: TRAIN (60%, scaler+fit) + inner-VAL (20%, HPO + champion/pair SELECTION) + untouched outer TEST (20%, the ONLY set the summary/leaderboard/parity/disentanglement report on) — so the paper's numbers never come from the set that chose the model. Also: `_find_cfg` resolves each per-seed row to its OWN seed's model (was always SEEDS[0]); the champion must win on a COMPLETE study matrix (all DOF×seed studies finished, ≥N_TRIALS) — an arch with 1 lucky study can't win; "BEST" is the best MEDIAN test MSE, not the luckiest single run; champion-manifest seed/n_trials mismatches are HARD errors; the manifest is written only AFTER a fully-completed s0 run. TESTS (all green): `check_split_grouping` (3-way, test-exclusion), `check_loader_provenance` (now 12 adversarial cases incl. mandatory manifest / completion marker / extra file / hard-fail contact — all REJECTED), A00 `checkcode` clean + SHA-256 path verified standalone, `parfor_lint` clean; `smoke_geometry`/`smoke_stage3`/`smoke_audit` unchanged (no physics change in R5). HONEST STATUS: five rounds in — the pipeline now enforces provenance end-to-end, hard-fails on contact loss, and reports on an untouched test set with exhaustive placement search. Still worth a final external pass before committing multi-day compute.

- **🔴 EXTERNAL AUDIT — ROUND 6 VERIFIED + FIXED 2026-07-18 (Codex re-review of R5; a 9-agent independent cross-check confirmed all 9 claims first-hand).** R5 was NOT ready to start: R5's OWN selection round re-introduced two defects, plus several hardening gaps. Every fix below was implemented and, where deps allow on this PC (no optuna/MATLAB here), tested. Two of Codex's framings were corrected by the cross-check (noted). Coordinated bump: MATLAB `gen_schema`→`audit-2026-07-18-r6`, loader `_EXPECTED_GEN_SCHEMA` mirror, Optuna `SCHEMA_TAG`→`gs3a20260718r6` (no R6 ladder data existed yet, so the bump is free). Areas:
  - **(C1, BLOCKING) Champion selection deadlock.** `_pick_champion_arch` required N_TRIALS trials in state COMPLETE, but the pipeline sizes a run by `len(study.trials)` and the `SuccessiveHalvingPruner` marks ~2/3 PRUNED — so COMPLETE never reaches N_TRIALS and NO arch was ever eligible → `RuntimeError`. FIX: a shared `_study_is_finished(study, n_trials)` = `len(study.trials) >= n_trials and n_complete >= 1` (mirrors the pipeline's own resume gate; robust to FAIL/stale-RUNNING; warns on thin COMPLETE samples). Applied to both `_pick_champion_arch` and `auto_select_pair` (the latter previously had NO completeness gate at all).
  - **(C2, SCIENTIFIC) Outer test set was the pair SELECTOR.** `summarize()` evaluated all ~52 configs on the untouched TEST set and crowned the argmin by median TEST MSE — winner's-curse over 28 pairs, and the "test" number was then no longer held-out. FIX: SELECT the best PAIR by median **inner-validation** MSE (`study.best_value`, verified to be the inner-val objective in `trainer.py`), restricted to `n_sensors==2`; REPORT that frozen choice's TEST MSE once. `leaderboard.csv` is kept but ranked by inner-val and labelled diagnostic-only (never re-read to pick the headline). Arch selection was already inner-val (unchanged).
  - **(C3, hardening — PARTIALLY confirmed) Cache fast-path.** Codex's zero-MATs / corrupt / swap attacks are NOT reachable in the normal build-then-reuse flow (the `complete` flag + count already flip and force a rebuild→strict-abort; the fast path returns a previously strict-validated snapshot). Real residual: a same-count file swap / in-place edit is invisible to `prov.json`. FIX (defense-in-depth): fold a sorted `(filename, size)` inventory (pure `os.stat`, no MAT reads) into `prov.json` + an explicit `complete==True` gate.
  - **(C4, SCIENTIFIC) Loader accepted un-audited passages.** `available < npass` (not `==`) + a load loop bounded by `min(200, available)` let a file with MORE passages than the manifest load its extras past the contact-tension gate; no finiteness/range guard on labels or signals; `Npass=0` silently substituted `available`. FIX: exact `available == npass_here`, loop bound on `npass_here`, finiteness on ALL contact_log columns, finite+[0,100] scour label / finite fixity∈[0,1] / finite signals, and reject `passages_per_state<=0`. **6 new adversarial cases added to `check_loader_provenance.py` → now 18/18 REJECTED.**
  - **(C5, hardening) MATLAB resume validated provenance, not payload.** A correctly-stamped but garbage `data` was silently SKIPPED forever (never regenerated), only to blow up at Python load. FIX: resume now checks `data` exists and holds `{AcelPrimVag, AcelRodaPrimVag, PitchPrimVag, scour_vector, contact_log, gen_schema, gen_fingerprint}` with `>=Npass` passages, aborting otherwise; completion marker is written atomically (temp+movefile) and any stale marker is deleted at the start of a run.
  - **(C6, practical — Codex's mechanism CORRECTED) MATLAB warned, never aborted, on contact loss.** Codex claimed a *wrong-column* bug (MATLAB warns on col 2, Python gates on col 4). The cross-check showed col 2 = `(F_onTrack_max>0)` and col 4 = `F_onTrack_max` derive from the SAME quantity, so `col4>1000 ⟹ col2=1` — MATLAB was never silent when Python rejects; the real defect was **warn-not-error**. FIX: A00 now HARD-FAILS (with a saved diagnostic) on `ContactLog(:,4) > 1000 N`, byte-for-byte the Python gate, so a multi-day run stops at the first offending state instead of finishing and being rejected at load.
  - **(C7, SCIENTIFIC) Fingerprint omitted the profile asset.** Fixed-profile stages (s0/s11/s12/s13/s14) read the rail irregularity from `Calc.ProfileData15_05.mat` (`B19:118`); only the string knob `profile_mode` entered `fp_cfg`, so swapping the asset changed every signal without changing the hash. FIX: fold a SHA-256 of the asset BYTES into `fp_cfg` (`local_file_sha256`), `'ABSENT'` if missing. (Hashing all code is overkill; git already versions it — a git-SHA note is an optional secondary, not a substitute.)
  - **(C8) Pair-matrix completeness + one-factor ladder + anchor stratification.** (A) `summarize()` now asserts the FULL pair matrix (all SEEDS, and under a search stage all 28 pairs) before declaring a best pair — mirrors the arch-matrix guard. (B) The champion **PAIR** is now frozen in the manifest (like the arch) and carried across the ladder; `PAIR_SEARCH_STAGES` controls which rungs re-search vs reuse the frozen pair, so the ladder is genuinely one-factor-at-a-time (see the OPEN decision below). (C) The grouped split is not anchor-stratified (~15% cross-seed chance no bearing-only anchor lands in test), but **seed 42 is verified to place 4/8 bearing + scour anchors in test** (independently reproduced); `disentanglement_report` now FAILS LOUDLY if the probe is empty rather than reporting NaN silently. A deterministic anchor-stratified split is a proposed follow-up (deferred to avoid a leak-introducing strata-plumbing change).
  - **(C9, practical) Cost.** Freezing arch+pair at s0 and skipping the single-DOF sweep on later rungs collapses the campaign from **1128 studies / 112,800 trials to ~183 / 18,300** (per-rung 108→3) under the "freeze after s0" strategy; the hybrid default sits between. Screening (reduced-budget screen of 28 pairs then full budget on finalists) is an optional s0-only further cut.
  - **DECISION (C8B/C9 ladder strategy) — CONFIRMED by user 2026-07-18: HYBRID.** `PAIR_SEARCH_STAGES = {s0_scour, s16_all, s21_scour4, s22_bearcrack4, s23_all4}`: s0 selects+freezes the pair; the L60 EOV rungs (s11–s16) reuse it for clean per-nuisance attribution; the fully-EOV rung and the L99.6 4-span scale-up RE-SEARCH (their per-rung winners are exploratory, they do NOT overwrite the frozen pair). Rationale: the L100 pilot showed the best pair can shift under heavy roughness (FrontBogie+Wheel1 beat the clean auto-pair by 17% MSE), so re-search where the premise genuinely changes; freeze where attribution matters. Alternatives considered & rejected: `{s0_scour}` (freeze everywhere, cheapest, strictest one-factor) or every rung (old R5 behaviour, breaks one-factor + expensive).
  - TESTS on this PC (no optuna/MATLAB here): both Python files byte-compile; `core.dataset` imports at r6; the 3-way split is leak-free + deterministic + seed-42 anchor coverage confirmed (bearing anchors [9,10,15,16] in test, healthy in train); `check_loader_provenance` **18/18 PASS**. TO RUN ON THE TRAINING PC before compute: `check_split_grouping`, a full ablation import/dry-run (needs optuna), and the MATLAB `smoke_stage3`/`smoke_geometry`/`checkcode`/`parfor_lint` (C5/C6/C7 touch A00). Rebuild the stage bundles (`build_stage_bundles.py`) so they carry r6.

- **🔴 EXTERNAL AUDIT — ROUND 7 IN PROGRESS 2026-07-18 (Codex re-review of R6; he ran MATLAB R2025b and caught a regression I introduced).** R6 was NEVER distributed (working-tree only), so R7 supersedes it; coordinated bump gen_schema→`audit-2026-07-18-r7`, loader mirror, `SCHEMA_TAG`→`gs4a20260718r7`, cache tag `_gs3`→`_gs4`. Codex confirmed the R6 fixes hold and agreed with my C6 correction. Fixes landed + tested this session:
  - **(P1) Optuna completeness.** `_study_is_finished` now = NO RUNNING/WAITING (in-flight) AND COMPLETE+PRUNED ≥ N_TRIALS (FAILs don't count) AND ≥1 COMPLETE — so 1C+99FAIL / +99RUNNING / +99WAITING are rejected while 1C+99PRUNED (the pruner working) is accepted. `pipeline.py` now RETRIES past FAILs to the useful budget (cap n_trials+20, so a broken config stops and is flagged, never loops). `_pick_champion_arch` requires the COMPLETE matrix for **every** architecture (an arch with zero studies can no longer win unopposed). The finished-check gates pair studies too.
  - **(P2) STRICT test-once.** `summarize()` now (1) builds the leaderboard from INNER-VAL only (`study.best_value`, no test opened), (2) validates completeness, (3) SELECTS + freezes the winner, (4) touches the outer TEST set ONCE — only for the frozen winner + pre-registered comparators (the frozen ladder pair + `EXTRA_PAIRS`, declared in config, never chosen from test), (5) reports those. `leaderboard_innerval*.csv` = selection; `leaderboard_test*.csv` = the held-set report. This makes "test reported for the frozen winner only" literally true (it was not in R6).
  - **(P3) Cache.** Atomic writes (temp + `os.replace`) for arrays/scaler/prov; SHA-256 digests of each cache artifact stored in prov and verified on reuse (a swapped `groups.npy` or a flipped feature byte → rebuild — TESTED); groups asserted canonical `0..n-1 × Npass` on load; `_GENERATION_COMPLETE` **content** (schema+fp) validated by the loader; source (filename,size) inventory retained. **NOT done (deliberate):** per-source-file SHA of the raw MAT bytes — that defends only against deliberate byte-tampering of one's own generated data (not the threat model for a trusted campaign; corruption changes size/structure and is already caught). Offered as optional.
  - **(P4) Loader payload.** Exact per-channel passage counts (wheel/pitch too, not just AcelPrimVag); full-`scour_vector` finiteness + range bounded to the **dano_max** ceiling (read from the manifest's `scour_dano_max_frac`), not merely [0,100]; contact-log lost-flags ∈ {0,1} and tension_frac ≥ 0; ragged crop > `_CROP_RAGGED_TOL`(4) is an error not a silent truncation; the legacy raw-`bearing_vector` fallback is dropped (r7 requires `bearing_fixity`). `check_loader_provenance` extended to **24/24 REJECT/accept PASS**.
  - **(P5) MATLAB — the real regression.** The R6 `save(...,'-struct',...)` I put inside the parfor is illegal (checkcode PFSV `SAVE cannot be called in a PARFOR loop without '-fromstruct'`). Replaced with a PER-PASSAGE hard gate that aborts at the FIRST bad passage (saving the rest of the state's compute) and writes a plain-**text** diagnostic via `fprintf` (no `save` in parfor). The gate is now `~isfinite(F) || F > 1000` (a bare `NaN > 1000` is false, so non-finite tension previously slipped through). Resume: the stale marker is deleted BEFORE the scan (survives an aborting scan); payload validation now checks wheel/pitch/contact counts == Npass and the file's OWN `data.gen_schema`/`gen_fingerprint`; marker written atomically.
  - **(P7) Cost — Codex right, my R6 number wrong.** With the HYBRID choice (5 search stages), the campaign is **522 studies / 52,200 trials** (s0 = 72 singletons + 84 pairs = 156; each of the 4 later SEARCH rungs = 84; each of the 5 FROZEN rungs = frozen pair + EXTRA_PAIRS × 3 seeds = 6). My "~183 / 18,300" was the FREEZE-EVERYWHERE figure. For reference: freeze-all = 210 studies, research-all = 912, old R5 (phase_1 at every rung) = 1128. A reduced-budget SCREEN of the 28 pairs at the 5 search stages (then full budget on finalists) is the lever if 52k trials is too much — the freeze rungs have nothing to screen.
  - **OPEN DECISIONS for R7 (asked user this session):** (i) **anchor stratification (P6)** — replace the random grouped split + threshold-based anchor ID + loud guard with deterministic coverage. Options: label-derived stratified split only (guarantees ≥1 scour-only & ≥1 bearing-only anchor per partition; no generator change; but the single healthy state still can't span all three), vs the full Codex proposal (generate MULTIPLE independent healthy + explicit `state_family`/`split_role`, guaranteed coverage — bigger, changes n_states + generation cost). (ii) **arch re-selection (the science question)** — keep the arch frozen from s0 across the ladder (then the paper must say "best pair CONDITIONAL on the s0-selected architecture") vs add a final deployment-selection stage that re-compares architectures + pairs on train/inner-val at the all-damage rung and tests the frozen combo once.
  - TESTS this PC (no optuna/MATLAB): all 4 `.py` compile; `core.dataset` imports at r7; split leak-free+deterministic+seed-42 anchors OK; `check_loader_provenance` 24/24; a new cache-hardening test PASS (atomic + digest catches tampered groups & feature bytes). ON TRAINING PC: `check_split_grouping`, ablation dry-run, MATLAB `checkcode` (expect **no PFSV**), `parfor_lint`, `smoke_audit`/`smoke_geometry`/`smoke_stage3`. Bundles stay R5 until Codex re-verifies R7 — do NOT rebuild yet.

- **🔴 ROUND 7.1 IN PROGRESS 2026-07-18 (Codex re-review of R7; he ran MATLAB, confirmed PFSV=0 + smoke_audit + loader 24/24, then found more blockers and a FACTUAL error in my Feature A premise).** Fixed + tested here: loader now REQUIRES the RAW format (rejects files without DimSpace/DimAcel/crop_start/crop_end — the previous "valid" fixture never even exercised the raw path) and mandatory dano_max ∈ (0,1], full-vector scour/fixity range checks, tension_frac ∈ [0,1], all-channels-present, crop `1<=start<=end<=DimSpace` (**check_loader_provenance 30/30**); selection completeness (no singleton fallback — `selected_pair=None` at a search stage is fatal; the driver refuses a pairless champion; winner + every comparator must have the full seed set on test; inner-val kept full-precision for selection); Optuna retry is now an in-run loop (auto-retries past FAILs to the useful budget, capped); manifest hardening (mandatory fields, `champion_pair` two DISTINCT DOFs, `desvio` in the study name); cache hardening (unique temp names fix a concurrent-build crash, the scaler is now hashed, the marker CONTENT is validated on the fast path, dano_max + a case_info digest are in the cache provenance — **new cache test 10/10**); `check_split_grouping` cache tag `_gs3`→`_gs4`.
  - **CORRECTION to my own R7 claim (Codex right, I was wrong):** crack, FRA profile, and track damage are **PER-STATE** persistent conditions (A00_Run.m:182 `crack_draw='per_state'`, :223 profile per-state, :247 track per-state, held across a state's passages), NOT per-passage as I stated. Only wheel-OOR / vehicle / speed / temperature are per-passage. Consequence: **Feature A cannot derive families from `y` alone** — a zero-head state may still carry a crack/profile/track fingerprint (that is `target_healthy`, not healthy). Feature A must stratify on support/side × severity bin × persistent-nuisance state, include nuisance-only anchors, and persist a fingerprinted `split_manifest`; and healthy needs ~12–15 independent `target_healthy` states (50 correlated passages ≠ 50 states), not 6.
  - **REMAINING R7.1 (next pass):** source per-state SHA-256 + root digest (A00 writes, loader verifies — I CONCEDED my "adversarial-only" pushback was wrong: bit-flip / disk-transport corruption / accidental overwrite routinely preserve file size, and a hash pass costs seconds vs days of generation); a single unified `protocol_hash` (dataset fingerprint + split + seeds/trials/epochs/pruner/search-space/full-noise/targets/preproc+model versions) validated across studies, manifests and outputs; MATLAB resume FULL validation (shapes + finiteness + ranges + labels equal to `DamageStates(dc_idx,:)`); per-cache-stem lock. **Feature B redesign:** deployment rungs run 3 arch × 28 pairs × 3 seeds = 252 studies each (hybrid total ~858, or ~702 if s21/s22 are frozen), comparators keyed by `(architecture, pair)` not DOFs, separate s16/s23 manifests, state-level bootstrap CIs; the claim is "best among **these 3 architectures × 28 two-sensor pairs**, at this budget and geometry" — NOT best sensor COUNT.

- **🔧 ROUND 7.2 — 2026-07-19 (internal continuation; Codex retired — external credits exhausted, so the audit role moves to in-house adversarial review + persisted check scripts).** Items 1–2 of the agreed R7.1 close-out queue landed this session:
  - **(1) UNIFIED `protocol_hash` — DONE.** New `core/protocol.py`: SHA-256 over a canonical-JSON descriptor of the WHOLE protocol. **Two hashes by design:** `protocol_core_hash` (dataset/stage-independent: split policy+seed+fractions, SEEDS **as an ordered list** (SEEDS[0] is the parity representative), N_TRIALS, EPOCHS, batch/patience/optimizer/scheduler, full Optuna sampler+pruner config incl. retry slack, the complete hyperparameter search space, full SENSOR_NOISE dict, architecture arm definitions incl. flags, EXTRA_PAIRS, PAIR_SEARCH/ARCH_SELECTION stage sets, SCHEMA_TAG + expected gen_schema + cache tag) and the full `protocol_hash` (core + stage + dataset name/targets + gen_fingerprint + **source root digest**). Rationale for the split: the ladder deliberately carries the s0 champion across rungs whose *dataset* differs, so the cross-rung validity condition is "same **selection protocol**" (core hash must match — hard SystemExit with a knob-level `descriptor_diff` if not), while the full hash is the per-rung identity baked into every study name (`_cfg_suffix` → `_ph-<12hex>`, replacing the weaker `_sc-` tag) and the summary dir (`results/<stage>_summary_ph-<hash>/` — previously UNTAGGED, a silent-overwrite hole). Also stamped into the champion manifest (name + fields), `frozen_selection.json`, `DT_metadata.json`, and a full `protocol_descriptor.json` written at run start (crashed runs still leave an auditable record; library versions recorded there but EXCLUDED from the hash — the campaign PCs differ).
  - **Anti-drift architecture (the R5 sprintf lesson applied to Python):** nothing in the descriptor is a hand-written description. The split/preproc constants moved to `core.dataset` module constants the split/cache code itself reads; the Optuna sampler+pruner are now BUILT from `core.protocol.OPTUNA_PROTOCOL`; the search space is a declarative `trainer.SEARCH_SPACE` dict that `_suggest_params` interprets (call order preserved exactly for seeded-sampler reproducibility). New persisted `check_protocol_hash.py` (**39 checks, ALL PASS**): canonical-JSON determinism (key/set order irrelevant, list order significant, alien types rejected), an exhaustive 54-combination proof that `_suggest_params` and `SEARCH_SPACE` agree call-for-call, per-knob core-hash sensitivity (16 knobs incl. monkeypatched split constant + pruner value), full-vs-core separation (dataset regeneration/stage change moves full, NOT core), 7 dataset-provenance hard-fail cases, and the diff naming the exact changed leaf. Computing the hash at driver import also means the ablation now refuses to START without a complete, marker-verified, digest-consistent dataset (previously it failed only at first load).
  - **(2) MATLAB A00 resume FULL payload validation — DONE** (untestable locally, needs `checkcode` + a resume smoke on the training PC). The resume scan (already loading each present file's `data`) now additionally verifies, per file: `contact_log` EXACTLY Npass×4, all-finite, and within the SAME tension gate as generation (col 4 ≤ `F_CONTACT_TOL_N`, hoisted to top level so the two gates share one constant — it was defined inside the parfor, i.e. duplicated by value); labels **bit-equal to this run's rows** — `scour_vector == DamageStates(dc_idx,:)`, `bearing_fixity == BearingFixity(dc_idx,:)`, `bearing_vector == BearingStates(dc_idx,:)`, `scour_supports` equal (valid because the fingerprint match already guarantees the config matrices are bit-identical; `isequal` is NaN-false so non-finite labels abort too); range defence-in-depth (scour ∈ [0, dano_max], fixity ∈ [0,1)); all six RAW fields finite 1×Npass with integral dims/crops and `1 ≤ crop_start ≤ crop_end ≤ DimSpace` per passage; every signal cell a finite 2-D matrix with consistent row count (≥ the loader's channel needs: 3/2/3) and time axis == `DimAcel(j)`. Threat closed: a stamped-but-garbage or renamed-NNNN state file could previously survive every resume forever (provenance matched, so it was never regenerated) and only explode — unrepairably — at Python load time on another PC.
  - **Bundle fix caught in passing:** `build_stage_bundles.py` takes its file list from the OLD source zip, so new files must be added to `EXTRA_FILES` — without this the bundles would have shipped WITHOUT `core/protocol.py` and the driver would crash on import on every PC. Added `core/protocol.py`, `check_protocol_hash.py`, and (missing since last session) `check_cache_provenance.py`; bundle README updated (protocol-hash note + full check-script list). Bundles themselves NOT rebuilt yet — that stays the single final step at the r8 promotion.
  - TESTS this PC: `check_protocol_hash` **ALL PASS** (39); `check_loader_provenance` **38/38**; `check_cache_provenance` **13/13**; `check_split_grouping` PASS; `parfor_lint` clean (A00 parfor body untouched by the resume edits); all edited `.py` compile. Still queued: Feature A redesign (state families + stratified split), Feature B ((arch,pair) deployment selection), then the single r8 promotion + commit + bundle rebuild.
  - **(3) FEATURE A — state families + stratified split — DONE** (same session). The user-approved FULL-FAMILIES design, correcting my per-passage premise error (crack/profile/track are PER-STATE, so `y` alone cannot identify families):
    - **A00 generates identity, Python never infers it.** Anchor construction now builds five explicit families row-aligned with `DamageStates`: **12 `target_healthy`** (were: 1 — 50 correlated passages of one state are not 50 healthy states), **`scour_only`** per pier (4 levels × **2 replicas**), **`bearing_only`** per abutment (4 × 2, bearing rungs), **6 `nuisance_only`** (crack rungs), **250 `joint`** LHS. `StateFamily`/`AnchorTarget`/`AnchorLevel` ship in `damage_states.mat`, in every NNNN.mat (`data.state_family`), in the fingerprint, and in `case_info` counts; the resume scan and the Python loader both cross-check per-file family against the table (a renamed NNNN.mat is now caught three independent ways).
    - **Crack activation PRE-DRAWN outside the parfor** (`CrackOn`, own seeded stream): forced OFF for `target_healthy`/`scour_only`/`bearing_only` (controlled probes), ON for `nuisance_only` (the false-positive probe), natural p=0.25 for `joint`. Rationale for crack being the ONLY controlled nuisance: the FRA profile class is fixed at 4 (only its phase varies) and track damage is present w.p. ≈1 under its Poisson rates — neither has an informative on/off stratum. Location/intensity still come from the per-state stream.
    - **Split**: `canonical_grouped_splits` is now a **deterministic family-STRATIFIED** split (no GroupShuffleSplit): strata = family | (family, anchor target) | (joint, crack flag, severity tercile); within-stratum seeded permutation (SHA-derived per-stratum seeds — `hash()` is process-salted and must never be used); fixed assignment pattern `[train, test, val, train, train]` = exact 60/20/20 with singletons→train. **Guarantee replaces the R6 C8C warning**: every family and every (family, target) anchor stratum spans all three partitions — asserted at split time AND in `check_split_grouping`. Anchor severity LEVELS are deliberately NOT strata (with 2 replicas a per-level stratum could never reach inner-val); which level landed where is pre-registered in the manifest.
    - **`split_manifest.json`** persisted next to the data (atomic), binding the dataset `gen_fingerprint` + policy + per-state family/stratum/assignment; every later call recomputes and must match byte-for-byte (canonical JSON) — catching nondeterminism, foreign files, or a policy change without a schema bump. A same-stratum row swap in a tampered `damage_states.mat` is caught because the manifest also records the family list itself.
    - **Grouped mode now REQUIRES `dataset_name`** (threaded through trainer / robustness / confusion / driver / cache scaler-fit) — there is no silent un-stratified fallback; legacy classification (`groups=None`) is bit-compatible.
    - **Disentanglement by family identity**: the driver probe drops the S_THR/B_THR label thresholds; it now selects outer-test samples by family — false scour on `bearing_only`, false bearing on `scour_only`, both on `nuisance_only` (crack-only invariance) and on `target_healthy` (clean baseline). An empty probe for a family that exists in the dataset is now a RuntimeError (split-guarantee violation), not a warning.
    - **Ladder counts changed** (`_LADDER` + A00 + README): s0 278, s11 294, s12 284, s13–s16 300, s21 286, s22/s23 308.
    - TESTS: `check_split_grouping` REWRITTEN (18 checks incl. coverage guarantees, manifest write/verify/tamper, no-dataset_name rejection, legacy bit-compat) ALL PASS; `check_loader_provenance` extended to **41/41** (missing table / missing per-file family / family-table mismatch all rejected); `check_cache_provenance` 13/13 with the family fixture; protocol hash covers the new split policy automatically (split_protocol() gained the stratification fields).
  - **(4) FEATURE B — deployment selection at s16_all + s23_all4 — DONE** (same session; user-approved, Codex-concept):
    - **`DEPLOYMENT_SELECTION_STAGES = {s16_all, s23_all4}`**: these rungs run the pair sweep over ALL THREE architecture arms (3 × 28 × 3 seeds = **252 studies each**; hybrid campaign total ≈ **858**). Why: the s0 architecture was selected on clean scour-only data — right for the one-factor ladder, but a DEPLOYMENT recommendation must not be conditioned on a clean-data winner (the L100 pilot showed heavy roughness reorders arms). Asserted disjoint from ARCH_SELECTION_STAGES: the deployment winner can never overwrite the ladder champion.
    - **Comparators keyed `(architecture, pair)`** everywhere (`_preflight_comparators`, `_assert_reported_complete`, the reported-loop filter, frozen_selection.json) — at single-arch rungs identical to the old pair keying; at deployment rungs it distinguishes the same pair under different arms. Pre-registered set = winner combo + the carried `(s0 champion arch, frozen pair)` continuity point + EXTRA_PAIRS on the carried arm. `_assert_pair_matrix_complete` now requires 28 pairs × EVERY arm running at the stage (a 3-arm rung with 84 mixed rows fails), plus per-arm completeness.
    - **State-level bootstrap 95% CI** on every reported outer-test row (`_state_bootstrap_ci`): resample whole STATES (the independent unit — passages share the label and persistent-nuisance realization, so a per-passage bootstrap would understate variance; equal Npass per state makes the cluster bootstrap exact), B=2000, seeded/deterministic. Columns `agg_mse_ci95_lo/hi` in leaderboard_test.
    - **Separate deployment manifest** `results/_deployment_selection_<stage>_<schema>_ph-<hash>.json`: exploratory winner + CIs + carried-champion reference + the EXACT claim string: "best among these 3 architectures × 28 two-sensor pairs, at this trial budget, seed count and geometry — NOT a best-sensor-COUNT claim and NOT a global optimum". No later rung reads it.
    - Protocol: `deployment_selection_stages` + the bootstrap policy entered the CORE descriptor (build_protocol_descriptors gained both, with back-compatible defaults); `check_protocol_hash` extended (+2 knob-sensitivity checks) — **ALL PASS**.
  - **(5) R8 PROMOTION — DONE** (same session). The external multi-agent review was blocked by the monthly spend limit, so the audit role was filled by an INLINE 30-checkpoint adversarial self-review + NEW machine validation now possible on this PC (MATLAB R2025b found locally; Python 3.13 turns out to HAVE optuna — the "no optuna here" note was stale):
    - **MATLAB validation (first time locally):** `checkcode('A00_Run.m')` = 41 messages, ALL benign pre-existing infos (dead else-arms of hardcoded toggles, broadcast-variable perf notes) — **0 syntax / 0 undefined-variable / 0 PFSV**; `smoke_audit` ALL PASS (LHS corr −0.015, PSD ratios ≡1, jitter 0 — and its check [3] parses A00 live, proving the config section is still machine-readable post-edit).
    - **NEW `smoke_familytable.m` + `check_familytable_roundtrip.py`** (persisted, in bundles): the Feature-A MATLAB↔Python contract proven with REAL MATLAB `save` artifacts — cellstr/logical family arrays round-trip exactly; the resume-scan isequal/strcmp checks hold after genuine save/load (no false-positive aborts on legitimate resumes); `jsonencode` of the cellstr+logical fingerprint fields is deterministic; and `core.dataset.read_state_table` parses the GENUINE MATLAB cell encoding (the other check fixtures use scipy-written cells, which encode differently — this was the identified fixture-vs-reality gap, now closed).
    - **NEW driver import smoke (real optuna, py3.13):** fixture r8 dataset → `import comprehensive_ablation_multidamage` exercises the whole import-time path — protocol descriptors assemble, hashes propagate into `_cfg_suffix`/`SUMMARY_DIR`/`CHAMPION_MANIFEST`/`make_config`, Feature-B asserts hold, pipeline builds its sampler/pruner from OPTUNA_PROTOCOL.
    - **Self-review findings:** 1 real defect fixed — the DEPRECATED `crack_draw='per_passage'` would silently ignore the forced `CrackOn` of `nuisance_only` states (family semantics broken); A00 now hard-errors on that combination. Everything else verified clean (30 checkpoints: old-API leftovers, bootstrap equal-Npass assumption (guaranteed by the loader's exact-count gate), disentanglement at no-crack rungs (NaN by design, not error), RNG-stream ordering of the CrackOn pre-draw (seeded after all existing draws), comparator keying at every stage type, MAX_PATH (new `_ph-` suffix is 2 chars SHORTER than the old `_sc-` tag), fp_cfg jsonencode types, cache↔split↔table staleness chains).
    - **Coordinated R8 bump:** gen_schema `audit-2026-07-19-r8` (A00 + `_EXPECTED_GEN_SCHEMA`), `SCHEMA_TAG` `gs5a20260719r8`, cache tag `_gs4`→`_gs5`. FULL suite re-run at r8: check_protocol_hash / check_loader_provenance (41) / check_cache_provenance (13) / check_split_grouping (18) / check_familytable_roundtrip / parfor_lint / checkcode / smoke_audit / driver-import smoke — **ALL PASS**. Single clean commit + all 10 bundles rebuilt from the working tree.
    - ~~Still pending on the training PC~~ **ALL REMAINING SMOKES RUN LOCALLY same day (user authorized; MATLAB R2025b + py3.13/optuna on this PC):**
      - `smoke_geometry` ALL PASS (contact clean on every config incl. L99.6 @3 speeds, pure compression −7.8e4 N, crop 9960); `smoke_stage3` ALL PASS (healthy parity, track damage on-deck in the global frame, zero tension).
      - **MICRO END-TO-END GENERATION** (new `make_micro_smoke.py`, committed): the REAL A00 text-patched to 17 states × 3 passages (3 healthy + 2 piers×2 levels×2 reps + 6 joint), full parfor run 14 min 52 s on 32 workers → 17/17 + digests + marker. **Real finding:** at Npass≤3 the LHS anti-transposition guard fires spuriously (|corr|≡1 with 2 points; 4-quadrant occupancy impossible with 3) — the guard is CORRECT at campaign Npass=50 (its design point, P(fail)<1e-5); the micro smoke bypasses it below Npass=10 and the incident is recorded so nobody ever "fixes" the guard the wrong way.
      - On the REAL generated dataset: `smoke_raw_parity`→`check_raw_parity` **PARITY PASS (2×10⁻¹³)**; Python loader accepts it (per-file SHA + family checks live), family table reads {healthy 3, scour_only 8, joint 6}, stratified split populates ALL THREE partitions with every family + writes/verifies `split_manifest.json`.
      - **RESUME exercises on real data:** (i) delete 0005.mat → rerun validates 16 files through the FULL payload scan, regenerates ONLY state 5, rewrites digests+marker; (ii) tamper 0003.mat's scour_vector with provenance strings intact → resume ABORTS with the exact "scour_vector ≠ DamageStates row" diagnostic (and the marker was already deleted pre-scan, so the folder cannot pose as complete). Restored + re-certified 17/17.
      - **ABLATION DRY-RUN with real optuna** (driver text-patched to N_TRIALS=2/EPOCHS=2/1 seed): the ENTIRE s0 pipeline executed — 24 single-DOF studies → champion arch → 28-pair sweep → strict summarize (52-model inner-val leaderboard, pair-matrix gate, winner on inner-val, outer test touched ONCE for winner + pre-registered comparator) → leaderboards/parity plot/`frozen_selection.json`/`protocol_descriptor.json` → champion manifest `_champion_arch_gs5a20260719r8_ph-<core12>.json` with both protocol hashes. Numbers meaningless at toy budget, by design.
      - Cost datum: ~180 s/passage on this PC → a 278-state × 50-passage rung ≈ multi-day per PC, consistent with the campaign plan.
      - GPR "FI>30" verification brief written for the user's Gemini deep-research pass: `docs/research_brief_gpr_fouling.md` (includes the decision rule for what to do with the answer).
  - **🔧 CONTACT GATE RECALIBRATED — 2026-07-19, first campaign dispatch.** The user's s23_all4 laptop run hard-aborted at **state 24 = the maximum-severity scour anchor** (60% at the middle pier, L99.6, + FRA-4 profile + track damage + poly OOR): **6.4 kN peak tension on 0.042% of the path** (`tension_frac_max 4.2e-4`). Diagnosis: NOT a physics regression — a brief wheel-unloading-past-zero tail event from superposing extreme scour with the severe EOVs, which the BILATERAL solver renders as small spurious tension instead of a few-ms separation + re-contact. The zero-tolerance gate (1000 N) had been calibrated on HEALTHY-bridge smokes (smoke_geometry tests no track damage/OOR/scour); the true regressions it exists for sit orders of magnitude higher (R3 profile-seam bug: 170 kN = 144% of static; flats: 12–38× the uplift threshold). Censoring/resampling the event would MNAR-bias exactly the severe states the paper studies (a rule refused twice already, R2 and R6).
    - **Fix: TWO-TIER gate, both sides identical.** TOLERATED + logged: peak tension ≤ **12 kN (10% of the ~118 kN static wheel load)** AND tension on ≤ **0.2% of path samples**. FATAL: beyond either bound, or non-finite. Applied to the A00 per-passage gate (+ per-state tolerated-tier note), the A00 resume validation, and the Python loader (`_CONTACT_F_TOL_N`=12e3 + new `_CONTACT_FRAC_TOL`=0.002); the loader now also prints a dataset-wide **micro-tension incidence summary** (count / worst F / worst frac) — the paper reports this as the scope of the bilateral-contact approximation on severe-EOV rungs. Both thresholds entered PREPROC_PROTOCOL → the protocol hash moves (no studies exist; nothing orphaned that matters).
    - **Resume compatibility (deliberate):** the tolerances are NOT in fp_cfg — a stricter-gate dataset is a strict subset of what the recalibrated gate accepts, so the user's partial s23 folder (states generated pre-patch all passed the stricter gate) RESUMES seamlessly under the patched A00. **Proven with real artifacts**: the patched generator resumed the pre-patch 17-state micro dataset 17/17-validated, fingerprint identical.
    - TESTS: `check_loader_provenance` extended to **43** (sustained-tension frac violation REJECTED; the real 6.4 kN / 4.2e-4 event ACCEPTED in the tolerated tier + summarised); all other suites re-run green; checkcode 41 benign / 0 blocking. Watch item: if a full rung aborts again on a materially larger event (≥ tens of kN or sustained), the tail is heavier than believed → revisit (unilateral-contact solver work vs censor-with-report), do NOT just raise the thresholds again.
  - **✅ GPR BALLAST ANCHOR RESOLVED — 2026-07-19 (user ran the NotebookLM verification; full Q&A + citations in `docs/research_brief_gpr_fouling.md`).** Decision = the middle path of the pre-registered rule: **λ = 1.2 patches/100 m KEPT (no protocol change, nothing regenerates)** — the Slideshare "FI>30 on 10–20% of route" is retired as non-citable, but our 15%-of-length sits inside a NEW citable envelope: Norway ~9.4% national defect-rate proxy (Husøy et al. 2024, Civil-Comp — a rate per inspection, order-of-magnitude only) → the only continuous regional-line GPR survey, 66% at Selig FI≥20 and **12% highly-fouled FI≥40** (Sadeghi et al. 2018, J. Appl. Geophys. — the strongest anchor, its 12% ≈ our 15%) → US heavy-haul "majority fouled" (FRA deck, corroboration only). **Threshold semantics corrected for the paper**: Selig's own "highly fouled" is FI≥40 (fouled 20–40); "FI>30" is the Zetica/FRA BFI schema boundary AND the functional drainage-loss limit (Chrismer & Hyslip, AREMA) — so our patches are worded as "ballast at/beyond the FI≈30 drainage-loss limit". Bonus outcomes: patch length U(5,20) m is now CITED (FRA empirical 1.5–33.5 m; Guo et al. 2023 GPR 2.4 m minimum feature) and the secondary-line vulnerability is citable (Network Rail 25 vs 50-yr ballast life, Musgrave 2024). The λ ∈ {0.6, 1.2, 2.4} sweep (7.5–30% of length) stays as the honesty mechanism and now spans the envelope's regional band. Applied to `A00_Run.m` comments (value untouched → fingerprint unchanged), `track_eov_sampling_spec.md`, `paper1_methodology.md`; bundles rebuilt for text sync. Pre-submission diligence noted: spot-verify the four load-bearing quotes against the PDFs; keep the peer-review flags (FRA + AREMA are official/industry, not peer-reviewed).

- **🔬 SUSPENSION-DAMAGE EOV — DEEP RESEARCH RECEIVED + RECIPE ADOPTED (pending implementation) 2026-07-22** (report `papers/Railway Suspension Degradation Simulation.{md,docx}` from `docs/deep_research_prompt_suspension_damage.md`; motivated by the 2026-07-21 methodology review: train-side damage = wheel OOR only, yet suspension faults alter the very suspension filtering that decides which channels see the bridge — could shift the sensor ranking). **Decision (user, in principle): ONE new isolated rung on L60; NOT blocking the current dispatch.**
  - **Adopted randomization recipe (A00 knobs, when the rung is built).** Per-passage independent Bernoulli per fault family (faults travel with the VEHICLE — different fleet member each passage, same policy as wheel OOR; report endorses this explicitly); affected bogie drawn at random (front–rear asymmetry is representable in 2-D and excites pitch — keep it). Families, sub-alarm multipliers, standing prevalence:
    (1) primary vertical damper fade: c_prim × U(0.6, 0.8) on ONE bogie, p = 0.05;
    (2) secondary vertical damper fade: c_sec × U(0.5, 0.7) at one bogie–carbody interface, p = 0.03;
    (3) primary spring (coil/chevron) degradation: k_prim × U(0.7, 0.9) on one bogie, p = 0.02;
    (4) air-spring surge-pipe orifice blockage (the insidious one — no static height change, invisible to pit inspection): c_sec × ~0.4 with a dynamic-stiffness rise, p = 0.02.
    Combined ⇒ ~11–12% of passages carry ≥1 anomaly. **EXCLUDED by design:** air-spring deflation→ERS landing (<1% prevalence AND immediately detected + speed-restricted — violates the undetected-nuisance premise; also its 3–4 Hz carbody shift is a detectable event, not fleet noise) and anti-yaw dampers + every left–right/roll mode (no roll DOF in 2-D; representing a one-sided failure as a halved axle property overstates bounce and suppresses roll — the report confirms the planar simplification is standard for vertical/bridge-interaction studies).
  - **The sub-alarm boundary is the design principle** (this is what makes these faults NUISANCES): EN 14363/UIC 518 ride-monitoring limits (carbody ~0.75–1.5 m/s² RMS windows, bogie ~7 m/s² RMS / 20 m/s² peak, 20 Hz LP simplified method) are NOT exceeded by primary damping losses ≤30–40%, secondary ≤~40–50%, spring loss ≤30% on decent (QN1/QN2) track — such faults plausibly ride undetected for months (30,000 km pit-inspection cadence; 600–750k-mile overhauls). Losses ≥70% alarm and remove the vehicle from service ⇒ outside the nuisance regime. The adopted multiplier ranges sit strictly inside the undetectable band.
  - **Spectral-overlap hypothesis to TEST at the rung** (report §signatures + our corrected decks): secondary-damper faults inflate the carbody 1–3 Hz band and primary faults the bogie 8–20 Hz band, bracketing our deck fundamentals (4.18 Hz L60 / 2.75 Hz L99.6); wheel channels live ≫50 Hz and should be immune. Prediction: secondary fade is the dangerous confounder for sprung channels — if the ranking shifts anywhere, it shifts there. Either outcome is a result (ladder logic).
  - **Literature gap CONFIRMED** (report's strongest strategic finding): drive-by/VSM studies systematically hold suspension parameters constant — vehicle uncertainty is modelled as mass/speed variation only; no published study randomizes in-service suspension DEGRADATION while estimating bridge damage. The rung is therefore a citable novelty claim for Paper 1, and the prevalence numbers double as the DT-Stage-D prior P(degraded vehicle) for observation quality.
  - **⚠ SOURCING FLAGS (this report is weaker-sourced than the profile/crack and track-prevalence ones — treat accordingly):** the prevalences (5/3/2/2/<1%) are explicitly INFERENCES from maintenance-latency reasoning over mixed-grade sources (a ResearchGate bogie failure-rate table, Rail Engineer, Scribd maintenance manuals, market reports) — adopt as order-of-magnitude and SWEEP (×0.5/×1/×2), same honesty mechanism as ballast λ. The 75/50/25/0 damper-fade grid convention + the sub-alarm boundary cite the SCIRP model/data-fusion paper and Polimi (Alfi/Fu/Bruni VSDIA 2018) — quote-check both before the paper. EN 14363 numeric limits: verify against the standard text. NOTE: the report's table encodes the multiplier ranges as equation IMAGES (lost in the .md export) — the ranges above were reconstructed from the prose; verify against the .docx table before coding A00 knobs.
  - **Rung design:** baseline = s13_bearcrack (bridge damages, clean rail/track/train) + suspension faults ONLY — an isolated parallel rung in the s14 style, so the suspension effect is attributed against s13 without roughness in the way; profile/track/OOR stay off. Optional later: toggle suspension into a refreshed s16/s23 for the everything-at-once deployment picture. Naming suggestion: `s14b_susp` (or `s17_susp` with its baseline documented). Requires: A00 fault-family draws + damage_config plumbing for per-bogie c/k multipliers (vehicle side — currently only healthy-range property randomization exists), new state-family counts, bundle rebuild — schedule AFTER the current campaign lands.
- **🔧 CONTACT GATE F-TIER RECALIBRATED (2nd time) — 2026-07-22, s15 dispatch.** The user's s15_track run aborted at **state 244 passage 33/50** (scour [0, 50.5%, 13.3%, 0] + per-state track damage): **F_tension_max 13.42 kN** vs the 12 kN F-tier, `tension_frac_max 6.33e-4` (3× BELOW the 0.002 sustained tier), `contactLost_track = 1` → **ONE sample at dt = 1 ms, on the TRACK portion (off-bridge)**. Diagnosis: same bilateral-solver micro-unloading class as the s23 calibration event — NOT a regression, and physically expected to be heavier here: the void rung is *designed* to produce ±70–85% contact-force transients (its unloading tail scales accordingly), and s16/s23 add poly OOR on top, so their tails will be heavier still. The pre-registered watch item ("tens of kN or sustained ⇒ revisit solver/censoring, don't just raise") is NOT triggered — 13.4 kN = 11.4% of the ~118 kN static load, 2.1× the first observed event, ultra-brief. **DECISION (user, from options 24 kN / 18 kN / escalate): F-tier 12 → 24 kN ≈ 20% of static** — same calibration recipe as 07-19 (≈2× worst observed, rounded to a static-load fraction), now with headroom for the OOR rungs to avoid a third abort-patch round-trip; still **7× below the known-regression scale** (R3 seam bug: 170 kN = 144% static). Frac tier (0.2%) and non-finite checks UNCHANGED — they are the real regression catchers (sustained tension is the R3 signature). Watch item RENEWED verbatim: tens of kN or sustained ⇒ solver work or censor-with-report, no third raise.
  - Implemented: `A00_Run.m` `F_CONTACT_TOL_N` 12000→24000 (+ both-events calibration history in the comment); `core/dataset.py` `_CONTACT_F_TOL_N` 12000→24000 (same); values flow into `PREPROC_PROTOCOL` → **protocol hash MOVES** (core + full). `check_loader_provenance` extended **43 → 45** (new: the REAL 13.4 kN/1-sample event ACCEPTED in the tolerated tier; 25 kN brief REJECTED — the tier moved, it didn't vanish). Suites re-run green: loader-provenance 45/45, protocol-hash suite ALL PASS, MATLAB checkcode 41 benign/0 blocking (baseline unchanged). All 10 bundles REBUILT from the tree and spot-verified (A00 + loader both carry 24000 inside the zips).
  - **Resume mechanics (proven pattern):** tolerances NOT in fp_cfg ⇒ every state generated under the stricter gate stays valid; the s15 PC extracts the new bundle over the existing folder and A00 resumes at state 244 (per-state RNG ⇒ identical draws; passage 33 now lands in the tolerated tier and is logged).
  - **⚠ COST — the hash move orphans Python studies (user confirmed s0 ablation started/done):** study names embed the protocol hash and the champion manifest embeds `ph-<core12>`, so old-hash s0 studies/champion are unreachable under the new tree (champion carry at s11+ would hard-fail by design). Datasets are NOT affected anywhere. Since the s0 dataset was fully generated+validated under the 12 kN gate, its data acceptance is bit-identical under 24 kN — a re-run reproduces the selection; the cost is pure compute. Recommended path: move ALL PCs to the recalibrated tree/bundles and re-run the s0 ablation under the new hash (a split-brain campaign with the old loader on some PCs would be rejected at s15+ anyway, and mixing hashes is exactly what the machinery forbids). Alternative if the finished-s0 compute proves too painful to repeat: a one-off VERIFIED migration (prove zero (12,24] kN events in s0's contact logs — true by construction since the old gate enforced ≤12 — then rename studies+manifest to the new hash with a persisted migration record). Deliberately NOT built unless needed.
  - Considered and DEFERRED: relocating the contact tolerances from PREPROC_PROTOCOL (core hash) to the per-dataset generation side, which would make future gate recalibrations orphan-free. Rejected for now: the relocation itself moves the core hash (same one-time cost), adds machinery mid-campaign, and the loader genuinely gates data acceptance — arguably protocol. Revisit only if a third recalibration ever becomes necessary (which the watch item forbids anyway).
- **🔴 EXTERNAL AUDIT ROUND 3 (Codex) — VERIFIED + FIXED 2026-07-22.** Codex delivered a "no-go" audit; per house rule every central claim was independently re-verified against the code BEFORE acting. **11 claims verified: 10 confirmed, plus documented overreaches.** User decision: fix everything, correctness over schedule; s15 (and any track rung) stopped for regeneration. This lands in the SAME hash-move window as the 24 kN recalibration, so the Python re-run cost was already sunk.
  - **CONFIRMED + FIXED (Python; all change feature bytes or selection → cache tag `_gs5`→`_gs6`, PREPROC/TRAIN protocol entries added, hash moves):**
    (1) **"PAA" was a linear-interpolation resampler**, not Keogh window means (`interp1d(kind='linear')`; [0,0,0,4]→[0,4] not [0,2]) — point-subsampling with NO anti-aliasing, contradicting the cited definition and under-filtering exactly the wheel channels. FIX: true PAA via exact integration of the sample-and-hold signal (fractional windows for non-divisible lengths; global mean preserved exactly; chunked float64). NEW `check_paa.py` (18 checks incl. brute-force overlap reference + the audit example) ALL PASS. `paa_impl` now in PREPROC_PROTOCOL.
    (2) **Noise not paired across sensor subsets**: one sequential RNG made a DOF's realization depend on its position in the loaded subset (same DOF: different noise alone vs in a pair) — realization variance leaked into config comparisons. FIX: RNG keyed `[NOISE_RNG_SEED, global_dof]` → subset-invariant, sensor-set comparisons noise-paired. `noise_pairing` in PREPROC_PROTOCOL. (The uniform `all_mult` 5% remains the deliberate symmetric stress model; per-channel additive datasheet floors stay the queued robustness arm.)
    (3) **Objective mismatch**: aggregate MSE pooled scour (0–60) with bearing (0–95) heads — selection could buy bearing accuracy with scour accuracy. FIX (pre-registered): `objective_value` = **SCOUR-head MSE** on bearing rungs (bearing trained + reported, never selected on); training loss = **range-normalized multi-task MSE** (`WeightedHeadMSE`, w∝1/range², normalized to mean 1; constants SCOUR_RANGE_PCT=60 / BEARING_RANGE_PCT=95). Both are executable in `TRAIN_PROTOCOL`; the protocol descriptor and `frozen_selection.json` embed that exact objective mapping rather than a duplicated prose string.
    (4) **Cache stem ignored N_SEGMENTS** (editing the constant would silently reuse stale-length caches): stem now carries `_seg<N>`.
    (5) **No pooling ablation existed** (all arms `use_nhits=True`, in Part I too) and the module is `MultiRatePooling1D` (N-HiTS-*inspired*), not full N-HiTS. FIX: 4th arm **`PAA_CNN`** (no multi-rate pooling → models.py native GAP path) = the pooling-ablation control; paper wording must say "multi-rate pooling", never claim N-HiTS (Sinha-rename precedent). Deployment rungs now 4 archs × 28 pairs × 3 seeds = 336 studies.
    (6) **No full-8 control** (the driver docstring literally said so; "2≈8" was only ever a Part-I claim). FIX: `CONTROL_SETS=[ALL_DOFS]` runs at EVERY rung as a **non-selectable** comparator (summarize() selects only among n_sensors==2; publish-gate refuses non-pairs) + is added to the pre-registered comparator set → the 2-vs-8 economy is measured per rung. In the protocol descriptor.
  - **CONFIRMED + FIXED (MATLAB; affect only track rungs):** (7) **hanging↔fouling coupling was 9:1, not the documented ×3** (A00 rejection weights compounded to mult²; the ballast-transition block next to it had the correct pattern) → fixed to acceptance odds 3:1. (8) **Overlapping ballast patches multiplied** their η (stacked draws could leave the documented per-patch band) → governing-patch rule: largest |log η_k| wins and supplies BOTH η_k and η_c (wet/dry coherence); B54 + Python mirror in lockstep. (9) **B66 on-track mask had no upper bound** (post-exit F≡0 samples inflated the frac denominator → sustained-tension gate read laxer than pre-registered) → mask now [0, Calc.Profile.L], both solver branches; applies forward, saved logs unaffected (all passed with >3× margin). **CONSEQUENCE: any s15/s16/s23 data generated before 2026-07-22 must be REGENERATED** (9:1 draw is distribution-wide); all other rungs' data stands. checkcode: A00 at the 41-benign baseline, B54/B66 0 findings; smoke_audit ALL PASS.
  - **Provenance hardening (Codex #8, subset):** missing best-trial weights now **FATAL** at study finish (was a WARNING); `RUN_TAG` joins SUMMARY_DIR (tagged re-runs no longer share/overwrite the untagged summary); cache provenance block now pins `file_digests.mat` + `damage_states.mat` SHAs (a digest-chain-consistent tamper invalidates the cache; residual accepted + recorded: an inconsistent same-size NNNN.mat edit is *ignored* while a cache exists — cache holds pre-edit bytes — and is caught at rebuild; full per-file re-hash on reuse = queued); bundle builder writes **`bundle_sha256.txt`** manifest.
  - **Codex overreaches, for the record:** "ladder isn't one-factor" (the DATA changes one factor per rung; per-rung re-tuning is part of the estimand — wording discipline only, keep "scenario/stress" framing available); "rename PAA or lose Fernandes comparability" (false dilemma — implementing true PAA fixes both); contact 24 kN "chosen to let the campaign continue" (half-right: post-hoc calibration acknowledged since 07-19, and the Δt-refinement study it proposes is ADOPTED as the closure — see queue); `.bib` "missing" (exists for the talk; paper bib pending as known).
  - **✅ QUEUED (recorded commitments, not yet implemented):** contact-closure study (rerun the two flagged states — s23#24, s15#244 — at Δt=1/0.5/0.25 ms, compare gates 0/12/24 kN, then freeze the criterion permanently); statistical-inference upgrades for finalists (repeated stratified grouped CV; hierarchical state→seed bootstrap; paired per-state contrasts; larger nuisance_only counts at the next full regen — 6 states leave ~1–2 in test); additive datasheet noise arm; full per-file SHA verify on cache reuse; A00-side digest coverage of damage_states.mat/case_info.mat; study.user_attr protocol dump; env lockfile; settlement/profile-dip as a separate EOV (current voids = stiffness removal, honestly documented — Codex wanted the geometric pathway too); FRA-4 wording conditioned per 49 CFR §213.9 (Class 3 is passenger-legal at 70–90 km/h and rougher — FRA-4 is a benchmark choice, not the legal roughest; fix in paper1_methodology purge); terminology sweep (support-stiffness loss / nominal fixity DOE / damaged-element crack / "all modeled vertical-pathway EOVs" / "selection under the simulated design distribution").
  - **🔧 FOLLOW-UP 2026-07-23 — `gen_rule_ver` added to the fingerprint (caught while planning the 3-PC dispatch; would have silently corrupted it).** The r3 MATLAB fixes changed generation RULES while leaving every knob VALUE untouched (`hang_foul_mult` is still 3.0; only the odds arithmetic changed) — so `gen_fingerprint` and `gen_schema` were both UNCHANGED across the fix. Consequence: extracting the new bundle over a pre-fix partial folder (e.g. the user's s15 stopped at state 244) would pass the resume guard and continue, MIXING 9:1-drawn states 1–243 with 3:1-drawn states 244+ in one dataset — exactly the silently-mixed-code-versions failure the R2 schema guard exists to prevent, re-entering through the value-vs-rule gap. FIX: `gen_rule_ver = 'r3-2026-07-22-hangodds3to1-patchgovern-b66bounded'` added to the fingerprint config (and to case_info for the human record) → the fingerprint now moves with BEHAVIOUR, so any pre-fix partial folder ABORTS on resume. **Rule going forward: bump `gen_rule_ver` whenever generation behaviour changes without a knob value changing.** Deliberately NOT a `gen_schema` bump: that is the global data contract and would invalidate every COMPLETE pre-fix dataset (s0/s11–s14/s21/s22 are provably unaffected in their SIGNALS — the track rules never fire without track EOV, and the B66 mask touches only the logged contact diagnostics, which passed with >3× margin). Complete datasets keep loading (the loader reads the stored fingerprint, never recomputes it); only RESUME of a partial folder is blocked. **Dispatch consequence: every not-yet-COMPLETE folder on every PC must restart in a fresh folder, regardless of rung**; complete datasets stand.
  - Checks after all fixes: py_compile all edited files OK; check_paa 18/18; check_loader_provenance 45/45; check_protocol_hash ALL PASS; check_cache_provenance ALL PASS; check_split_grouping ALL PASS; MATLAB smoke_audit ALL PASS; smoke_stage3/smoke_geometry rerun in progress; bundles rebuilt from the committed tree with SHA manifest (see commit).

- **EXTERNAL AUDIT ROUND 4 (Codex) — IMPLEMENTED 2026-07-25.** The executable
  result and honest publication gate are in `docs/audit_r4_results.md`; source
  checkpoint `f805fbe`. Supersedes the R3 “queued” status for finalist inference
  and provenance. Implemented: finalist-only 5-fold × 2-repeat state-grouped CV
  with fold-local scaling/frozen HPO; immutable outer-test firewall; top-5 +
  per-architecture finalists; state-first cross-seed bootstrap and aligned paired
  contrasts; state-first disentanglement; exact pair/seed/full-array guards;
  full source re-hash on cache reuse; canonical Optuna protocol records;
  weight/scaler/best-trial cryptographic linkage and idempotent export; exact
  Python/CUDA environment lock; nondeterministic PyTorch operations hard-fail;
  MATLAB R2025b gate; contact-refinement harness; tracked bundle source manifest.
  **Still empirical, not “fixed by code”:** execute s23#24 and s15#244 at
  1/0.5/0.25 ms; run the actual campaign; nuisance-only has only one outer state
  and remains exploratory; current multiplicative noise is a controlled stress
  model rather than a datasheet additive floor. The paper claim is continuous
  support-stiffness-loss estimation/localisation under the simulated distribution,
  not binary detection or global sensor/architecture optimality.

- **🔍 AUDIT R4 INDEPENDENTLY VERIFIED — 2026-07-25 (my review of Codex's `f805fbe`/`a91f2f2`/`c0ead72`; full record `docs/audit_r5_handoff.md`).** Method: green suites treated as UNPROVEN until each guard was shown to fail. Ran all 12 Python checks + the MATLAB battery, then a **mutation harness** (re-inject each original defect byte-for-byte, run its guard, require non-zero exit, restore from original bytes with a byte-identity assert). **Result 5/6 caught → one blind spot → fixed → 6/6.**
  - **BLIND SPOT (substantive, FIXED): the pre-registered primary estimand was unguarded.** Reverting `objective_value` to `return metrics["mse"]` — selection silently seeing the bearing heads again, undoing the r3 pre-registration — passed the ENTIRE suite including `check_protocol_hash`. Root cause: `TRAIN_PROTOCOL["objective"]` only DESCRIBES the estimand in prose; nothing bound description to implementation. Contrast the two mechanisms that do work: `batch_size`/`patience` are READ FROM the protocol by the trainer (cannot drift), and the loss is asserted behaviourally by `check_weighted_head_mse`. The objective had neither — the same declared-vs-actual gap R7.1 closed for the search space, never extended to the objective. FIX: five behavioural assertions in `check_campaign_controls.py` (scour-primary with bearing heads; aggregate NOT returned there; correct fallback without bearing heads; scour improvement outranks bearing improvement; TRAIN_PROTOCOL prose still declares scour-primary so re-registration moves the hash). **Open question handed to r5:** whether the stronger fix is to DERIVE `objective_value` from `TRAIN_PROTOCOL` (the batch_size pattern) so divergence is impossible by construction — I chose the behavioural guard because it does not move the protocol hash.
  - **MINOR (FIXED, THEN MADE FAIL-CLOSED IN R5): `check_b54_overlap_parity` could silently degrade.** With MATLAB absent from PATH it printed `[SKIP]` then `ALL PASS`, so a dispatch-PC preflight log would assert cross-language parity that never ran. R4 first made the degraded scope explicit; R5 additionally makes the normal command exit non-zero unless the MATLAB half actually runs. `--allow-python-only` is an explicit development-only escape hatch and is not a valid dispatch preflight.
  - **MINOR (documentation, CLOSED IN R5): intentional breaking change.** `digital_twin/assets.py` routes `DigitalAsset.__init__` through `verify_standalone_dt_package`, which RAISES on any package lacking the new provenance fields — every pre-R4 DT package is now unloadable. This is correct because the pre-mass-fix packages are invalid and provenance cannot be backfilled by assertion. The compatibility break and scope boundary are now explicit in `docs/audit_r4_results.md`. The broader DT layer still awaits its own audit pass.
  - **STRUCTURAL PROPERTIES I TRIED TO BREAK AND COULD NOT:** (1) **Cross-language B54 parity is genuine, not a shared-bug trap** — the extracted `B54_TrackVectors.m` helper is tested against an INDEPENDENT hardcoded oracle in `check_b54_overlap_parity.py`, and mutating the MATLAB helper ALONE (governing rule → last-wins) turns the check RED via row-order invariance while the Python mirror stays correct. (2) **Test-once is enforced at runtime**, not by convention: finalist CV receives `development_idx` only and the driver raises if `development_idx ∩ outer_idx` is non-empty. (3) `batch_size`/`patience` genuinely protocol-bound (verified they are read from `TRAIN_PROTOCOL`, not re-hardcoded).
  - Evidence at this review: 12/12 Python checks PASS; MATLAB `smoke_audit`, `smoke_b54_overlap_parity`, `smoke_contact_closure`, `smoke_stage3`, `smoke_geometry` PASS; working tree byte-identical after every mutation. Codex's own R4 self-report checked out on every claim I tested.

- **AUDIT R5 — GENERATION IDENTITY CONVERGED 2026-07-25.** Because the user
  elected to discard and regenerate the entire campaign, the generator contract
  is bumped globally to `audit-2026-07-25-r9` (MATLAB + Python loader), and the
  study/artifact tag moves to `gs6a20260725r9`. The feature transform is
  unchanged, so `CACHE_SCHEMA_TAG` correctly remains `_gs6`; cache provenance
  already compares the source schema/fingerprint/root and therefore rebuilds
  rather than reusing an R8 source. The overlapping manual rule markers
  `gen_rule_ver` (unconditional) and `track_eov_impl` (conditional) are replaced
  by exactly one unconditional `generation_behavior_version`, stored once in
  the canonical fingerprint config and once in `case_info` for inspection.
  MATLAB release provenance remains a separate environment field, not a second
  rule-version key. Rule-only code changes must bump the behavior version;
  changes to the global data contract bump `gen_schema`. New
  `check_generation_contract.py` binds the MATLAB schema, Python expectation,
  study tag, fingerprint field and manifest field, bans both legacy keys, and
  proves sensitivity with seven in-memory mutations. No generated data, result
  or bundle was modified; all current bundle ZIPs remain stale.

- **AUDIT R5 — EXECUTABLE TRAINING PROTOCOL 2026-07-25.** The stronger answer
  to the F1 design question is derivation *plus* behavioural guards, and the
  protocol hash is intentionally allowed to move because no valid ablation has
  begun. `TRAIN_PROTOCOL["objective"]` is now a structured mapping consumed by
  `task.objective_value`: bearing-head regressions select `scour_mse`; all
  other tasks select `mse`; missing metrics or malformed policies fail closed.
  The same declared-vs-executed gap also existed for loss, optimiser,
  scheduler, trial-seed source and determinism, so each is now executable
  protocol data consumed by the production factories/call sites. The Optuna
  seed is mandatory (`config["seed"]`); the silent default to 42 is removed.
  Protocol descriptor schema v3 records the exact executable objective mapping
  in `selection.selection_metric`; `frozen_selection.json` records the same
  mapping, eliminating the former duplicated `scour-primary` prose contract.
  The canonical determinism mapping is shared by `core.utils` and
  `TRAIN_PROTOCOL`, including cuDNN, hard-fail deterministic algorithms and
  the cuBLAS workspace contract. Behavioural and AST call-site guards prove
  both policy sensitivity and end-to-end consumption.

- **AUDIT R5 — MUTATION AND COMPUTE GATES 2026-07-25.** The R4 statistical,
  artifact and environment machinery is tested by an isolated 12-mutant
  harness (`check_r4_mutation_guards.py`), while
  `check_training_policy_mutation_guards.py` targets the executable objective,
  loss, optimiser, scheduler, trial-seed and determinism bindings. Each
  baseline must be green, every injected defect must turn the intended guard
  red, each temporary target is restored byte-for-byte, and the real tree is
  hash-checked unchanged. A separate compute-only benchmark
  (`benchmark_r5_compute.py`) runs the real production Optuna `Objective` for
  100 useful trials and one refit through the exact shared finalist-fold
  implementation; `check_benchmark_contract.py` is its lightweight, non-GPU
  contract guard and belongs to the dispatch preflight suite. The benchmark's
  legacy cache is immutable workload material only; it writes no scientific
  performance values (including suppression/restoration of Optuna's INFO-level
  trial-value logging), uses a `BENCHMARK_ONLY_DO_NOT_PUBLISH__` study name,
  requires a clean committed tree, records source/fixture/environment/hardware
  hashes and timing, and confines all outputs to `.audit_tmp`. HPO timing is
  cumulative across explicitly acknowledged restarts; after an abrupt stop its
  heartbeat is an honest lower bound at a nominal five-second cadence, not a
  strict five-second tail guarantee. Trial timestamps are converted from
  Optuna's naive local datetimes to timezone-aware UTC before publication.
  HPO completion requires a matching completed heartbeat, two concurrent stale
  recoverers are serialized by a crash-released OS mutex, and contradictory or
  missing timing evidence is never fabricated. The finalist has at-least-once execution
  semantics and exactly one *durably accepted* completion: a published marker
  repairs a torn attempt pointer without retraining, while an unmarked prior
  attempt may have physically completed and is reported as unaccepted. The
  final summary hashes every HPO/finalist artifact it announces, points to an
  immutable logical SQLite backup (rather than unstable WAL/SHM files), and
  supports only explicit, evidence-preserving repair of a torn completion
  pointer. Its durable marker has an exact, type-checked field schema and is
  validated on first publication as well as resume. The genuine-MATLAB family
  table and raw-transform parity checkers are fail-closed: missing artifacts,
  non-finite differences, or tolerance failures return non-zero. The mutation
  campaigns and heavy benchmark are audit-only, not
  per-PC preflights.
  Commit-bound evidence in `docs/audit_r5_results.md` must authorize dispatch
  before bundles are rebuilt.

- **✅ r9 GENERATION PATH RE-VALIDATED — 2026-07-25, at commit `0db3bf2`.** Gap found while planning the idle-week work: `smoke_stage3` and `smoke_geometry` (the two heavy full-solve smokes that validate generation physics end to end) had last run on the **r4** tree, BEFORE Codex's r5 `gen_schema` bump to `audit-2026-07-25-r9` and the `generation_behavior_version` consolidation in A00. Re-ran both at r9: **SMOKE STAGE-3 ALL PASS** (healthy byte parity, damaged path, track damage placed on the real deck in the global frame, compressive contact) and **SMOKE GEOMETRY ALL PASS** (all six fixed/FRA × L60/L99.6 × 70/80/90 km/h configs; live bridge length, full-deck crop, finite full-track profile, no tension). Together with checkcode A00 = 41 baseline, `smoke_audit`, `smoke_b54_overlap_parity` and `smoke_familytable` at the same commit, **the r9 generation path is validated to the same standard the r4 tree was** — the r5 A00 delta was provenance-only (schema string + one version key), not physics. This is evidence FOR the pending R5 dispatch authorization, not a substitute for it: `docs/audit_r5_results.md` still reads DISPATCH BLOCKED and the bundle builder still refuses to write ZIPs.

- **🔓 MATLAB RELEASE GATE WIDENED TO A VALIDATED ALLOWLIST — 2026-07-25 (hardware constraint: the university has no licence to install R2025b on the campaign PCs).** Situation: the only R2025b machine is the user's personal laptop (i9-14900HX, 24 cores — actually the strongest generator available), but tying it up for the ~2–3.5 weeks a full 10-rung campaign needs is not acceptable. The two campaign PCs run **R2023b and R2023a** (Python 3.10, easily re-pinned; MATLAB, not). The r4 gate hard-required R2025b, which would have forced central generation on the laptop.
  - **The reframe:** the gate exists to stop **mixed** releases inside one campaign, not to bless one version. A campaign generated entirely on ONE validated release is homogeneous whichever release that is — and critically, the two PCs do not need to match R2025b, **they need to match each other**. Compatibility pre-check found no language barrier: the only modern constructs are `string()` (R2016b) in A00 and `arguments` blocks (R2019b) in the post-campaign `contact_closure_study.m`.
  - **IMPLEMENTED:** `validated_matlab_releases = {'2025b'}` allowlist in A00 with the qualification protocol documented inline — a release is added only after BOTH (1) the MATLAB smoke suite passes ON IT (physics), and (2) **generation identity** vs an already-validated release is demonstrated. New **`compare_generation_releases.py`** does (2): compares manifests + `gen_fingerprint` (refuses if the CONFIG differs, not just the release), requires damage-state labels to be EXACTLY equal, then per-file SHA-256, falling back to a max-relative-difference sweep over the RAW signal fields. Verdicts: **BIT-IDENTICAL** (qualifies) / **NUMERICALLY-EQUIVALENT** (≤1e-10 rel; qualifies only if explicitly accepted and recorded here) / **MATERIALLY-DIFFERENT** (never mix). Why (2) is mandatory rather than assumed: Statistics Toolbox samplers (`poissrnd`/`wblrnd`/`lhsdesign`) may change algorithm between releases, and BLAS/LAPACK drift can accumulate over ~8.7k Newmark steps — labels differing would mean a *different dataset*, not a rounding difference.
  - **Escape hatch, deliberately marked:** `A00_RELEASE_QUALIFICATION=1` permits an unvalidated release so a machine can run the smokes/micro-smoke needed to qualify it; the run prints a stderr banner and sets `case_info.release_qualification_run = true`, so its output can never be mistaken for campaign data. VERIFIED both branches by mutation (allowlist temporarily set to exclude the running release): without the flag A00 ABORTS with the validated-release error; with it, the qualification banner is emitted and the gate is passed. A00 restored byte-for-byte; checkcode still 41 (baseline).
  - **▶️ NEXT (user, on each campaign PC):** run the MATLAB smoke suite, then `make_micro_smoke.py` under the qualification flag, then `compare_generation_releases.py <2023a-dir> <2023b-dir>`. If BIT-IDENTICAL → add both releases to the allowlist and generate the campaign across both PCs (laptop free). If not → generate the whole campaign on ONE of them. **Proposed for round 6 (NOT implemented):** the s11+ champion-carry check should also compare `case_info.matlab_release` against the release recorded in the s0 champion manifest, so cross-rung release homogeneity is enforced rather than merely auditable.
  - **Npass as the preferred cost lever (user rejected cutting rungs, correctly):** cutting ladder rungs attacks the paper's one-factor attribution contribution; `Npass` does not. Inference is state-first (the bootstrap resamples STATES; passages are pseudo-replicates), so 50 → 30 cuts generation ~40 % while leaving the independent-unit count untouched. Honest cost: training samples per rung fall 15k → 9k, which is why 30 rather than 25. Do NOT cut states — they are the inferential unit and `nuisance_only=6` is already flagged as too thin.

- **📊 R5 COMPUTE BENCHMARK RUN — 2026-07-26, contract-valid at commit `a0793a1` (evidence: `docs/evidence/r5_compute_benchmark_a0793a1.json`; runtime dir under the gitignored `.audit_tmp/`).** Closes Codex's R5 gate 4. Machine: this laptop (i9-14900HX, RTX 4070 Laptop, Python 3.13.3 / Torch 2.7.0+cu128). Workload = the real production path on the legacy 259-state fixture (explicitly a NON-SCIENTIFIC timing rig; no objective values exported).
  - **Headline numbers.** One Optuna study (100 useful trials, epoch cap 50): **`hpo_active_wall_seconds_cumulative` = 7,175.6 s ≈ 1.99 h**; 29 complete / 71 pruned / **0 failed**; per-trial duration median 59.8 s, p95 162.8 s, max 264.6 s; epochs/trial median 10.5 (the pruner is doing real work). One finalist-CV fold refit (`fit_predict_finalist_fold`, 5-fold × 2-repeat, 8,250 train samples / 165 train states): **157.2 s**.
  - **Campaign projection** against Codex's own count (~1,344–1,359 studies ≈ 135k useful trials, plus ≤~2,040 CV refits): **≈ 2,690 GPU-hours ≈ 112 days of HPO + ~3.7 days of refits ≈ 3.8 GPU-months single-GPU.** This makes the **ABLATION, not generation, the campaign's long pole** (generation is ~1–3 weeks) and inverts the earlier dispatch planning.
  - **⚠️ CORRECTION TO MY OWN EARLIER ADVICE — the 6 GB RTX 2060 is NOT disqualified.** I had argued (dispatch planning, 2026-07-24) that ablations must stay off the 6 GB card because `study.optimize(..., catch=_oom)` would silently prune large configs and make the reachable search space hardware-dependent, confounding cross-rung comparisons. **Measured peak VRAM: 842 MB allocated / 1,760 MB reserved for the entire HPO workload** — nowhere near 6 GB, and 0 failed trials. The concern was real in principle but empirically inert at this model scale, so the 2060 box can take ablation work, giving a third GPU exactly when compute is the binding constraint. Caveat: the fixture is a 2-DOF pair; re-measure before trusting it for the 8-DOF control sets and the multi-head deployment rungs.
  - **Run-to-run variance / measurement caveat.** An earlier invocation of the same workload recorded 4,666 s for the HPO phase before aborting on the HEAD-stability contract (my fault: I committed while it ran — `source_unchanged` and `fixture_unchanged` were both true, only git HEAD moved). The contract-valid re-run took 7,176 s, i.e. **1.54× slower for identical work**, almost certainly sustained-clock/thermal behaviour on a laptop. **Do not treat this laptop as the campaign timing reference** — benchmark the actual desktop machines before committing weeks of schedule.
  - **Levers, with measured impact** (user has ruled out cutting ladder rungs, correctly): **Npass 50 → 30** cuts data per rung ~40 % and therefore both generation AND per-epoch training time — the single biggest lever, ~112 → ~67 GPU-days; N_TRIALS 100 → 75 ≈ −25 %; SEEDS 3 → 2 ≈ −33 % but destroys the median-of-3 the robustness claim rests on (do NOT). **Open question for round 6:** the two deployment rungs (s16, s23) re-open 4 archs × 28 pairs and account for **~740 of the ~1,350 studies — over half the campaign**. If the s0 architecture selection is sound, re-opening all four arms twice may buy less than it costs; restricting the re-opening to the top-2 arms would save ~27 % of the entire campaign.

---

## Reference shortlist
- Kamariotis et al. (2024) — value of SHM; gradual + shock scour; € costs; CNN drive-by scour detection (5–10% stiffness loss).
- Torzoni et al. (2024, 2026) — DT decision frameworks (DBN; active inference).
- Chadha et al. (2023); Ames et al. (2025) — risk-aware / risk-sensitive SHM decisions.
- Fernandes et al. (2024–2026) — drive-by railway-bridge damage / scour.
- Adnan (2026) — scour correlation between nearby foundations.
- FHWA HEC-18 (Evaluating Scour at Bridges) & AREMA Ch.8 — design (100-yr) / check (500-yr) floods; Plan of Action gauge triggers.
- Maroni, Tubaldi, Douglas et al. — Bayesian-network DSS for bridge-scour risk; monitoring-based adaptive water-level thresholds; VoI.
- Lamb et al. (2019) — probabilistic economic risk to Britain's railway from bridge scour during floods.
- Prendergast & Gavin — bridge natural frequency as a scour indicator (≈40% drop at 10 m scour).
- Briaud et al. — SRICOS-EFA scour-rate method (cohesive vs non-cohesive soils, critical shear stress).
- Scour fragility: "A methodology to derive scour fragility functions for masonry arch bridges" (NSF par 10579270); "Flood-Induced Scour Fragility Analysis of Bridges in Different Soil Conditions" (E3S 2025); MCEER-03-0002 (lognormal fragility statistics).
- EN 61373, EN 50155 — rolling-stock vibration/shock categories & electronics.
- Sandia NL — MEMS reliability in a vibration environment.
- MDPI Sensors (2024) — MEMS inertial reliability review; dual-mechanism gyroscope degradation (Wiener).
- Tronics AXO305 / GYPRO4300; ADI ADIS16488 — rail-qualified IMU datasheets.
- (full source list in the two `papers/Confiabilidade … MEMS …` reports.)
